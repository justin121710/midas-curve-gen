# 線形建模器 — 規格 v3(群組架構)

> **v3 重新定位:** 目標是補 midas 不擅長的**空間線形建模**,不是產生一座完整的橋。
> **一組 = 一串節點 + 連接它們的元素。組與組預設互不相干,起終點在全域座標直接給。**
> 介面改為**模型樹 + 屬性面板**(左邊選一組,中間就是它的全部設定)。
> 程式:`index.html`(單檔純前端,內建 **354 項**自動驗收測試)。v2 版本備份於 `index-v1-backup.html`。

## v3.1:組裡面可以裝多個「段」

**一組 = 一串段。段與段預設接續前段終點,而且共用接縫節點(不重複)、編號連續。**

主索的實際情況就是這樣建的:**塔頂直線 → 懸鏈線 → 支承直線**,三段一組,接縫處只有一個節點。

| 段型別 | 定義方式 |
|---|---|
| 直線 | 終點座標,**或** 長度 + 方位角 + 仰角 |
| 圓弧 | 端點 + 半徑 + 偏向 + 優/劣弧 + 平面(水平/鉛直),**或** 圓心 + 轉角 |
| 懸垂 | 終點 + 垂度(拋物線 / 懸鏈線) |
| 螺旋 | 半徑(可錐形) + 起始方位角 + 圈數 + 螺距 + 旋向;接續前段時**圓心由起點與方位角反推** |
| 線形鏈 | 直線/圓弧/緩和串接 + 樁號 + 選用縱斷面 + 強制節點 |
| 節點 | 單一節點;接續前段時同時拉一根直線元素(適合塔頂、錨碇、支承點) |

- 每段有**自己的分割規則**(四種模式,見 §4.1),可**覆寫元素類型/材料/斷面**(預設沿用整組)。
- 把某段的「起點」改成自訂座標即可**斷開**,同一組裡可以有互不相連的幾段。
- 切換段型別會自動補上新型別缺少的預設參數。
- 連接族的「組對高程」加了**節點範圍**,例如主索只在懸鏈線那一段掛吊索。

## v3.2:節點可手動編輯、拖曳排序、Z 縮放

- **點俯視圖上的節點**即可編輯它的 X/Y/Z 或刪除它。**刪除後前後兩個節點直接相連**(元素跨過去)。
  - 覆寫與刪除以「該組的第幾個原始節點」為索引,存進 `g.edits` / `g.dels`,會隨 JSON / .mct 一起存檔還原。
  - 幾何仍以原始參數計算 —— 刪掉某段的最後一個節點**不會**讓後續段落位移。
  - 被覆寫的節點畫成橘點,且**不再計算弦線偏差**(它已經不在原曲線上)。
  - 分割數改變導致索引失效時會發警告,並提供「清除本組全部覆寫」。
- **排序改成拖曳**:段卡片左側的把手、樹上的組直接拖放。原本的 ▲▼ 按鈕移除。
- **立面圖的「垂直誇大」改成可調的 Z 軸縮放**:`×1` 就是與水平真正 1:1,可手動輸入任意倍率,按「自動」回到自動配合面板高度。
- **自動配號時,節點/元素起始欄位直接顯示算出來的實際編號**並反灰(以前顯示的是過期的儲存值 1001)。

## v3.3:復原、輸入修正、連接關係保證

**復原 / 重做**:`Ctrl+Z` / `Ctrl+Y`(或 `Ctrl+Shift+Z`),標題列也有按鈕。涵蓋所有會改動模型的操作 —— 改欄位、下拉、勾選、新增/刪除組、新增/刪除/拖曳段、編輯/刪除節點、載入檔案。同一個欄位 700 ms 內連續輸入算**一次**操作,不會被拆成一堆步驟。堆疊上限 80 步。

**輸入框錯亂的根因與修法**(重要,別再犯):

> 原本每按一鍵就把整塊面板 `innerHTML` 重畫,再把游標「還原」回去。
> 但 **`<input type="number">` 的 `selectionStart` 在 Chrome 是 `null`**,`setSelectionRange` 也會丟例外 ——
> 結果游標每次都被丟回位置 0,打「350」變成 `3` → `53` → `053` → 存成 **53**。

正確做法:**打字時完全不重建輸入框**。

- `input` 事件(數字/文字)→ 只更新 state、重算模型、重畫樹與畫布,面板只同步 `[data-ro]` 標記的**唯讀顯示**(區塊標題尾巴、結果欄、段卡片摘要、樁號欄、MCT 預覽)。輸入框本身完全不動。
- `change` 事件(下拉、勾選,或離開欄位)→ 才整塊重畫。
- 若唯讀元素數量對不上(代表結構真的變了),才退回整塊重畫。

**畫面畫的是「實際元素」,不是參數曲線**(v3.3 修正的視覺 bug):

> 原本俯視圖/立面圖畫的是**參數曲線的取樣**。把節點手動移開之後,線仍然沿著原本的數學曲線走,
> 看起來就像元素斷掉了 —— 但模型其實一直是連的。

現在:**粗線 = 由實際元素串出的折線**(節點被移動或刪除,線就跟著走);
**淡線(28% 透明)= 原始參數曲線**,只當參考底線,方便對照手動改動偏離了多少。

**座標覆寫保證不改變連接關係**:節點只是被移動,不會被移除或重排,所以元素的 `i–j` 配對、節點編號、節點數全部不變 —— 連相依的偏置組、組對組也一樣(有測試逐項鎖住)。另外覆寫改成**逐軸容錯**:某一軸打到一半是空的,只有那一軸沿用原始值,不會把整組覆寫丟掉害節點跳回原位。

## v3.4:立面視角、終點接續、平移旋轉、可調版面

**立面圖比照俯視圖操作**:拖曳平移、滾輪縮放(兩軸同步,Z 誇大倍率不變)、**Shift + 滾輪只縮 Z**、
**Shift + 拖曳轉視角**(改橫軸方位角)、點節點或元素直接選取。工具列上有方向下拉(自動 / 沿 +X / 沿 +Y / 自訂)、
方位角輸入、±15° 轉向鈕與「重新框選」。手動動過視角就進入**鎖定**狀態,模型重算不會把視窗拉回去,按「重新框選」才解鎖。

**段的「終點接續下段起點」**(與既有的「起點接續前段終點」互為反向):

- 開在**直線 / 圓弧(端點模式)/ 懸垂**上;條件是**下一段的起點必須是自訂座標**(否則兩段互指會循環定義)。
- 這一段的終點座標欄位會被下一段的起點取代,**接縫節點共用不重複**,元素照樣串起來。
- 下一段是「節點」型別時,那個節點就由前段的終點兼任,不會多產生一個重合節點。
- 典型用途:先擺好一個錨碇點 / 支承點,再叫前面那根線自己拉過去;之後改錨碇座標,線就跟著走。

**平移與旋轉**(四個層級,全部是剛體變換 —— 元素長度、弦線偏差、編號、`i–j` 連接都不變):

| 層級 | 存在哪 | 行為 |
|---|---|---|
| 組 | `g.xf`(參數,可隨時改、可復原) | 整組節點一起動;依附本組的偏置 / 連接組會跟著移動 |
| 段 | `pc.xf`(參數) | 只動這一段;**接續本段的後面幾段跟著走**,旋轉中心預設是本段起點,所以接縫不會被扯開 |
| 節點 | 一次性 → 寫成 `g.edits` | 套用即固化成座標覆寫,可按「還原座標」取消 |
| 元素 | 一次性 → 寫成 `g.edits` | 同時移動兩端節點 |

- 參數:`dx / dy / dz`、`rot`(**逆時針為正**)、`axis`(Z / X / Y)、旋轉中心(自動 / 起點 / 範圍中心 / 自訂座標)。
- Z 軸旋轉時節點的方位角 `az` 直接減去轉角(偏置組才不會算錯法線方向);繞 X / Y 轉則清掉 `az` 由 `annotate()` 重算。
- 段變換也會**同步套用到參考用的參數曲線**(`info.curve` / `info.evalAt` 包一層),否則淡色底線會跟實際元素分家。
- 「下一段用範圍中心旋轉」+「前一段終點接續它」會有先後相依,無法精確求解,此時發警告請使用者改用起點或自訂中心。

**自動配號改名為自動編號**,勾選框換成大顆切換鈕:按下 = 主題色實心,彈起 = 白底。

**版面可調**:面板與預覽之間有一根分隔桿,拖曳即可改預覽區寬度(雙擊回 330 px);
標題列的「⇄ 版面」把輸入區與預覽區左右對調(預覽變中間的彈性欄、輸入變右側固定欄)。
兩者都存在 `localStorage`。畫布高度改用 `clamp(…, vh, …)`,視窗越高畫布越大。

> **對調版面時 grid 會爛掉的原因**(踩過一次):四個項目只寫了 `grid-column`,列讓它自動配置。
> 自動配置的游標**只會往前走**,而 DOM 順序是 pane → resizer → right;對調後 pane 指到第 4 欄,
> 後面兩個要退回第 3、2 欄就只能換到下一列 —— 一列變三列,整個版面散開。
> 解法:四個項目一律 `grid-row:1`(RWD 斷點裡也各自寫死列號)。

**編輯中的組:各段分色**。選到某一組時,它的每一段在俯視圖與立面圖用不同顏色畫(元素折線、節點、
淡色參考曲線都跟著),段卡片左上角的序號徽章、圖例也用同一組顏色對照;**沒被選到的組一律維持自己的主題色**,
只有一段的組也不分色。實作上折線改成依段切開(`elemPolysBy()` / `curvePolys()` 回傳 `{ pi, pts }`),
顏色由 `colOf(r, pi)` 決定 —— 條件是「這一組正在被編輯,而且不只一段」。

**順手修掉的 bug**:段的「起點」下拉存進 state 的是字串 `"0"`/`"1"`,而判斷式寫的是 `pc.cont !== false`,
所以**把非第一段改成「自訂座標」其實沒有生效**。現在 `assignFromEl()` 對 `cont` / `endCont` 轉成布林,
`normState()` 也會把舊檔的字串正規化。

---

## v3.5:單位定義擺最上方,輸入即輸出

**單位在標題列設定,是整份模型的唯一定義**(長度 + 力),不再藏在「輸出」頁裡。

**規則只有一條:輸入什麼單位,就算什麼單位,匯出的 .mct 也是什麼單位 —— 全程不做任何換算。**
畫面上的欄位標籤、KPI、CSV 標題、比例尺都跟著顯示目前單位;.mct 開頭的 `*UNIT` 就寫這個單位,
`*NODE` 的座標**就是使用者輸入的那些數字**。

- v3.4 以前是「一律以公尺輸入,匯出時再乘換算係數」,現在**整條換算路徑拿掉**(`mlen` = `mnum`)。
- **弦線偏差容差**從固定的 `mm` 改成 `split.tol`(當前長度單位);舊檔的 `split.tolMm` 由 `normState()`
  自動搬成 `tolMm/1000`(舊檔一律是公尺模型)。
- **樁號的 `0k+125.500` 只在單位是 M 時使用**;其他單位退回純數字,免得 "0k+125.500 mm" 這種鬼東西。
- **物理判準跟著換算**:KPI 的「弦線偏差 > 50 mm」「最短元素 < 0.3 m」用 `inU()` 換到當前單位比較,
  換單位後警示不會失準。
- **新增段 / 組的預設尺寸**乘上單位係數(`dL()`),在 MM 底下不會生出一條 100 mm 的線。
- **緩和曲線的積分步長改成固定 4000 等分**。原本是 `h = min(0.1, Ls/1000)` —— 以公尺寫死,
  在 MM 底下 `Ls = 50000` 會變成 50 萬步,直接把頁面卡死。

**換單位時可以選擇要不要一起換算數值**(有組時才問):

- **確定** → `scaleModel(state, k)` 把所有長度性質的欄位乘上比例:座標、長度、半徑、螺距、樁號、
  縱斷面、分割間距/容差、平移量與旋轉中心、節點座標覆寫、強制樁號字串 —— **幾何大小完全不變**,
  只是換一種單位表示(有測試鎖住:M→MM 節點數與 `i–j` 不變、座標與偏差都恰好 ×1000,來回換算回到原值)。
- **取消** → 數值原封不動,直接當成新單位讀(等於重新定義尺寸)。
- 兩種都進復原堆疊,`Ctrl+Z` 可以整個退回去(單位與數值一起)。

> **踩到的坑:`num("")` 回傳的是 0 不是 NaN**(因為 `+"" === 0`)。
> 換算時把螺旋的「終止半徑(留白 = 等半徑)」從 `""` 變成了 `0`,整段螺旋就報「半徑必須 > 0」。
> 凡是「留白代表沒有這個值」的欄位,批次處理前都要先擋掉 `""` / `null` / `undefined`。

---

## v3.6:兩個畫布合成一個 3D 視圖

俯視圖與立面圖**合併成單一畫布**,正射投影、可以自由旋轉;原本兩張圖只是這個相機的兩個預設角度。

**相機 = 方位角 az + 仰角 el**(正射,沒有透視變形,量測才可靠):

```
螢幕右 r = ( sin az,        cos az,       0      )
螢幕上 u = (−cos az·sin el, sin az·sin el, cos el )
視線   f = (−cos az·cos el, sin az·cos el, −sin el)   ← 指向螢幕內
```

- `el = 90°` → 俯視,而且**北上、東右,和舊的平面圖完全一致**;`el = 0°` → 立面,`az = 90°` 就是舊的預設立面。
- **相機所在的方位(站在哪一側往模型看)= az + 90°**。北側 az=270、東側 az=0、南側 az=90、西側 az=180。
- 螢幕 → 世界的反投影只在「指定高程的水平面」上求解,行列式 = `sin el`,所以**純立面時無解**(游標讀數會改成只顯示節點資訊)。

**九宮格視角鈕(仿 midas Civil)**,擺在畫布右上角,依羅盤方位排列(北在上):

| | | |
|---|---|---|
| ↘ 西北 45° | 北 立面 | ↙ 東北 45° |
| 西 立面 | **TOP** 俯視 | 東 立面 |
| ↗ 西南 45° | 南 立面 | ↖ 東南 45° |

四個角是相鄰兩側之間、再俯視 45°(`el = 45`)。目前視角命中哪一顆就把那顆點亮。

**滑鼠操作與 midas Civil 一致**(使用者要求「一模一樣」):

| 操作 | 動作 |
|---|---|
| **中鍵拖曳** | 平移 |
| **Ctrl + 中鍵拖曳** | 旋轉(模型跟著手走:往右拖 az 增加、往下拖 el 增加 = 看到更多頂面) |
| **Shift + 中鍵拖曳** | 縮放(往上拖放大) |
| 滾輪 | 縮放,游標下的世界點不動 |
| **左鍵** | **只做點選**(節點 / 元素),不平移也不旋轉 |

左鍵完全不碰視角,是為了避免想點節點卻把畫面拖走;要重新框選用工具列的按鈕。
中鍵按下時要 `preventDefault()`,不然 Windows 的 Chrome 會啟動自動捲動;
滑鼠移出畫布後放開中鍵的情況用 `e.buttons & 4` 補判。

**格線分兩種模式**:`|sin el| > 0.12` 畫**地面格網**(在模型最低高程的水平面上,並且只畫視窗範圍內的線,
不然大模型會畫上萬條);接近立面時退回**等高程的水平格線 + 高程標籤**(那個角度下等高線在螢幕上本來就是水平的)。
右下角加了 **X / Y北 / Z 三軸指示**,長度就是各軸的投影縮短率,一眼看得出目前的視角。

**Z 誇大**(原本的立面 Z 縮放)保留成 `view.zEx`,投影前先把 Z 相對視點放大;比例尺量的是**與螢幕平行**的長度。

---

## v3.7:偏置改成「複製 + 變換」、每組可手動加點與元素、MCT 全英文

### 7.1 偏置不再是一種組別

**偏置變成「對任何一組做的動作」:組的 ⋯ 按鈕或右鍵 → 「偏置複製…」。**
產生的是一個**同型別、內容完全相同的複本**(`clone` + 新 id + 新名字),偏置量疊進複本的 `g.xf`,
之後仍可在新組的「平移與旋轉(整組)」隨時微調。對話框給 dX / dY / dZ、旋轉角、旋轉軸、份數;
份數 > 1 會**連續疊加**(dY = 5、份數 3 → 產生 +5 / +10 / +15 三組)。

> **⚠️ 幾何語意變了,不是等價替換。**
> 舊的偏置組是**沿基準組法線投影的平行曲線**,曲線上當地半徑 = `R − 偏向·d`;
> 新的偏置複製是**剛體平移**,複本的半徑與原曲線**相同**,不是平行曲線。
> 這是使用者要求「偏置後的組等級與內容都要複製原本的」必然的取捨 ——
> 平行曲線根本沒辦法用原組的參數(直線 / 圓弧 / 緩和曲線)表示出來,要「內容完全相同」就只能是剛體。
> 需要真正的平行曲線時,§5.1 的法線投影邏輯仍在程式裡(舊組別),但不再提供新增。

**舊的 `OFFSET` 型別標成 `legacy:true`:仍然可以正常建立與匯出,只是不出現在「+ 新增組」選單裡。**
這是為了讓使用者既有的 .json / .mct 還原回來不會壞掉。

### 7.2 平移與旋轉開放給所有組別

原本只有曲線組與偏置組有「平移與旋轉(整組)」,現在**每一種組都有**,只作用在**本組自己的節點**上。

- 幾何族:全部節點都屬於自己 → 剛體,元素長度與弦線偏差都不變(和以前一樣)。
- **連接族:元素有一端在別組,平移後長度會真的改變。**
  所以 `buildOne()` 在套用變換之後必須呼叫 `refreshElemGeom()` 重算每根元素的 `chord` / `len3d`,
  否則面板、CSV 與檢核會顯示**變換前的舊長度**。UI 的提示文字會依族別分別說明這件事。

### 7.3 每組可以手動新增節點與元素

每個組的屬性面板都多一個「手動節點與元素」區:

| | 存在哪 | 怎麼給 | 編號 |
|---|---|---|---|
| 手動節點 | `g.addN = [{x,y,z}]` | 直接輸入座標 | 接在本組自動節點之後 |
| 手動元素 | `g.addE = [{i,j}]` | **在 3D 視圖上點兩個節點** | 接在本組自動元素之後 |

- **手動元素的兩端可以落在任何一組** —— 這正是它的用處:把兩組手動接起來。
  因此 `addE` **必須等所有組都建完才能解析**(`addManualElems()` 在 `buildModel()` 裡跑),
  不能放在單組的 `buildOne()` 內,否則引用還沒建立的組會找不到節點。
- 端點找不到 / 兩端同一點 → 記進 `errs` 並跳過該根,不影響其他元素。
- 手動元素用**整組的類型 / 材料 / 斷面**。
- 手動節點會跟著整組的平移旋轉一起走。
- 點選模式 `PICK` **不進 state、不進復原堆疊**(和一次性變換欄位 `XFT` 同一個原則),
  它只是「現在正在點第幾個端點」的暫時狀態。

> **關鍵設計:手動節點不能混進下游組看到的節點串。**
> 手動節點沒有 `az` / `k`,偏置組拿去算法線投影會得到 **NaN 座標**,KPI 直接顯示 NaN(實際踩過);
> 而且使用者只是補一個輔助點,卻讓既有偏置 / 連接組的節點數跟著改變,也違反「獨立新增」的預期。
> 解法是 `chainOf(r)` —— 回傳過濾掉 `manual` 的節點串,`OFFSET` / `LINK2` / `LINKZ` / `FAN`
> 一律改用它取代直接讀 `r.nodes`。手動節點仍然照常匯出、可被點選、可當手動元素的端點。

### 7.4 所有節點都能被選取

`pickNodeAt()` / `pickElemAt()` 拿掉了 `type !== "CURVE"` 的過濾 ——
**不論屬於哪一組、目前正在編輯哪一組,畫面上每個節點都選得到。**

選取的 key 從「原始節點索引 `ri`」改成**節點編號 `SELNODE.num`**,因為只有曲線組才有 `ri`。

能不能**改座標**是另一回事,由 `nodeEditable()` 判斷:

| 節點來源 | 可選取 | 可改座標 | 面板 |
|---|---|---|---|
| 曲線組的原始節點 | ✓ | ✓(寫進 `g.edits`) | 完整編輯 + 刪除 + 平移旋轉 |
| 手動新增的節點 | ✓ | ✓(直接改 `g.addN[i]`) | 座標欄位 + 刪除 |
| 偏置 / 連接族推導出的節點 | ✓ | ✗ | 唯讀座標 + 說明來源 |

唯讀的節點仍然可以拿來當**手動元素的端點**。

### 7.5 .mct 一律純 ASCII

`.mct` 匯入時常以系統 ANSI 編碼讀取,**中文會變亂碼**,所以整份檔案不留任何非 ASCII 字元。

- 所有註解文字改成英文。
- **`*GROUP` 的 `NAME` 是功能性欄位,不是註解** —— 這才是真正的問題所在。
  `mctName()` 只保留 `[A-Za-z0-9_]`、其餘轉底線;整串都不是英數(純中文名)時**退回 `G01` 這種序號**。
  `assignGNames()` 統一去重:**原名相同的組本來就會合併成同一個 MCT 群組(維持原行為)**,
  只有「原名不同卻清洗後撞名」才加序號後綴。
- 「基本」區會顯示**實際匯出的群組名**,而且在退回序號時提示使用者在名稱裡加英數。
- 檔頭的參數 JSON 用 `asciiJSON()` 把非 ASCII 轉成 `\uXXXX`,仍是合法 JSON,
  拖回頁面還原後與原字串完全相同。

> **踩過的坑:`asciiJSON` 的字元類別。**
> 一開始寫 `/[^\x20-\x7E]/g`,結果把 `JSON.stringify` 縮排用的**換行也轉成 `\u000a`**,
> 逸出序列跑到字串外面 → 整份 JSON 再也 parse 不回來,「.mct 拖回頁面還原」**靜默失效**。
> 正解是只轉 `/[\u007F-\uFFFF]/g`。內建測試抓到了這個。

### 7.6 時間一律本機時區

`toISOString()` 是 UTC,台灣會少 8 小時。改用 `localStampText()` / `localStampFile()` / `localStampISO()`,
檔名與 .mct 檔頭都是本機時間,存進參數 JSON 的時間戳**自帶 `+08:00` 偏移**,日後不會誤讀。

### 7.7 組對組補上節點範圍

`LINK2` 加上 `from` / `to`,行為與組對高程 (`LINKZ`) 一致(訖填 **0 = 到底**),
可以只連接其中一段(例如只在跨中連橫繫桿),不必再為此拆組。

### 7.8 這一版的測試

驗收測試 246 → **266 項**。新增涵蓋:手動節點編號與座標、手動節點不影響下游組、
手動元素跨組連接與編號、端點不存在時報錯、組對組範圍與 every、偏置複製後同節點數 /
剛體長度不變 / 連接族平移後長度有重算、`.mct` 全檔純 ASCII、`*GROUP` 名稱為 ASCII。

> **測試 fixture 的坑**:`build()` 對段型別的 fixture 會把「不認得的鍵」全部塞進 `pieces[0]`,
> 所以 `addN` / `addE` / `xf` 這些**組層級**欄位必須在解構時明確列出來,
> 否則測試會靜默測到「段」而不是「組」,而且看起來還會通過。

---

## v3.8:段間任意參照、面板重整、單位寫法、midas 名稱庫

### 8.1 段的起點與終點可以指到任何一段

原本只有兩個布林(起點接前段、終點接下一段起點),而且終點只能接「下一段」。
現在兩端都改成**參照**:`sRef` / `eRef`,值是 `""`(自訂座標)、`"prev"`(前一段終點)
或 `"<段索引>:<s|m|e>"` —— **同組任何一段的 起點 / 中點 / 終點**都可以指。

**介面上分兩層下拉**:先挑「哪一段」,挑完才出現「起點 / 中點 / 終點」。
把所有段 × 三個位置攤平成一列會爆炸(10 段就 30 個選項),而且很難掃。
存進 state 的仍然是單一個字串,兩個下拉由 `applyField()` 的 `…RefG` / `…RefA` 分支拼回去:

- 選了段但還沒選位置 → 預設 `:s`(起點)
- 只改位置 → 段不變
- 換段 → **保留原本選的位置**
- 退回「自訂座標 / 接續前段終點」→ 位置下拉一起收起來


> **第一段沒有「前一段」可以接**,所以它的 `sRef` 不能是 `"prev"`。這件事有四個入口要顧到:
> 新建組(`defaultGroup`)、新增段、**刪除第一段後遞補上來的那一段**、**拖曳排序後排到第一位的那一段**。
> 統一收斂成 `fixFirstPiece()`,而且**只清 `"prev"`** —— 第一段指到**別的段**是合法的
> (例如反過來接第 3 段的起點),不能一起清掉。
>
> **踩過的坑**:`defaultGroup` 只留著舊的 `cont:false`(改用 `sRef` 之後那個欄位已經沒作用),
> 第一段因此帶著 `defaultPiece` 的 `sRef:"prev"` 進到畫面 —— 起點座標欄位被藏起來、下拉還選不到值。
> **新建的組不會經過 `normState()`**,所以舊檔沒事,只有新建的組會中。
> 面板另加一道保險:第一段就算殘留 `"prev"` 也顯示成自訂座標。

因為參照可以任意連,段就不能再照索引順序算了。`orderPieces()` 先做**相依排序**,
算完再依段的索引順序把節點串回去(元素仍然沿段的順序連接)。**循環參照會報錯並退回自訂座標,不會當掉。**

> **使用者的例子:段 1 與段 2 的終點同時指到段 3 的起點** —— 可以,兩段都會拉到那個點,
> 而且**真的會接在一起**(見 §8.1.1 的節點合併)。

**產生階段**只有「起點接前一段的終點」會共用接縫節點 —— 曲線組內部是「一串段」的線性鏈,
不相鄰的段之間沒辦法在產生時就共用。所以上面的例子在產生階段會留下**兩個重合節點**,
接著由下一節的合併把它們併掉。

### 8.1.1 合併座標重合的節點(等同 midas 的 Merge Nodes)

**同一個位置只留節點編號最小的那一個**,其餘刪掉,所有元素的端點改指到留下來的那個。
`weldNodes()` 跑在 `buildModel()` 的最後 —— 節點編號是全域的、手動元素也要一起改,
所以必須等所有組(含手動元素)都建完。

- 掃描用**空間雜湊**(格子邊長 = 容差),並檢查相鄰的 26 個格子,
  免得剛好跨在格線兩側的兩點被漏掉;是 O(n) 不是 O(n²)。
- 依**節點編號由小到大**掃,先看到的自然就是那一群裡編號最小的 → 留下來的一定是最小號。
- **跨組也會合併**(連接族接到既有幾何時常常這樣),被併掉的那個節點就從它原本的組移除。
- 兩端被併成同一點的元素會**退化成零長度**,直接刪掉並回報數量。
- **容差**可設(預設 `0` = 座標完全相同才併)。

> **副作用要講清楚:節點編號會出現跳號。** 例如 1006 被併進 1003 之後,該組的編號變成
> 1001–1005、1007…。MCT 不要求編號連續,所以功能上沒問題,但畫面上顯示「節點範圍」
> 就不能再用 `nodeBase + 數量 − 1` 去推,得看實際的 min–max(`numRange()`)。

**預設開啟**,可在「編號總表」關掉。關掉時退回原本的行為:只列出重合的節點編號當警告。

> 完全重合的兩個節點在 FE 模型裡幾乎一定是錯的(看起來相連、其實各自獨立),所以預設開。
> 已存檔的模型如果本來就有重合節點,打開本版後節點數與編號會改變 —— 這也是為什麼合併一定會回報。

### 8.2 餘數處理多一種:頭尾各分一半

`rem = "half"` —— 把餘數除以二,**頭尾各留一半**,中間維持標稱間距:

```
全長 100、間距 30 → 餘數 10 → 站點 0, 5, 35, 65, 95, 100
                              頭 5 · 30 · 30 · 30 · 尾 5
```

> **實作時踩到的**:最後不能拿終點去**蓋掉**最後一個整距站點,要用 push 補上去,
> 否則尾段會變成 `nominal + rem/2`(以上例就是 35 而不是 5),頭尾根本沒平分。內建測試抓到了。

### 8.3 段的顏色綁在段身上

顏色槽 `pc._c` 存進 state,`pcSlot(g, pi)` 取用。**拖曳排序只換編號,配色不動** ——
原本咖啡色的懸垂段被拉到第 1 位,它還是咖啡色。舊檔由 `normState()` 照當時順序補上 `_c`,外觀與以前一致。

### 8.4 屬性面板重整

| 區塊 | v3.8 的位置 |
|---|---|
| 元素(整組預設) | 併進**基本** |
| 編號 | 併進**基本** |
| 平移與旋轉(整組) | 改成**小視窗**,入口在標題列(和「偏置複製…」並排) |
| 節點 | **沒點到節點就整區不顯示**(還留著覆寫 / 刪除紀錄時才露出來讓人清得掉) |
| 纜索 | 新增,見 §8.6 |

平移旋轉的小視窗**直接改 `g.xf` 以保留即時預覽**,「取消」把開啟前的值放回去。

**左側組清單也可以拖曳調整寬度**(`--tw`,存 localStorage,雙擊還原),與右側預覽區同一套機制。

### 8.5 單位一律用公制寫法

顯示改成 `m` / `cm` / `mm`、`kgf` / `kN` / `N` / `tonf`,不再全大寫;
**預設改為 `m` + `kgf`**。`.mct` 的 `*UNIT` 仍然寫大寫代碼(`M`、`KGF`)—— 那是格式要求,不能改。

3D 視角的「Z 誇大倍率」改稱**「Z 放大倍率」**。

### 8.6 纜索設定下放到各組

原本掛在全域「輸出」頁的 iSUB / EXVAL / EXVAL2 全部移到**組層級**(`g.cable`),
而且**只有該組(或它某一段)的元素類型真的是 CABLE 時才顯示**。
各組可以有不同的初始張力 —— 這解掉了 v3.7 記下的「全模型共用一組」限制。

介面上**不再出現 iSUB / EXVAL 這種代碼**,只給 midas 裡看得到的名詞:

| 欄位 | 選項 | 寫進 MCT |
|---|---|---|
| 子類型 | Cable / Truss(允許有限壓力) / Hook | `iSUB` = 3 / 1 / 2 |
| 初始值種類(Cable 才有) | Lu(無應力長度) / Pretension(預拉力) / Horizontal(水平分力) | `EXVAL2` = 3 / 1 / 2 |
| 數值 | 標籤跟著種類換單位 | `EXVAL` |

### 8.7 匯入 midas 的材料與斷面名稱

在 midas Civil 把斷面尺寸與材料參數建好 → Export `.mct` → 拖進本工具(或用「輸出 → 匯入 midas .mct」),
`parseMctLib()` 讀 `*MATERIAL` 與 `*SECTION` 兩個區塊(欄位:`iMAT/iSEC, TYPE, NAME, …`,
**第 1 欄是編號、第 3 欄是名稱**),存成 `state.lib`。

之後所有「材料 / 斷面」欄位(組層級與段的覆寫)都變成**下拉,顯示「3 — C40」**,不用再對編號。
模型裡用到但清單裡沒有的編號會標成「(不在匯入的清單裡)」而不是被吃掉。

只讀這兩個區塊,**不動幾何**;同一個檔案若同時含本工具的參數區塊,兩者互不衝突。

### 8.8 這一版的測試

266 → **354 項**。新增涵蓋:餘數頭尾平分的站點與頭/中/尾段長、均分吸收未回歸、
段排序後顏色跟著段走、兩段終點同指一段起點、起點指到別段中點、循環參照報錯不當掉、
關掉合併時會點名重合節點、單位寫法與預設、`parseMctLib` 讀材料 / 斷面且不誤讀 `*NODE`、
各組可有不同的纜索初始值;以及節點合併:留下編號最小者、元素端點改指、跨組合併、
容差生效與否、退化元素被刪除、合併會回報。

> **測試 fixture 又踩一次同類的坑**:`buildPieces()` 會先展開 `defaultPiece()` 再蓋上測試給的參數,
> 所以 `sRef` / `eRef` **永遠是有值的**,`normState()` 裡「舊欄位才遷移」的判斷不會觸發,
> 8 個舊的 `endCont` 測試整組失敗。真實的舊檔沒這問題(它們根本沒有新欄位)。
> 修法是讓 fixture 自己把 `cont` / `endCont` 翻成 `sRef` / `eRef`,
> 另外用一個「真的刪掉新欄位」的 fixture 去測遷移本身。

---

## v3.9:編號全自動、纜索併進元素設定、捲動位置不再跳

### 9.1 編號一律自動

每組的「自動編號」切換鈕與起始號欄位**整組拿掉** —— 編號永遠是
`全域起始 + 本組在清單中的順序 × 號段增量`,起始號與增量在「編號總表」統一調。

`g.autoNum` / `g.nodeBase` / `g.elemBase` 三個欄位從模型移除,`normState()` 會把舊檔殘留的
`delete` 掉。**舊檔如果用過手動編號,載入後會改用自動編號**,節點與元素編號因此改變。

> 保留手動編號的成本不只是那幾個欄位:每個組都要檢查衝突、UI 要多一組狀態、
> 而群組輸出本來就是設計來一次選取整批的,手動編號的實際用途很有限。

### 9.2 纜索設定併進「元素(整組預設)」

原本是獨立的一個 SEC,現在變成元素預設底下的一個小標題(`cableFields()`),
**只有整組或某一段的元素類型是 `CABLE` 時才出現**。同一個區塊裡看得到
「類型 / 材料 / 斷面 → 子類型 / 初始值種類 / 數值」,不用再跳到別的區塊。

材料 / 斷面欄位在**還沒匯入名稱庫**時會多一行指路,告訴使用者去
「輸出 → 材料與斷面名稱」匯入 midas 的 .mct(或直接把檔案拖進頁面)。

### 9.3 屬性面板重畫後捲動位置不再往上跳

**症狀**:在任何欄位輸入完值(數字、下拉、勾選)之後,整個面板會往上滑一截。

**原因**:`renderPane()` 整塊重寫 `innerHTML`,新內容一貼上去時所有 `<details>` 都是
HTML 裡寫的**收合**狀態 → 內容瞬間變矮 → 瀏覽器把 `scrollTop` 夾到新的上限;
接著 `restoreOpen()` 才把卡片展開回來,但 `scrollTop` 已經被夾小了,畫面就往上跳。

**修法**:重寫前把 `scrollTop` 存下來,`restoreOpen()` 之後再放回去。
只有 `FORCE_OPEN`(明確要展開某張卡片,例如剛新增一段)時不還原,讓它自己捲過去。

> 這是「整塊 innerHTML 重畫」的第二個副作用了 —— 第一個是 v3.7 的 `<details>` 展開狀態
> (見 v3.7 的 capture/restore)。同一套 UI 模式,狀態要自己保存的東西不只一種。

---

## v3.10:懸垂的垂度基準可選、動過的段不再留參考曲線殘影

### 10.1 垂度基準(`fBase`)

原本垂度一律**自弦線量**。兩端等高時無所謂,但**鞍座不等高**時「自弦線量 20 m」跟
「自高側鞍座往下 20 m」差很多,而實務上給的通常是後者。現在多三種基準:

| 基準 | 意義 |
|---|---|
| `chord` | 自兩端連線量垂距(**預設,與舊版完全相同**) |
| `high` | 自**高側端點**的水平面往下量到**曲線最低點** |
| `low` | 自**低側端點**的水平面往下量到曲線最低點 |
| `z` | 自**指定高程**往下量到曲線最低點 |

`z` 模式旁邊有一顆按鈕:先在 3D 視圖點一個節點,再按它就把**那個節點的高程**帶進來
——「直接點選節點,自該處向下計算」。

**解法**:後三種是「已知最低點要落在哪個高程」,但曲線是用**自弦線的垂度 `fc`** 參數化的,
兩者沒有封閉解(尤其懸鏈線)。垂度越大最低點越低,是**單調**的,所以直接對 `fc` 二分:
求 `datum − min(z(u)) = f`。拋物線與懸鏈線共用同一套,不必各寫一份公式。

> **不可解的情況會報錯而不是硬算**:例如兩端高差 30 m,卻要求「自高側往下只有 20 m」——
> 低側端點本身就已經在高側下方 30 m,再怎麼拉直都做不到。錯誤訊息會講明白。

### 10.2 節點被動過的段,不再畫「原始參數曲線」

畫面上一直有兩層:**粗線 = 實際元素串出的折線**、**淡線(28%)= 原始參數曲線**。
節點被手動移動或刪除之後,淡線還停在原本的位置 —— 看起來就像舊的那一條留在圖上。

改成:建模時記錄哪些段的節點被動過(`info.touched`,座標覆寫與刪除都算),
`curvePolys()` 直接跳過那些段。**只影響被動到的段**,同一組其他段的參考線照舊。

> 參考線本來的用途是「對照分割密度夠不夠」;一旦節點被手動改過,它已經不代表現況,
> 留著只會誤導。這也是 v3.3 那條「一定要畫實際元素折線」的延伸。

---

## v3.11:直線可兩端延長、段改用「編號徽章」而不是分色

### 11.1 直線的兩端延長(`extS` / `extE`)

已經畫好的直線要再伸出去時,不必回頭改座標:填一個長度,**沿這條線自己的方向往外多接一段**,
方向與角度都不變。兩端各有一欄,**只吃 0 或正值**(要縮短請改終點座標)。

> **延長 = 增加元素,不是把整條線重新分割。**
> **原本那段的節點座標與元素完全不動**,延長段自己再依同一套分割規則切,
> 切不滿一格的零頭照使用者選的**餘數處理**(均分吸收 / 頭尾各半 / 獨立留一段 / 併入前一元素 / 截短)決定。

**做法**(`splitLineExt()`):幾何上先把線畫到含延長的全長,但**分割時分三段處理** ——

1. **基準段**用原本的 `split` 參數切。長度與參數都沒變,所以切出來的站點與沒延長時**逐點相同**。
2. 延長段用一個**標稱間距**改走「指定間距」模式:間距模式沿用該間距、
   容差模式用 `maxSpacing`、元素數模式沿用**基準段實際的元素長** `L/N`。
3. 三段的站點合併、排序、去重。

**起點端是由基準段起點「往外」切的**(`dir = -1`),整距元素才會貼著基準段、
零頭落在**最外側**;直接由外往內切的話零頭會卡在接縫上。

> **被別的段定義的那一端不延長。** `sRef` / `eRef` 指到別段的意思是「就在那個點上」,
> 再往外推等於自我矛盾,而且會把共用的接縫節點扯開。引擎層看 `start` / `endAt`
> (以及 `cont`,涵蓋「前一段把終點接到本段起點」)直接擋掉,UI 上那一欄也會反灰並說明原因。

**例**(基準 0–100、`count = 4`,兩端各延長 30,間距 25):

| 餘數處理 | 節點 |
|---|---|
| 獨立留一段 | `-30, -25,` **`0, 25, 50, 75, 100,`** `125, 130` |
| 併入前一元素 | `-30,` **`0, 25, 50, 75, 100,`** `130` |
| 均分吸收 | `-30,` **`0, 25, 50, 75, 100,`** `130` |

粗體是基準段 —— 三種情形完全一樣。

### 11.2 段不再各自分色,改用畫在段中間的編號徽章

**原本**:正在編輯的曲線組會把各段染成不同顏色(`PCOLORS`)。
**問題**:那組顏色會跟**別組的主題色**撞在一起 —— 尤其被選到的組第一段是藍色,
跟其他藍色的組根本分不出來;而且顏色數量有限,段一多就得重複。

**現在**:
- **整組一個顏色**(組的主題色),`colOf(r)` 不再看段。
- 段別靠**畫在該段中間的圓形編號徽章**分辨(組色實心 + 白色描邊 + 白字)。
- **正在編輯的組用「加粗」表示**(3.6 px vs 1.9 px),**不換色**。

徽章位置取該段**實際折線的中點**(沿長度取一半,不是取中間那個節點 —— 節點被刪過時才不會偏掉)。
同一段被斷點切成好幾串時取最長的那一串;幾個徽章擠在一起會自動往上錯開。

> **徽章要畫在整個群組迴圈之後。** 一開始畫在各組自己的迴圈裡,結果**立面視角**下
> 兩條 Y 不同的主索投影重疊,後畫的組直接把前一組的徽章蓋掉(實際踩到,用畫布取樣才發現)。
> 徽章是**標註**,一律最上層。

`PCOLORS` / `pieceColor()` / `pcSlot()` / `freeSlot()` 與段上的 `_c` 欄位**整組移除**,
`normState()` 會把舊檔的 `_c` 刪掉。

---

## v3 型別(組層級):兩族共 5 種

| 族 | 型別 | 定義方式 | 取代 v2 的 |
|---|---|---|---|
| 幾何 | **線**(v3.14 前叫「曲線組」) | 一串段(見上表);段與段共用接縫節點 | 主線 / 主纜 / 拱肋 / 匝道… |
| 幾何 | ~~偏置~~(**v3.7 起為舊型別**) | 依附某組,橫向 d / 垂直 h | 偏置線 |
| 連接 | **對接**(前「組對組」) | 兩排節點依**配對方式**連接(見 §v3.14.1) | 橫梁 / 橫繫桿 / 斜撐 |
| 連接 | **垂落**(前「組對高程」) | 每節點垂直連到指定高程,可指定節點範圍 | 吊索 |
| 連接 | **扇出**(前「點對組」) | 一點連到某組的節點區間(扇形),可分割 + 垂度 | 斜張索 |

> **v3.14 改名 + 圖示。** 組別名稱不再帶「組」字,也不再用「組對組」這種描述關係的抽象名:
> 線 / 對接 / 垂落 / 扇出。清單的型別欄改成圖示,新增選單改成圖卡。
> 圖示規則只有一條:**這個組真正會產生的元素 = 主題色粗線(`currentColor`),
> 只是用來說明位置的既有幾何 = `ICON_CTX` 淺灰細線**。所以垂落凸顯的是吊下來那幾根,
> 來源曲線與目標高程都退成灰。「對接」的主題色同時從灰 `#7b8592` 換成綠 `#3f8f5f` ——
> 原本跟背景線同色系,粗細再怎麼拉都凸顯不出來。

### v3.33 螺旋的半徑由「層間淨高」反推:3 → 5.5 m

**層間只有 1.51 m,人走過去會壓迫。** v3.30 把坡度鎖在 8%、半徑留在 3 m,
螺距就只剩 `0.08 × 2π × 3 = 1.508 m` —— 那是**層與層的垂直間距**,
也就是走在下面一圈時頭上的淨高,比一般樓層還矮。

**三個數字裡只能動一個。** 螺距 = 坡度 × 2πR,而「至少 6 圈」「夾角 120°(⅙ 圈)」
管的是圈數、不影響螺距。所以要 2.5 m 淨高只有兩條路:

| 改哪個 | 結果 | 取捨 |
|---|---|---|
| 坡度 8% → **13.3%** | 半徑保持 3 m | 人行坡道 13% 太陡,無障礙直接出局 |
| **半徑 3 → 5.5 m** | 坡度保持 8% | 佔地變大,但坡度是規範、半徑不是 |

取後者:**R = 5.5 m → 螺距 2.765 m**。這個數字不是湊 2.5 的下限
(下限是 R ≥ 4.97 m、螺距剛好 2.513 m)—— 中心線模型沒有畫踏面結構厚度,
留 0.25 m 左右給梁深,**實際淨高才還有 2.5 m 以上**。
總爬升跟著變成 **17.05 m**(6⅙ 圈 × 2.765 m)。

順帶把 v3.30.2 為了「第一圈爬太慢」而跳過的第二根落地柱**收回來**:
螺距回到 2.77 m 之後,φ = 110° 那根已經離地 0.85 m,不再是超短元素,
落地段回到 `from: C0 + CS`(只跳過 φ = 20° 那一根,它離地 15 cm)。

外柱柱距**維持 90°**。半徑放大之後引道路面的禁區從 62.4° 縮到 48.2°,看起來 60° 塞得下,
其實不行:60° 一根時柱子的角度固定落在 {10°, 70°, …, 310°},310° 那根離引道路面只剩
**0.16 m**,擦邊。90° 那組(20/110/200/290)最近也有 2.4 m。

測試補一條「層間淨高(螺距)≥ 2.5 m」,半徑那條從 3 改成 5.5。驗收測試 642 → **643 項,全過**。

### v3.32 圖上點選改成一顆開關,預設只選群組

**問題:平常在圖上點來點去,多半只是要換看哪一個群組。** 但舊版每一下都會順手選到
**節點或元素** —— 面板立刻跳去展開那一區(`focusHot()` 還會把它捲進視窗),
要看群組本身的欄位,得再按一次「取消選取」。模型一密集,這個動作每次都要做。

**做法:3D 視圖工具列多一顆「選取」開關(`#btn-pick`),預設關。**

| 狀態 | 在圖上按左鍵 | 面板 |
|---|---|---|
| **關(預設)** | 點到節點 / 元素 / 線 → 一律只選**畫出它的那個群組**;點空白 = 取消選取 | 停在群組本身 |
| 開 | 節點(14 px)優先,其次元素(12 px)—— 和 v3.31 的滑過高亮同一組容差 | 展開節點 / 元素那一區 |

- 關掉開關時順手把 `SELNODE` / `SELELEM` 清掉,不會留一個看不到的選取狀態。
- **點選模式(PICK)完全不受影響** —— 「圖上點起點」「點兩個節點」「點一根元素選群組」
  那幾種是使用者主動進入的模式,照樣點得到,不必先開開關。
- 滑過的高亮(v3.31)兩種狀態都保留:整組打亮 + 讀數列報出節點座標 / 元素資訊,
  都是唯讀回饋,不會動到面板。

順手把三處說明改掉:視圖標題列的操作提示、節點區的空狀態提示、沒選群組時的提示,
都明講「預設只選群組,要點節點 / 元素請先打開『選取』」。

### v3.31 桁架斜撐全面 X 型、變換過的組不再留鬼影、滑過就整組打亮

**v3.31.1 桁架的斜撐一律改成 X 型,三個面都要。** 原本側面與底面都是「錯開 2 個節點」
的單斜桿(N 型),改用對接的 **交叉(cross)** 配對:相鄰兩個格點拉一對來回
(`A[j]–B[j+1]` 與 `A[j+1]–B[j]`),一格兩根、方向相反,就是 X。三個面分別是:

| 面 | 起始群組 → 目標群組 | 配對 | 間隔 |
|---|---|---|---|
| 兩側面 | 邊樑左/右 → 同側下弦 | 交叉 | 每個節點(2.5 m) |
| 底面 | 下弦左 → 下弦右 | 交叉 | 每個節點(2.5 m) |
| 橫隔面 | 邊樑左 → 下弦**右**、邊樑右 → 下弦**左** | 逐根對逐根 | 每 2 個節點(5 m) |

橫隔面的兩根落在**同一個 x**,在斷面內交叉;它是把斷面撐成矩形用的,不必跟著格點走,
所以 5 m 一組(側面 / 底面才要一路跟)。順帶把 v3.30 為了避開「對不到節點」而寫的
`to:31` 拿掉了 —— 交叉配對本來就只在成對的格點之間連,不會跑出範圍。

**v3.31.2 橋面橫梁改成每 2.5 m 一根,和吊索同節奏。** 吊索是每 2.5 m 一根、
落在邊樑節點上;橫梁原本 5 m 一根,等於一半的吊索底下沒有橫梁可以把力橫過去。
改成每個節點一根(33 根),再加一條測試:**每一根吊索的落點都要有一根橫梁接著**。
桁架豎桿也跟著改成每個節點一根,格點與橫梁 / 吊索完全對齊。

**v3.31.3 整組做過變換的群組,不再畫「原始參數曲線」。** 圖上淡淡那條是段的數學曲線,
用來對照節點串(弦線)差多少。但群組層級的平移 / 旋轉 / 鏡射是**建完曲線之後**
才套到節點上的,曲線本身留在原位 —— 抗風索繞橋軸轉 45° 之後,圖上就多一條
對不上任何元素的鬼影。`curvePolys()` 開頭加一行 `if (r.xfOn) return []`。
段層級的變換不受影響(那是在建曲線的當下就套進去的,曲線本來就在對的位置)。

**v3.31.4 抗風索那種「繞軸旋轉」到底是走哪一條路」—— 完整流程表。**
UI 上就是群組面板的**「平移與旋轉」**那一區(旋轉角 / 旋轉軸 / 旋轉中心)。

| # | 階段 | 做了什麼 | 程式位置 |
|---|---|---|---|
| 1 | 段:算曲線 | `SAG` 在自己的座標上算出拋物線。垂度給 **負值** → `dip(u) = −4·fc·u(1−u)` 變成正的,往弦線**上方**鼓 | `curveSAG()` |
| 2 | 段:分割 | 依 `split` 取 u = 0…1 的節點(count 24 → 25 個節點) | `splitCurve()` |
| 3 | 組:判斷要不要動 | `xfActive(xf)`:dx/dy/dz/rot 任一 ≠ 0,或有鏡射 | `xfActive()` |
| 4 | 組:取旋轉中心 | `cmode:"custom"` → 直接用 (cx, cy, cz);其他模式是起點 / 範圍中心 | `xfCenter()` |
| 5 | 組:造變換函式 | 依 `axis` 選公式。X 軸:`y' = Y·cosθ − Z·sinθ + cy`、`z' = Y·sinθ + Z·cosθ + cz`(Y、Z 是相對中心的座標),x 不動 | `xfMake()` |
| 6 | 組:套到節點 | 只動 **gid 是自己的** 節點(借用別組的節點不動),就地改座標 | `xfApplyNodes()` |
| 7 | 組:補方位角 | 非 Z 軸旋轉沒有單一角度增量(`dAz = null`)→ 整串節點重算方位角 | `annotate()` |
| 8 | 組:重算幾何 | 元素長度、弦線偏差、統計全部重來;標記 `R.xfOn = true` | `refreshElemGeom()` / `finishStat()` |
| 9 | 畫圖 | `xfOn` 的群組不畫原始參數曲線(v3.31.3) | `curvePolys()` |

抗風索左的實際參數:旋轉角 **45°**、旋轉軸 **X**、旋轉中心 **自訂 (0, 8, 2)**
—— 中心落在兩端錨碇的連線上,所以錨碇是旋轉軸上的點、位置不動,
只有中間鼓起來的部分從「正上方」轉到「內上方 45°」。右側同理,角度改 **−45°**、中心 (0, −8, 2)。
這是**剛體**變換:索長、弦線偏差都不變。

**v3.31.5 滑鼠滑過元素也會亮,而且是「整組一起亮」。**
以前只有節點會亮(一個圈圈),元素滑過去沒有任何反應。現在:

- 滑到**節點**或**元素** → 它所屬的**整個群組**打淡藍(元素加粗線、節點加圓點),
  滑到的那一根再單獨加一層濃的;讀數列同時報出「群組名 · 元素 #編號 · 兩端節點 · 長度」。
- 左邊清單滑過一列 → 同一種效果,群組多起來之後才對得上圖上是哪一條。
- 游標在有東西的地方變成 `pointer`。

順手修掉一個不一致:滑過時找節點的容差是 24 px、按下去卻是 14 px(元素 12 px),
所以會發生「亮的是節點、按下去選到元素」。現在**滑過與按下用同一組容差**,
看到什麼就會選到什麼。

驗收測試 637 → **642 項,全過**。吊橋 24 群組 / 319 節點 / 754 元素。

### v3.30 螺旋改成「斗笠」夾角 + 8% 坡度;吊橋補桁架與抗風索系統

**v3.30.1 進出口夾角的幾何條件補完:120° 還要「夾得住」。**
v3.29 只釘住角度值,結果 6⅚ 圈那個模型俯視是**兩條線交叉成 X**,不是把圓夾在中間。
把它畫清楚:兩條直線段都與圓相切,切線交於 P,四邊形 OAPB 在 A、B 都是直角,
所以 ∠APB = 180° − ∠AOB —— 要 120° 就要**兩個切點的圓心角 = 60°**。
但 60° 有兩種走法,差別在**走的方向**:

| 總轉角 mod 360 | 切點位置 | 兩段直線 | 俯視長相 |
|---|---|---|---|
| **60°** | 一樣 | 從切點往**外**發散,P 在圓的另一側 | **斗笠**(把圓夾在中間)✓ |
| 300° | 一樣 | 兩段都**穿過 P** 再往外跑 | 交叉成 X ✗ |

所以圈數的小數部分必須是 **⅙**,不是 v3.29 的 ⅚;取 **6⅙ 圈**(至少 6 圈)。
測試也補一條:除了量夾角,還要用線段相交判斷**兩條直線段在平面上不相交**。

**v3.30.2 坡度改成 8%,螺距由坡度算。** `PITCH = 0.08 × 2πR ≈ 1.508 m`,
總爬升 = 圈數 × 螺距 ≈ **9.30 m**(半徑 3 m 一圈只有 18.85 m,8% 爬不高,這是幾何決定的)。
**副作用:層間淨高只有 1.508 m,人走不過去**(v3.33 已把半徑放大到 5.5 m 解掉) —— 要 2.2 m 淨高得把半徑放大到 4.5 m
或把坡度拉到 12%。範例照使用者給的數字建,這件事寫在這裡備查。
落地外柱也跟著改:第一圈爬得太慢,前兩根(φ = 20°、110°)只離地 8 cm 與 46 cm,
等於直接坐在地上,所以落地段從第三根(φ = 200°)才開始 —— 否則會生出 0.46 m 的超短元素,
踩到 v3.28.3 的「沒有短到不像話的外柱」那條測試。

**v3.30.3 人行吊橋補上橋面下的箱型桁架。** 邊樑當上弦,各往下偏置 1.6 m 得下弦;
每 2 個節點(5 m)一根豎桿,再用「錯開 2 個節點」拉斜桿 → 一格一根的 N 型腹桿;
兩條下弦之間再加橫梁與斜撐。斜桿的起算節點只能走到倒數第 3 個(33 − 2),
不然最後一根對不到目標節點,會吃到「超出範圍已略過」的警告。

**v3.30.4 抗風索:用「負垂度 + 繞橋軸轉 45°」做出傾斜的拋物線。**
需求是:兩端錨在橋兩側岩壁(比主索短)、形狀像拋物線但**頂點朝向橋面板**、
整條**向內傾斜 45°**(縱視圖左側左下右上、右側右下左上)。做法拆成兩步:

1. 索身是一條 `SAG` 拋物線,**垂度給負值**(`f = −0.10`)→ 往弦線的另一邊鼓起來,
   頂點在上而不是下垂。
2. 整組用群組變換繞 **X 軸(橋軸)轉 ±45°**,旋轉中心用 `custom` 指到**兩端錨碇的連線上**
   (`cy/cz` = 錨碇座標)—— 錨碇落在旋轉軸上所以不動,只有中間的頂點從正上方轉到「內上方 45°」。

錨碇 (10 / 70, ±8, 2)、頂點 (40, ±3.76, 6.24),索長 61.6 m < 主索 126.9 m。
這是群組變換第一次在範例裡當**幾何手段**用(以前只是位移複製的附屬品)。

**v3.30.5 抗風支索用「自訂連接方式」做成純橫向。** 下弦節點間距 2.5 m、抗風索也切 2.5 m,
但兩者起點差 10 m —— 用 `free` 各自指定起算節點與步進(下弦第 7 個配抗風索第 3 個、
兩邊都每 2 個節點前進、重複 11 次),11 根支索的兩端就都落在同一個 x 上,
從 x = 15 到 65 對稱分布,兩端的錨碇節點留空不接。測試釘住「兩端同一個 x」。

驗收測試 622 → **637 項,全過**。吊橋 22 群組 / 319 節點 / 512 元素。

### v3.29 螺旋範例改成塔式:半徑 3 m、6⅚ 圈爬 18 m、進出口夾 120°

**v3.29.1 幾何規格重寫。** 半徑 9 → **3 m**(內外緣 1.9 / 4.1)、圈數 2⅓ → **6⅚ 圈**、
總爬升 11.67 → **18 m**。螺距不再是整數,直接寫成 `PITCH = ZTOP/TURNS`(≈ 2.634 m)——
「爬滿 18 m」是規格,螺距是算出來的結果,寫死反而會兩邊對不起來。
坡度 14.0%:半徑 3 m 的一圈只有 18.85 m 周長,要在 6⅚ 圈裡爬 18 m 就是這麼陡,
這是題目給的數字決定的,不是幾何算錯。中央柱跟著加高到 20 m(要高過最高一圈)。

**v3.29.2 夾角的定義改了(角度值仍是 120°)。** 舊版量的是**兩段直線的行進方位**差
(引道朝 180°、出口朝 `180° + 總轉角`)。現在量的是**兩支都指向螺旋的射線**:
引道口 → 螺旋起點、出口 → 螺旋終點。第二支是出口行進方位掉頭,所以

```
射線1 = 180°        射線2 = norm360(180° + 360×圈數 + 180°) = 總轉角 mod 360
夾角  = |norm180(射線2 − 射線1)|
```

兩種量法互為補角,同一個模型舊定義 120° = 新定義 60°。要新定義的 120°,
圈數的小數部分必須是 **⅙ 或 ⅚**(總轉角 mod 360 = 60° 或 300°),不是舊版的 ⅓ / ⅔。
取 **6⅚ 圈**(轉 2460° ≡ 300°):滿足「至少 6 圈」,而且比 6⅙ 圈的螺距緩。
分割數 `NT × TURNS = 36 × 41/6 = 246` 仍是整數,每圈 36 個節點的對齊條件沒破。
N3 的夾角測試改成從**最後一個節點反向量回螺旋終點**,定義才跟需求對得上。

**v3.29.3 外柱柱距 60° → 90°,不然半徑 3 m 一定有一根插在引道上。**
半徑縮到 3 m 之後外緣半徑 4.1 m **大於** R,引道那條切線(`x = R`)因此**切進外緣圓**:
外緣上 φ ∈ (297.6°, 360°) 這 **62.4°** 的位置都落在引道 2.2 m 寬的路面帶裡。
禁區比舊版 60° 的柱距還寬 —— 也就是**每 60° 一根,不管相位轉到哪裡都會有一根插在路面上**
(v3.28 的解法在新半徑下失效)。柱距改成 90°(`CS = 9` 個節點,仍整除每圈 36),
相位取 `C0 = H0+2`(φ = 20°):落地柱在 110°/200°/290°,離引道路面最近 1.6 m;
頂圈那根層間柱落在 290°(出口那一圈),離出口路面 1.26 m > 半寬 1.1 m。
φ = 20° 那根一樣不生落地柱(那裡坡道才離地 15 cm),v3.28 的兩條測試照樣把關。

**v3.29.4 測試。** 新增「總爬升 18 m」「至少 6 圈」「半徑 3 m」三條規格檢查;
「層間柱高 = 一個螺距」原本寫死 5 m,改成從模型讀 `pitch`。驗收測試 618 → **621 項,全過**。
模型規模 1307 節點 / 1612 元素。

### v3.28 螺旋範例:整圈外柱轉一個相位,不再插在引道上

**v3.28.1 有一根外柱正插在引道的路面上。** 外柱每 60° 一根、站在外緣半徑上,
而引道是沿 `x = R` 的一條直線帶(`x ∈ R ± W`、`y ≥ 0`)。舊版的相位是 30°
(六根落在 30/90/150/210/270/330°),**330° 那根的平面位置離引道中線只有 0.25 m** ——
半寬是 1.1 m,所以它整根從路面正中央穿過去。

修法是**把整圈柱子轉一個相位**,不是刪掉那一根 —— 六根柱子的角度必須維持等分,
而且三種柱段要共用同一組角度(v3.27),不能單獨挪一根。
`C0` 從 `H0+3`(相位 30°)改成 `H0+1`(相位 **10°**),六根落在
10/70/130/190/250/310°;離引道最近的兩根分別在**引道盡頭的另一側**(10°,y = −1.75)
與 **1.4 m 外**(310°,x = 6.49,引道帶從 7.9 才開始)。實測最近的平面距離從 0.25 m 拉開到 **1.99 m**。
`C0 = H0+1` 仍在接縫節點之後,方位角不受直線段影響,柱子照樣鉛直。

**v3.28.2 相位 10° 那根不生落地柱。** 坡道在那裡才離地 14 cm,等於直接坐在地上,
硬生一根 14 cm 的柱子只會在模型裡留下一個超短元素(FE 條件數會爛掉)。
落地段改成從第二個柱位起算,最短的外柱從 0.14 m 變成 0.97 m。
連續性測試同步放寬:層間柱的下端**貼著地面(z ≤ 0.6)也算有支撐**,
因為那裡的坡道本來就坐在地上。

**v3.28.3 加一條「柱子不可以插在人行步道上」的驗收測試。**
螺旋段的柱子只在端點碰到踏面(構造上就是這樣),真正會出事的是引道與出口那兩條直線段。
測試對每一根柱子量它的平面位置到兩條直線段的距離,**只在高程重疊時才算**
(柱子要真的從路面底下穿過去才叫撞到),要求大於踏面半寬。
拿舊相位回頭跑過:量到 0.25 m < 半寬 1.1 m,**測試確實抓得到** —— 不是寫爽的。
驗收測試 616 → 618 項。

> 這件事的通則:**柱子的角度相位不是可以隨便挑的參數**,它和引道 / 出口的平面位置綁在一起。
> 之後要改圈數、半徑、進出口角度,都得重新確認這一圈柱子還躲得開直線段 —— 測試會擋。

### v3.27 螺旋範例:外柱從地面連上去、進出口夾 120°

**v3.27.1 外柱不能架在兩圈中間的空中。** v3.26 的「層間外柱」與「落地外柱」用了
**不同的角度位置**(`every` 一個 8、一個 9,起點也不一樣),所以層間那幾根底下根本沒有東西
—— 柱子懸在上一圈與下一圈之間,結構上講不通。

改法:三種柱段**共用同一組角度位置**(`C0` 起、每 `CS` 個節點一根),疊起來才是一根
**從地面連到最高一圈的連續柱**,坡道每繞到那個角度就接一次(這也才是螺旋坡道真正的做法)。
`外柱(落地段)` 掛第一圈、`外柱(層間段)` 用「錯開一圈」一路往上接:
第一圈的第 j 個節點接第 `j + 一圈` 個,而 `j + 一圈` 自己也在範圍裡,所以會再往上接一段,
同一個角度就這樣疊到頂。

新增的測試直接檢查這件事:把層間柱的下端拿去比對「落地柱的頂端」與「其他層間柱的上端」,
**一根都不能落空**;再從每根落地柱往上追鏈,確認真的疊得上去。

**v3.27.2 進出口在平面上夾 120°(模擬繞山壁快速上升)。**
入口段的方位固定是 180°(螺旋起點的切線),出口段就是 `180° + 轉過的總角度`。
所以夾角只由**圈數**決定:取 **2⅓ 圈**(轉 840° ≡ 120°),兩段剛好夾 120°。(**v3.29 已改定義與圈數,見上面 v3.29.2**)
程式裡直接寫成 `EXIT_AZ = norm360(180 + 360*TURNS)`,改圈數時出口方位會自己跟著走,
不會又變回折角。分割數配成 `NT × TURNS = 36 × 7/3 = 84`,每圈仍是整數個節點 ——
外柱要靠「差一圈 = 差 36 個節點」才對得齊,這個不能亂改。

**v3.27.3 半徑縮小、吊索減量。** 半徑 12 → **9 m**(內外緣 7.9 / 10.1),總爬升 11.67 m,
坡度 8.8%;吊索由每 6 個節點一根改成每 9 個,14 → **9 根**。

順手把兩個寫死數字的 N1 測試改成從模型自己讀(總爬升 = 圈數 × 螺距、柱頂高度取中央柱的最高點)
—— 範例的參數一調,寫死的期望值就會假失敗。驗收測試 614 → 616 項。

### v3.26 螺旋範例改成合理的結構、範例選單不再失靈

**v3.26.1 範例選單載入過一次之後就打不開了(真的是 bug,不是設計)。**
「點別的地方就把選單關掉」的監聽器是寫在自己裡面 `removeEventListener` 的:

```js
setTimeout(() => document.addEventListener("click", function away(e3){
  if (!e3.target.closest("#demo-menu") && e3.target !== ev.currentTarget){
    closeDemoMenu(); document.removeEventListener("click", away);
  }
}), 0);
```

選了一個範例時,選單自己的 handler 會**先**把選單 `remove()` 掉,然後同一個 click 才冒泡到
document。這時 `e3.target.closest("#demo-menu")` 仍然找得到 —— 因為那個節點只是**脫離了 DOM**,
祖先鏈還在,`closest()` 照樣走得上去。條件不成立 → 監聽器**沒被移除**。
它就一直留在 document 上,下一次按「載入範例」時把剛開好的選單當場關掉。
(順帶一提 `ev.currentTarget` 在事件派送結束後會被設回 `null`,所以那個 `!==` 比對也是死的。)

改法:監聽器存成 `DEMO_AWAY`,由 `closeDemoMenu()` 統一移除;判斷改用
`d.contains(e3.target)` / `btn.contains(e3.target)` —— 節點被移除之後 `contains` 一定是 false,
不會像 `closest()` 那樣在殘骸上還走得通。加了 N4 組回歸測試:
第一次打得開、選完會關、**載入過之後再按還是打得開**、再按一次是收起來而不是開第二個。

**v3.26.2 開檔時把測試留在畫面上的東西清掉。** 驗收測試會借用真正的 `state` / `MODEL`
跑一堆模型,途中的 `renderTree()` / `renderPane()` 會把最後一個測試模型留在畫面上
(`state` 本身有還原,只是畫面沒跟著回來)。啟動序列最後補一次 `recompute(true)`。

**v3.26.3 螺旋範例改成結構上說得通的樣子。**
- **吊索改拉內緣。** 拉外緣的話,索從柱頂拉到外緣會**橫穿整個踏面**,人走不過去。
  改拉內緣之後,索全程待在半徑 10.9 m 以內 —— 那一圈裡面整個是空的,只有端點碰到坡道。
- **外緣改用柱子撐,分兩種:**
  - **層間外柱**:外緣的第 j 個節點接第 `j + 一圈節點數` 個 —— 同角度、剛好差一個螺距,
    所以是一根鉛直、正好 5 m 的柱子,上一圈就站在下一圈上。
    做法是對接的「錯開 n 格」,而且**起始群組與目標群組都選「外緣」自己**
    (`refOptions()` 只排除對接群組本身,所以 a = b 本來就選得到)。
    範圍刻意避開螺旋兩端的**接縫節點** —— 接縫的方位角是直線與曲線的混合,
    偏置方向會跟著歪一點,柱子會差 3~4 cm 不鉛直。
  - **落地外柱**:只掛在**最下面那一圈**(它下面沒有東西),垂落到地面 Z = 0。
    再往上就不能落地了,不然柱子會直接穿過下面幾圈的踏面。
- **進出口改成切線銜接。** 螺旋起點 (R, 0) 的切線朝南(方位 180°)、終點 (−R, 0) 的切線朝北
  (方位 0°),原本引道是沿 −X 徑向切進來、出口是沿 +X 出去,兩處都是 85° 的折角。
  現在引道從北邊直直開下來 `(R, 22, 0) → (R, 0, 0)`,出口 `endMode:"polar", az:0`。
  (節點串量到的是弦線,會比真正的切線差半個分割角 5°,測試容差就放 6°。)

群組 10 → 12。新增 N3 組驗收測試:進出口的轉折角 < 6°、吊索接的是內緣且不超出內緣半徑、
層間外柱鉛直且高度剛好一個螺距、兩端都在外緣上、落地外柱都到 Z = 0 且只掛在最下面那一圈。
驗收測試 601 → 614 項。

### v3.25 範例補齊構件、斜視角壓到 20°、開檔不預選

**v3.25.1 九宮格四個角改成俯角 20°(`CORNER_EL`)。** 橋梁這種東西長寬比很懸殊,
俯 45° 會把整條線壓成一團;壓低到 20° 才同時看得出縱向的起伏與橫向的寬度。
`VIEWS` 與 HTML 上的 `data-el` 都改吃同一個常數,`viewHit()` 的比對自然跟著走。

**v3.25.2 範例一律從西南 20° 開檔(`DEMO_VIEW()`)。** 俯視看不出高程、立面看不出平面線形,
斜視角才一眼看得懂;三個範例的 `s.view` 都設成 `{ az:135, el:CORNER_EL }`。

**v3.25.3 載入後不預選任何群組。** `loadState()` 本來會把 `SEL` 設成第一個群組 ——
一開檔就有一組被選走,其他組全部退成灰(`IDLE_COL`),反而看不出整個模型長什麼樣子。
現在 `SEL = null`、`SELSET` 清空、`PICK` 也清掉(舊版沒清,載入後點選模式會留著)。

**v3.25.4 範例選單的標題不再寫「(會蓋掉現有內容)」** —— 按下去本來就會跳確認框問一次。

**v3.25.5 範例補齊構件。**
- **螺旋人行步道:內外兩側都有護欄。** 內緣 / 外緣各往上偏置 1.1 m 當扶手,
  每 4 個節點一根立柱。群組 8 → 10。
- **公路橋梁:護欄 + 雙柱橋墩。** 兩側邊緣各往上偏置 1.05 m 當扶手、每 2 個節點一根立柱;
  橋墩從單根柱改成**雙柱構架** —— 墩頂橫梁(比一般橫隔梁重,另一個斷面)+ 兩根柱
  (從兩側邊緣落到傾斜地面)+ **承台繫梁**。繫梁用 v3.23 的「取哪些節點」
  (`aSide`/`bSide` = `"j"`)接到兩根柱的**下端**,不必為了柱底另外造一條線。群組 5 → 12。

**v3.25.6 兩個名字與幾何對不上的錯,順手修掉(有測試釘住了)。**
- **螺旋的內外緣寫反了。** 偏置的 `d` 是「沿線方向的右手邊為正」,而這個螺旋是順時針,
  所以「右」指向圓心 —— 原本 `內緣 d = −W` 實際落在 R = 13.1(外側)、`外緣 d = +W` 落在 10.9。
  兩個符號對調。
- **吊橋的「邊樑西 / 東」名不符實。** 橋沿 +X(東)走,兩側其實是**南北**不是東西。
  改成沿線方向的 **左 / 右**(邊樑左右、主索左右、吊索左右);塔架在 x = 0 與 x = 80,
  「西 / 東」本來就對,維持不動。同側的主索與邊樑要在同一個 y 上,吊索才垂得下來。

新增 N2 組驗收測試專門盯這件事:內緣真的比外緣靠近圓心、內外緣間距 = 踏面寬、
扶手高度、立柱垂直、主索與邊樑同側、吊索下端接在同側邊樑上、承台繫梁接的是柱底而不是柱頂、
繫梁跨距 = 兩柱間距、三個範例的預設視角。驗收測試 583 → 601 項。

### v3.24 圖上點選改吃元素、段分深淺、三個範例

**v3.24.1 點選模式進行中,畫面上的選取狀態一律不動。** 「圖上點起點」以前**點不準就直接取消**
(`if (!hit){ PICK = null; … }`),使用者看到的現象是:點一下沒反應,再點一下就跳去編輯別的群組
—— 因為第二下時 PICK 已經沒了,走的是一般的選取流程。現在:

- 點空了**什麼都不做**,維持點選模式;取消一律走 **Esc** 或面板上那顆紅色的取消鈕。
- 點選模式中**不會動** `SEL` / `SELNODE` / `SELELEM`,不可能中途跳進別組的編輯區。
- 面板上多一行常駐提示(`.hint.keep`)寫清楚現在在等什麼、怎麼結束。

**v3.24.2 要「一個群組」時只認元素,要「一個節點」時線也點得到。**
節點只有幾個像素,而且**常常被好幾個群組共用**(重合節點更是同一顆)——
點一個節點根本說不清楚是要選哪一組。所以:

- `PICK.mode === "group"`(群組下拉的「▸ 在畫布上點選…」)改成**只認元素**(`pickElemAt`):
  一根元素是誰畫的沒有歧義,而且比節點好點得多。滑鼠掃過時**整個群組一起高亮**,
  readout 直接寫「會選到:⟨群組名⟩(元素 #N)」,按下去之前就知道結果。
- 要節點的模式(`ref` / `link2`)用 `pickNodeLoose()`:先照舊找節點,找不到就退而求其次
  —— 取滑鼠壓到的那根元素上**離游標比較近的那一端**。

**v3.24.3 段用群組色的深淺分開。** v3.11 把「各段分色」拿掉是對的(換色相會跟別組的主題色撞在一起),
但整組同一個顏色時段與段之間也看不出界線。折衷:**色相不動、只動明度**
(`shade()` 對 RGB 等比例縮放或往白色內插,所以色相天然不變)。
`pieceColor(base, pi, n)` 以群組色為中心往深、往淺兩邊分開,**往淺的幅度只有往深的 0.62 倍**
—— 白底上太淡的線會看不見,往深不會;段數是奇數時正中間那一段剛好就是群組色本身。

同一個顏色同時用在:**面板上的段編號徽章**(本來一律是型別色,所以每一段都是藍的)、
**圖上的段編號徽章**、以及**那一段的線與節點**。三者對得起來,徽章才真的是「那一段的標籤」。
只有正在編輯的多段線群組會分深淺(`pcOn(r)`),其他群組維持單色,v3.11 擔心的事情不會發生。

**v3.24.4 線形鏈的「+ 直線」不再是主色鈕**(和 v3.23.10 的段新增鈕同一個理由)。

**v3.24.5 範例改成三個,點按鈕先挑一個。**
一次把所有東西倒進畫布看不出誰是誰,而且一個範例塞不下所有群組型別。現在是:

| 範例 | 內容 | 涵蓋 |
|---|---|---|
| **人行吊橋** | 跨徑 80 m、垂跨比 0.12。主索走「錨碇 → 塔頂 → 懸鏈線 → 塔頂 → 錨碇」三段一組;吊索垂落到橋面邊樑,**下端貼附既有節點**不另生節點;塔架一組三段(兩根立柱 + 頂橫梁);橋面下一組箱型桁架(下弦 + 豎桿 + 側面 / 底面 / 橫隔面三向 X 斜撐);兩側抗風索(負垂度 + 繞橋軸轉 45°)由抗風支索拉住桁架下弦 | 線(直線 / 懸垂)、偏置、對接(same / skew / **自訂**)、垂落 + 貼附、**群組變換** |
| **公路橋梁** | 平面「直線 → 緩和曲線 → 圓弧 → 緩和曲線 → 直線」共 458 m,縱斷面 VPI + 對稱豎曲線;10.5 m 寬橋面、每 20 m 一組橫隔梁;墩柱落在**傾斜的地面**上 | 線形鏈(含緩和曲線與縱斷面)、偏置、對接、垂落(斜面) |
| **螺旋人行步道** | 半徑 5.5 m、坡度 8%、繞中央柱 6⅙ 圈(爬升 17.05 m、層間淨高 2.77 m),進出口夾 120° 且俯視像斗笠夾住圓;踏面兩側偏置成內外緣,內外緣再各往上偏置 1.1 m 當扶手;柱頂用扇出拉斜索吊內緣,外緣用每 90° 一根、從地面連到頂的連續外柱撐 | 線(螺旋)、**偏置的偏置**、對接 ×2、扇出、垂落 |

三個都做到可以直接匯出:元素類型、材料、斷面全部填好。驗收測試(N1 組)會把三個都建一遍,
確認**沒有錯誤、每個群組都算得出東西、每根元素的材料與斷面都填過**,順便釘住幾個關鍵幾何
(吊索下端落在橋面高程、公路橋平面 5 段且第 2/4 段是緩和曲線、螺旋總爬升 = 圈數 × 螺距)。
`demoState()` 保留成「第一個範例」的別名,舊入口不會壞。

### v3.23 視圖工具列、序號範圍、自訂連接方式、參照取哪一側、廢除手動節點與元素

**v3.23.1 方位角 / 仰角只顯示一份。** 畫布左上角的工具列本來寫「方位」、左下角的
`readout` 又把「方位角 90° 仰角 90°」印一次,同一個數字在同一個框裡出現兩遍。
現在:工具列的標籤補成**方位角**(和面板、readout 同一個詞),`readout` 只留
工具列講不出來的東西 —— 目前落在哪一個九宮格視角(`viewHit().t`),沒有就整條隱藏
(`.readout:empty{display:none}`)。

角度輸入照舊在打字時就 `norm360` / `norm180`,另外補了 `change` / `blur` 的**寫回**:
打 400 進去,離開欄位時框裡會變成 40 —— 以前狀態已經收斂了、框裡卻還留著 400。

**v3.23.2 「1:1」改成小膠囊。** 原本是一段直接寫 inline style 的裸藍字(`border:0;background:none`),
和旁邊每一顆都有框的工具列鈕不成一套。改成 `.cvtools .zbox .zfit`:同一套 `--mono` 字體、
17px 高的膠囊、hover 才上主色;`syncZfit()` 在 `zEx ≠ 1` 時給它 `.on`(主色外框),
也就是「現在真的有東西可以歸位」才點亮。

**v3.23.3 序號欄位的合法範圍是 1..max。** 「第幾個節點」這類欄位以前可以打 −5 或 999,
狀態留著離譜的值、建模時才默默 `clamp` 掉,畫面和實際不一致。現在:

- `fld()` 多兩個選項 —— `idxMax`(序號欄位)與 `stepMax`(每 n 個),渲染成 `data-idx` / `data-step`。
- `normIndexEl()` 掛在 `#pane` 的 `change`:離開欄位時用 `wrapIndex()` 收斂並**寫回輸入框**。
- `wrapIndex(v, max)`:**超過上限就停在上限**(不繞回頭 —— 「連到最後一個」被打成「連到第一個」
  是很糟的意外),**0 或負值從上限往回扣**(0 → max、−1 → max−1),永遠不會出現負值。
  步進欄位只做 `clamp(1..max)`,0 一律當 1。
- 顯示的值一律是「實際生效」的那一個(`idxShow()`):舊檔裡 `to = 0`(意思是到底)
  會顯示成 max。**state 本身不動** —— 使用者沒去碰它,節點數變了它還是會自己跟著跑。

套用範圍:對接的 `from` / `to` / `every`、垂落的 `from` / `to` / `every`、
扇出的 `from` / `to` / `every` / `srcI`。`skew`(錯開幾格)刻意不收 —— 它本來就允許負值。

**v3.23.4 `free` 改成「自訂連接方式」:樣式單元 + 重複。**
舊的 `free` 只是「兩邊各給起點與步進」,能表達的東西 `same` / `skew` 幾乎都能表達,
存在感很薄。現在它是唯一能畫出**任意連法**的模式:

- 新欄位 `g.pattern = [[起始群組偏移, 目標群組偏移], …]`(0 起算)描述**一個樣式單元**;
  單元沿兩個群組各自以 `aEvery` / `bEvery` 的步進重複。
- 兩點直連 = `[[0,0]]`、斜撐 = `[[0,1]]`、X 形 = `[[0,1],[1,0]]`、
  四點交叉 = `[[0,0],[0,1],[1,0],[1,1]]`。
- `patOf()` 是唯一的讀取口:舊檔沒有 `pattern`(或是空陣列)一律當 `[[0,0]]`,
  結果和以前的逐根對逐根**完全相同**(有測試守著)。
- 可用重複次數 `room` 要扣掉單元寬度:`min(⌊(NA−1−a0−wA)/ae⌋, ⌊(NB−1−b0−wB)/be⌋) + 1`。

UI:配對方式的「自訂」卡片點下去**直接開小視窗**(`openLinkFreeDlg`),
面板上只留一句摘要(`patSummary`)和「編輯連接方式…」。小視窗裡是一張 5 欄 × 2 排的
SVG 節點圖 —— 點上排一顆、再點下排一顆就連成一條,點同一條取消;附四個預設樣式與清除。
原本獨立的「在視圖上點兩個節點…」鈕**併進這個視窗**(`PICK` 的 `mode:"link2"` 不變),
點完兩點會寫成 `pattern:[[0,0]]` + 兩個起點。

**v3.23.5 群組下拉可以在畫布上點選。** 連接族的每個群組欄位(對接 `a`/`b`、
垂落 `ref`/`zRef`、扇出 `ref`/`srcG`)在下拉最上面固定多一個 `PICK_OPT`
(`@pick` /「▸ 在畫布上點選…」)。選了它**不寫進 state**,而是進入 `PICK.mode = "group"`;
下一次在 3D 視圖點到的節點屬於哪個群組,這個欄位就填哪個群組(`locateNode()` 反查)。
面板同時顯示一行 `.hint.keep` 的提示,點空白處取消。下拉本身照舊可以直接選,兩條路並存。

**v3.23.6 兩顆相鄰的 i 鈕合成一顆。** `foldHints()` 對「不在任何區塊裡的提示」本來是
**一段一顆 i**,位移複製對話框裡兩段提示剛好相鄰,就並排出現兩顆意義相同的 i。
改成先把**互為相鄰兄弟節點**的提示歸成一組(`runs`),一組共用一顆 i。

**v3.23.7 用語去口語化。** 「這排 / 那排」→ **起始群組 / 目標群組**,
「自訂偏移與步進」→ **自訂連接方式**,「只留主要那一個」→ **只保留主要群組**,
欄位標籤 `第幾個起` / `到第幾個(0 = 到底)` → **起算節點** / **到第幾個節點**。
警告訊息與驗收測試的項目名稱一起改,程式碼裡的註解也跟著換,免得下次又照舊名字改回去。

**v3.23.8 多選清單只留名字。** `paneMulti()` 的群組列表本來一行塞了圖示、名稱、型別名、
「N 節點 · M 元素」。型別看圖示就知道、數量在各自的「結果」裡看得到,擠在這裡只是雜訊 ——
現在只留圖示 + 名稱。

**v3.23.9 參照一個群組時可以指定「取哪些節點」。** 這是「組對組的連接不一定都在同一側」
的根本修法。一個群組手上其實有兩種節點:**自己產生的**(`R.nodes`)與**自己的元素碰到的**
(`R.elems` 的兩端)。對連接族來說後者有一大半在別的群組上 —— 垂落群組自己只擁有下端那一票,
上端全落在來源線上。以前參照一律走 `chainOf()`,所以這種群組**永遠只有一側連得到**。

新增 `nodeSetOf(r, side)`,`side` 有四種:

| `side` | 取到的節點 |
|---|---|
| `own`(預設,舊檔行為) | `chainOf(r)` —— 群組自己產生的節點 |
| `i` | 每根元素的**起點端**,依元素順序去重 |
| `j` | 每根元素的**終點端**,依元素順序去重 |
| `all` | 元素碰到的**全部端點** |

**刻意不替使用者命名成「主索側 / 地面側」**:這是幾何工具,不預設在做吊橋或任何特定結構,
只把兩側攤開來讓使用者自己決定。

欄位:對接 `aSide` / `bSide`,垂落 `refSide` / `zRefSide`,扇出 `refSide` / `srcGSide`。
`sideOf()` 把不認得的值一律當 `own`,所以舊檔沒有這些欄位時結果完全不變(有測試守著)。

UI 只在**需要問的時候**才問:`sideful(r)` 判斷「這個群組的元素有沒有碰到別人的節點」,
是才顯示「取哪些節點」下拉,並把該區塊改成兩欄(群組與側別上下對齊);
線、偏置這類自有節點就是元素端點的群組完全看不到這個選項。
序號欄位的上限 `chainCount(id, side)` 也跟著選到的側走。

偏置(`OFFSET`)的基準群組不受影響 —— 它的下拉本來就只列幾何族,而幾何族沒有這個歧義,
而且偏置要用節點上的方位角與曲率,只有群組自有節點才帶得齊。

**v3.23.10 版面與清單的三個小修。**
- 段區塊的「+ 直線」不再是主色鈕 —— 七個新增鈕地位相同,只有它是藍的沒有道理。
- 欄位排版規則 `label.f` 改成 `.f`:「圖上點起點 / 圖上點終點」那種**只有按鈕的格子**
  包在 `<div class="f">` 裡,以前吃不到 flex column 的規則,整格會掉出對齊基準。
- 群組下拉只寫**群組名稱**,拿掉「(線,53 節點)」;段的跨群組參照只寫「⟨名稱⟩ 的節點」。
  型別看清單圖示、節點數在各自的面板裡,下拉裡重複一次只是雜訊。

**v3.23.11 廢除「手動節點與元素」—— 它完全被 節點段 / 直線段 取代。**
這一區本來是兩個沒有關係的工具擠在同一張卡:上半是一張 X/Y/Z 表(打字建點)、
下半是一排刪除膠囊加一顆「點兩個節點」。而它做的兩件事,段早就都做得到:

| 舊做法 | 現在 |
|---|---|
| 手動節點(補一個算不出來的點) | **節點段**(`POINT`):起點填自訂座標,產生 1 個節點、0 根元素 |
| 手動元素(把兩個既有節點接成一根) | **直線段**:起點與終點各指到「某群組的第 k 個節點」,分割數填 1 |

直線段的等價性是實測過的(見驗收測試 M1):`gRefPt()` 取的是對方節點的**座標副本**,
所以會在同一個位置生出兩個新節點,再由**合併重合節點**併回原來那兩個 —— 結果是
`1 根元素、自己 0 個節點,兩端就是那兩個既有節點`,和手動元素產生的東西一模一樣。
**同一個群組的兩個節點也連得起來**:段不能參照自己所屬的群組,但另開一個線群組指同一個群組兩次就行。

**唯一的行為差異要講清楚:直線段的做法依賴「合併重合節點」。** 關掉合併時,
兩端會留成重合的複本 —— 在 midas 裡看起來相連、其實沒有連。手動元素直接持有節點物件,
沒有這個顧慮。不過**段間互相參照、連接族接到既有節點本來就是同一套機制**(見 §7 / v3.9),
所以這不是新增的風險,是本來就一致的做法。

處理方式(不做自動搬移,舊檔的幾何一個位元都不變):
- **建模端 `addN` / `addE` 完全保留**,舊的 .mct / JSON 讀進來照樣建得出來、結果相同。
- **新增的入口全部拿掉**:`manualSec()` 整張卡片、`mnadd` / `meadd` 兩個 op、
  以及畫布上「連兩個節點生手動元素」的分支。`PICK` 現在只剩 `link2` / `ref` / `group` 三種模式。
- **舊檔留下來的項目歸位到它們本來就該在的地方**(這就是「版本 A」):
  在 3D 視圖點到那個節點 → 「節點」區可以改座標、刪除;點到那根元素 → 「元素」區多一顆
  **刪除此元素**(`addManualElems()` 現在會把 `mi` 索引帶進元素裡,選到才刪得掉)。
  兩處都附一句話指路到節點段 / 直線段。
- 段區塊的說明補上「起點與終點都可以指到別的群組的第 k 個節點」,讓人自己找得到替代做法。

沒有手動項目的模型(絕大多數)在面板上**完全看不到這些東西**,屬性面板少一張卡。

### v3.22 多選、手動元素不再脫鉤、企業級淺色

**v3.22.0 介面改成「企業級淺色」(風格 B)。** 暖紙色 → 冷灰中性色
(`--paper` `#f7f6f2` → `#f4f5f7`、`--accent` 深藍 → `#0f62fe`),圓角一律收一半
(6px → 3px、4px → 2px),字改用系統的 Segoe UI(**不外連 Google Fonts**,離線照樣可用)。
只動 `<style>` 區塊與三個寫在 JS 裡的介面顏色常數,**群組資料色(GCOLORS / TYPES)完全沒動**。

**v3.22.1 手動元素改存「群組 + 第幾個節點」。** 這就是「編輯 A,連著 A 的東西沒跟著動」的真正來源:
垂落、對接、跨群組參照本來就會跟著動(相依解析每次都重算),**只有手動元素存的是節點編號**,
上游一改分割整串編號就跑掉。現在存 `@<群組 id>:<序號>`(舊檔的編號寫法照樣認)。
`normState()` 原本會把 `addE` 洗成只剩 `{i,j}`,參照會被洗掉 —— 一併修好。

**v3.22.2 被合併掉的節點看得見。** `weldNodes()` 記下每一組被併走的位置(`r.weldAway`),
選到那一組時在畫布上用**空心方框**標出來,面板的「結果」也多一行
「重合已合併 N 個(M 個接到別的群組)」。合併本身是想要的行為(midas 的 Merge Nodes),
但以前節點就這樣消失,使用者無從判斷到底接上了沒有。

**v3.22.3 多選群組。** `SELSET` 與檔案總管同一套操作:直接點 = 只選這個,
Ctrl / Cmd 點 = 加選或取消,Shift 點 = 從上一個選到這個。選超過一個時面板換成多選頁,
可以一起**位移複製 / 全部啟用 / 全部停用 / 全部刪除**。
`dupGroup()` 泛化成 `dupGroups(seeds, …)`:被選到的群組**當成一套一起複製**,
它們之間互相的參照會改指到複本(實測「主索西 + 吊索西」一起複製,複本的吊索指到複本的主索)。
「獨立 / 連動」只在**選取之外還有上游**時才出現。

**v3.22.4 起點 / 終點的「在圖上點」分得出來。** 按鈕文字改成「圖上點起點 / 圖上點終點」,
起點那一排與終點那一排之間加一條虛線(`.fields.rowsep`)。

**v3.22.5 仰角可以轉過極點。** 原本 `clamp(-90, 90)` 會卡住;改用 `norm180()`,
角度落在 (-180, 180],轉過 ±90 時 `cos` 變號、上方向自然翻面 —— 就是「翻到另一邊」。
方位角仍是 `norm360()`。

**v3.22.6 啟用改成按鈕。** 小方框改成寫著「已啟用 / 已停用」的切換鈕(帶狀態圓點)。

**v3.22.7 線群組的圖示跟著段型別走。** 只有一種段時(`soloPiece()`),清單直接畫那一種段的圖示
（直線 / 圓弧 / 懸垂 / 螺旋 / 線形鏈 / 節點 / 座標表);混合才用通用的曲線圖示。

### v3.21 滿版固定版面 + favicon

**v3.21.1 整頁固定滿版,視窗本身不捲。** 原本 `#app` 寫死 `height:calc(100% - 41px)`,
假設標題列永遠 41 px —— 按鈕變多之後標題列會換行、實際 54 px,多出來的部分把整頁頂出捲軸。
改成 `body` 自己是 flex column(`header` 固定、`#app` 吃剩下),`html,body` 加 `overflow:hidden`。
會捲的只剩「自己該捲」的那幾塊:群組清單、屬性面板、訊息列、驗收測試。
**窄版(≤740px)例外**:上下堆疊時內容一定超過一個視窗,那裡把 `overflow` 放回去。

**v3.21.2 畫布吃滿右欄剩下的高度。** `#cv` 原本是 `clamp(320px, 58vh, 900px)` ——
和右欄實際高度無關,底下就空掉一大塊(1440×900 實測浪費約 38%)。
改成 `.cvwrap{flex:1 1 auto}` + `#cv{height:100%}`,畫布佔右欄 **83%**,
底下只剩圖例與「驗收測試」那一行(110 px)。
驗收測試展開時 `max-height:46%` 自己捲,不會把畫布擠爆。

**v3.21.3 favicon 用同一個標誌。** 內嵌 SVG data URI,`viewBox='0 -7 44 44'` 讓那條線
在正方形圖示裡置中。

### v3.20 改名、標誌、清單字距、畫布工具列

**v3.20.0 改名為「線形建模器」。** 原本叫「空間線型產生器」——
道路 / 橋梁的術語是**線形**(線形設計、平面線形),線型是 CAD 的線條樣式,
工具內部本來就用對了(線形鏈、線形表),只有標題是例外。
副標「線形表 → midas Civil 幾何」併進 header 那條 `.note` 裡。
匯出的 .mct 檔頭與參數 JSON 的英文名一併改成 `Alignment Modeler` / `alignment-modeler`
(讀檔那邊 `extractParams()` 不比對這個字串,舊檔照樣還原得回來)。

**以下是同一版的介面調整:**


**v3.20.1 標誌只留那條線,並排進 `<h1>` 裡。** 三個節點圓點拿掉,線加粗到 3.2。
原本標誌是 `header` 的直接子元素,`header` 又是 `align-items:baseline` + `flex-wrap:wrap` ——
換行之後 flex line 會被拉高,`align-self:center` 就飄掉了(實測差 100 px)。
改成放在 `<h1>` 內當一個行內元素、用 `vertical-align:-3px` 對齊,
單行時標誌與文字的視覺中心差 **0.5 px**。

**v3.20.2 清單的數量字距收窄。** `.meta` 加 `letter-spacing:-.04em` 與 `tabular-nums`,
`groupSummary()` 也把數字與單位之間的空白拿掉:「2 段 · 29 節點」→「2段 · 29節點」。

**v3.20.3 畫布工具列同高。** 「重新框選 / 節點」原本比「方位 / 仰角」矮 ——
因為 `.tag` 自己有一層框,裡面的 `input` 又套到同一條規則再畫一層框。
改成 `.cvtools > *` 統一 `height:23px` 的 inline-flex,`.tag input` 去掉邊框與背景。
五顆現在都是 23 px。

**v3.20.4 節點顯示改成眼睛圖示。** 「節點:開 / 關」→「節點 + 眼睛」,
關掉時眼睛畫一條斜線、按鈕轉成灰色(`syncNodesBtn()`)。

### v3.19 左欄單行極簡 + 標誌

**v3.19.1 左欄改成單行極簡(方案 A),預設寬度 238 → 150 px。**
一列只留圖示 + 名稱;節點 / 元素數改成**滑過或選到那一列**才顯示(`.meta{display:none}`,
`:hover` 與 `.on` 才 `inline`),名稱因此吃得滿。列的左右內距也收了 3 px。
左欄底下的統計改成**一欄**(標籤左、數字右,10 px),150 px 才排得下;
「最大弦線偏差 / 元素總長 / 編號衝突」的標籤縮成「弦線偏差 / 總長 / 衝突」。
拖曳下限從 150 放寬到 130。

存寬度的 localStorage key 換成 `scg.treeW2` —— 舊使用者存的 238 才不會蓋掉新的預設;
`initTreeW()` 讀不到 localStorage(例如 data: URL)時的 fallback 也一起改成 150。

**v3.19.2 標誌 = 「線」的圖示上下顛倒。** 同一條曲線、同樣三個節點,翻過來變成一條**垂下來**的線
(`M4 5C11 5 13 25 22 25s11 -20 18 -20`),放在標題最左邊,用主題色。
清單裡「線」這個型別的圖示維持原樣(拱形),兩者刻意不同:一個是品牌標誌,一個是型別圖示。

### v3.18 匯入 / 匯出分家、空面板收合

**v3.18.1 匯入與匯出拆成兩顆。** 右上角改成 `匯入` / `匯出` 一對(iOS 那種托盤 + 箭頭,
方向相反)。**匯入**是新的一頁(`paneImport()` / `SEL = "_imp"`),收兩件事:
還原參數檔(JSON 或帶參數檔頭的 .mct)、讀 midas 的材料與斷面名稱。
兩者都可以**選檔案**或**直接貼上文字**(`openPasteDlg()` → 沿用既有的 `loadText()`,
它本來就同時處理名稱庫與參數還原)。**匯出**只留單位、3D 視角、下載與 MCT 預覽。

**v3.18.2 空面板直接收起來(選項 C)。** 沒選任何群組時,`#app` 加上 `.nosel`,
編輯區與它的分隔條 `display:none`,畫布補上那一格。全域頁面(編號 / 匯入 / 匯出)不算「沒選」。
窄版(≤1180px)本來是上下堆疊,`.nosel` 時讓畫布直接補到第一列,不會留一塊空白。
判斷寫在 `renderPane()` 裡,因為 `recompute()` 與 `render()` 兩條路都會經過它。

**v3.18.3 元素設定:沒填就攤開,填完就收。** 新群組的 `etype` / `mat` / `sect` 一律**留空**
(以前預填 BEAM / 1 / 1,看起來像已經設定好了;垂落與扇出原本預填 CABLE 也一併拿掉)。
面板把這三個欄位包進 `<details class="adv elem">`:沒填完就 `open` 並標「尚未設定」,
填完自動收合並在標題顯示 `BEAM 梁 · 材料 2 · 斷面 7`。
因為打字走 `syncRO`(不整塊重畫),收合會發生在**離開欄位、去點別的地方**的時候。
留空不影響匯出 —— `attrOf()` 仍然退回 BEAM / 1 / 1。

**v3.18.4 只剩提示的區塊,收起來時不要留白。** `foldHints()` 多判斷一件事:
若 `.sec > .b` 裡除了提示沒有別的東西,收合時連 `.b` 一起藏掉(「單位」那一區就是這種)。

**v3.18.5 視角九宮格 = 切視角 + 重新框選。** 和 midas 一致:按下去除了轉到那個角度,
還會把整個模型重新框進畫面(`view.locked = false; fitView()`),不必再按一次「重新框選」。

**v3.18.6 合併容差寫清楚。** 欄位名從「容差 (m,0 = 完全相同才併)」(會被截斷)
改成「距離多近算同一點 (m)」,底下加一行**不會被 i 收起來**的 `.note`:
「兩個節點的距離小於等於這個值就併成一個。0 = 座標要完全一樣才併;
以公尺為單位時填 0.001 就是相距 1 mm 以內算同一點。」

### v3.17 版面整理、跨群組參照、提示收合

**v3.17.1 段的起點 / 終點可以指到別的群組的節點。** 參照語法多一種:`@<群組 id>:<第幾個節點>`
(`parseGRef()`;1 起算,存序號不存節點編號,上游重新分割也不會錯位)。
下拉多一組「某某群組的節點」,選了之後旁邊出現「第幾個節點」,另外有一顆
**「在圖上點」** —— 沿用 `PICK`(`mode:"ref"`),點一個節點就填好。
`refPt()` 先試跨群組再回頭找同群組的段。相依關係因此改成 `depIds(g)`:
固定欄位 **加上**線群組各段的跨群組參照,`buildModel` 與 `depChain`(深層複製)都吃這個,
所以被引用的群組排在清單後面也解析得到。**節點段的「連接」不提供這個選項** ——
那個下拉是在講「接續哪一段」,不是在講位置。

**v3.17.2 提示全部收成一顆 i。** 版面上原本一路攤著幾十行 `.hint`。改成
`foldHints()`:每個 `.sec` / 每張段卡只在標題列掛一顆 `i`,預設收起,點了才展開;
開合狀態記在 `HINT_OPEN`(key = 型別 + 區塊標題),重繪後還原,不會打一個字就收回去。
對話框與 3D 視圖的操作說明也同一套。

**v3.17.3 版面。**
- 統計數字(群組 / 節點 / 元素 / 弦線偏差…)從畫布上方搬到**左欄底下**,兩欄小字釘住不捲。
- 全域設定改成右上角兩顆**明確標示**的按鈕(`⚙ 編號設定` / `⬇ 輸出與匯出`),
  和復原 / 重做之間留一段空白;左欄只剩群組。
- 新增群組的選單改成**掛在 body 上的浮層**(和右鍵選單一樣),左欄再窄都不會被裁掉。
- 單位下拉只留英文代號(`m` / `kgf`),不再帶中文括號。
- 視角九宮格的**斜角箭頭**被選到時會跟著變白 —— `.cor` 寫在 `.on` 後面、同權重蓋掉了選取色,補一條 `.cor.on`。
- 節點一律長一樣(v3.16 起),連起點 / 終點的白圈也拿掉了。
- 「全部清空」改成紅框、沒東西可清時停用,確認訊息從「清空所有組?」改成「清空畫布上的所有內容?」。
  「載入範例」在畫布已經有東西時才問一次;空面板不再重複放一顆載入範例。

**v3.17.4 用語與動作。** 「組」在整個介面(含驗收測試的項目名稱)一律叫**群組**。
**原位複製移除**(和位移複製重疊,留著只會誤點),**偏置複製改名為位移複製**。
對接的「配對方式」從下拉改成**六張有圖的卡片**,圖示語彙與群組圖示一致
(灰 = 既有節點,主色 = 這個群組產生的連桿)。

### v3.16 沿線陣列 + 圖面選取

**v3.16.1 沿線陣列(`openArrayDlg` / `pathStations`)。**
把一個群組(可連同上游一起,沿用偏置複製的「獨立 / 連動」)沿**另一條線**複製好幾份。
位置三種給法:路徑上**每個節點一份**(可每 n 個)、沿弧長**等分 N 份**(含頭尾)、
**每隔固定距離**一份;上限 200 份。

**基準點**是群組的範圍中心或第一個節點,複本會被平移到讓基準點落在路徑那一站上。
**跟著線轉向**時繞基準點轉 Z,角度是該站切線方位角**減掉第一站的** ——
所以第一份維持原朝向,後面才跟著彎(方位角自 +Y 順時針,`xf.rot` 是逆時針,所以取負號)。
`dupGroup` 因此多吃兩個東西:`xfDelta.pivot`(把旋轉中心設成基準點)與
`quiet`/`afterG`(一次做很多份時不要每份都重算,而且要依序插在前一份後面,順序才不會反過來)。

**已知限制:**要複製的群組如果**本身已經有旋轉設定**,就沒辦法再自動轉向 ——
一個 `xf` 只表達得了「繞一個中心轉一次再平移」,兩段繞不同中心的旋轉合不起來。
這種情況對話框會擋下來並說明,請改用「只平移」或先把旋轉併進幾何。

**v3.16.2 圖面上點到任何群組的元素都會切過去。** 原本 `elemPickSec()` 只在
`g.type === "CURVE"` 時才顯示,所以點到對接 / 垂落 / 扇出的元素雖然有切換群組,
畫面上卻沒有任何回饋,看起來像沒反應。現在連接族也會顯示元素資料(編號、i–j、長度、
類型 / 材料 / 斷面),只是不給「搬動兩端節點」—— 那些節點是來源群組推導出來的。
元素的命中容差同時從 9 px 放寬到 12 px。

**v3.16.3 節點一律長一樣。** 拿掉每個群組**起點 / 終點**的白色外圈標記。
座標覆寫過的節點仍然標成橘色 —— 那是狀態,不是裝飾。

### v3.15 一次補齊的五個幾何功能 + 視圖與用語

**用語:「組」全部改叫「群組」。** 只有 .mct `*GROUP` 的名稱為了不撞名改叫**匯出群組名**
(KPI 也跟著分成「群組」= 本工具的群組數、「匯出群組」= `*GROUP` 的筆數)。

**v3.15.1 三點圓弧(`arcMode:"three"`)。** 起點 → 通過點 → 終點,圓弧落在這三點自己的平面上。
作法:以 `e1 = 正規化(pm−p1)`、`e3 = 正規化((pm−p1)×(p2−p1))`、`e2 = e3×e1` 建平面座標系,
把三點投影成 2D 求外心與半徑,再用夾角掃過去 —— 掃的方向一定要**經過中間那個點**,
否則走補角。三點共線 / 重合會報錯。水平、鉛直兩種模式保留不動;
在面板上把既有圓弧切成三點模式時,通過點會自動帶入**目前這段圓弧的中點**,
所以切換前後幾何完全一樣(測試以 `拱肋` 實測 3D 總長與弦線偏差不變)。

**v3.15.2 座標表段(`POLY`)。** 貼上一張 X / Y / Z 表,**每一行就是一個節點,不再分割**。
`parseXYZTable()` 每行取「前三個數字」,數字不足三個的行(標題列、空行)自動跳過,
所以從 Excel 整塊貼過來不用先清乾淨;前面有編號 / 樁號欄時用「忽略每行前 n 個數字」跳掉。
座標是**絕對值**,不接續前一段。

**v3.15.3 鏡射(`xf.mir` / `xf.mirAt`)。** 併進既有的變換:**鏡射先做**(對絕對座標的 X / Y / Z 平面),
再旋轉、平移。鏡射會把方位角翻面,沒有單一角度增量可用,所以 `f.dAz` 設 `null`
讓 `annotate()` 重算。偏置複製對話框也有鏡射欄位 —— 平移量留 0 就是純**鏡射複製**,
名字會取「… 鏡射」。

**v3.15.4 垂落的目標高程三選一(`zMode`)。**
`abs` 固定高程(原本的)/ `plane` 過某點、沿 X 與 Y 各給坡度的斜面 /
`ref` 取**另一個群組對應那個節點**的 Z(第 1 個對第 1 個),X、Y 仍照來源節點。
`ref` 模式下端常常剛好落在那條線的既有節點上,會被「合併重合節點」吃掉 ——
這正是想要的:吊索直接接到既有節點,不會多出一份重疊的。

**v3.15.5 扇出的起點(`srcMode:"gnode"`)。** 改用「某群組的第 k 個節點」而不是節點編號 ——
上游重新分割時編號會跑掉,序號不會。舊的 `node`(節點編號)保留但標示為舊做法。
`DEPS` 因此擴充成 `LINKZ:["ref","zRef"]`、`FAN:["ref","srcG"]`,相依解析與深層複製都吃得到。

**v3.15.6 3D 視圖。** 有選到群組時,**其他群組一律畫成黑色**(`IDLE_COL`)細線;
編輯中的群組線寬收到 2.8、節點半徑放大到 4.2 並加白色外圈(`nodeRad()`),
解決「編輯中的線太粗、節點反而看不見」。

**v3.15.7 每個群組可以自己指定顏色。** `g.color`(六位 hex)優先,沒指定就照清單順序輪配
(`groupColor()`)。清單圖示與 3D 視圖用**同一個顏色**,型別靠圖示形狀分辨;
面板名稱旁邊是色票 + 「自動」鈕。

**v3.15.8 手動節點與元素簡化。** 座標改用 `xyzTable`(多一個刪除尾欄),
元素列表改成一排小方塊,三段說明併成一句;新節點改成**生在目前選取的節點上**
(沒選才退回群組中心)。

### v3.14.1 對接的配對方式

`LINK2` 從「兩排同序、節點數必須相同」擴充成**六種配對方式**(`PAIR_MODES`),
節點數不同不再是錯誤 —— 連到用完為止,對不到的出警告。所有模式都吃「目標群組反序」(`rev`)。

> **v3.23 起用語改掉了:**「這排 / 那排」一律改叫 **起始群組 / 目標群組**(`a` / `b`),
> `free` 也從「自訂偏移與步進」改叫 **自訂連接方式**,見 v3.23.4。

| `pair` | 意思 | 額外欄位 |
|---|---|---|
| `same` | 一對一(同序),**舊檔沒有 `pair` 欄位時就是這個**,行為與 v3.13 相同 | — |
| `skew` | 起始群組第 j 個接目標群組第 j + n 個 | `skew` |
| `cross` | 相鄰兩格拉一對來回 → 一個組就是完整的 X 形斜撐 | — |
| `ratio` | 依**弧長比例**對應(`arcParam()` 累積 3D 長度正規化),兩個群組分割數不同也能連 | — |
| `near` | 每個節點接空間中最近的那個節點;多對一會出警告 | — |
| `free` | 自訂連接方式:一個樣式單元沿兩個群組重複(v3.23.4) | `pattern` `aFrom` `aEvery` `bFrom` `bEvery` `count` |

前五種以「起始群組」的節點範圍(`from` / `to` / `every`)為準;`free` 用自己那一組欄位,
`count = 0` 表示重複到其中一個群組的節點用完為止。

**視圖直選:**面板上的「在視圖上點兩個節點…」沿用手動元素那套 `PICK`(多帶一個 `mode:"link2"`),
點完兩個節點後由 `locateNode()` 反查各自屬於哪一組、在該組節點串裡排第幾個,
填成 `pair:"free"` + `a`/`aFrom` + `b`/`bFrom`。**存的是「第幾個」而不是節點編號**,
上游的線之後重新分割,對接關係才不會錯位。

### v3.14.2 偏置複製:獨立 / 連動

`dupGroup(g, mkName, xfDelta, deep)`。`deep` 時先用 `depChain()` 取出**這組所參照的全部上游**
(含自己),整串複製,再把複本的 `DEPS` 參照改指到複本。

為什麼要有這個:連接族的內容是**由來源組算出來的**,所以只複製它自己時,
複本仍然掛在原來那條線上 —— 使用者看到的現象就是「複製完還是跟著原組走,改自己的數值也沒用」。
垂落組更明顯:`xf` 只搬「本組自己的節點」(下端),上端還釘在原來的主纜上,複本會變成斜的。

對話框在該組有上游時才出現「複製方式」:**獨立(預設,連上游一起複製)/ 連動(只複製這一組)**。
連動仍然有用 —— 同一條主纜掛兩排吊索、或同一條線的另一段範圍。
份數 > 1 時每一份都從**原組**複製、偏置量乘份序(不再接力複製,名字才不會越疊越長)。

### v3.14.3 座標欄位排版

新增 `xyzTable()`:座標一律排成 **X / Y / Z 三欄的小表**,列是起點 / 終點 / 圓心,上下對齊,
缺某一軸留一格「—」。舊版把六個座標欄直接丟進四欄網格、前面還有下拉佔格子,
結果**終點 Z 被擠到下一行、而且跟起點的 X / Y / Z 對不齊**。
直線 / 圓弧 / 懸垂 / 螺旋 / 節點段都改用這個表。

> **v3.13.1:連接族的組也可以被引用。** 組對組的「組 A / 組 B」與點對組的「目標組」原本只列
> 幾何族,導致「組對高程 + 組對高程 → 用組對組把兩排吊點連起來」這種常見做法根本選不到組
> (組別一多時看起來就像「後面幾個組別不見了」)。現在所有引用組別的下拉都列出全部組,
> 並用 `optgroup` 分成幾何族 / 連接族、附上目前節點數。相依解析本來就不看清單順序,
> 循環引用仍會被 `buildModel` 擋下並報錯。

> **v3.7:偏置已經不是一種組別**,改成對任何一組執行的「偏置複製」動作(見 §v3.7.1)。
> 舊的偏置組保留可運作,但不再提供新增,所以新建的模型只會有 **4 種**組別。
> 另外,**每一種組都可以手動新增節點與元素**,也都可以整組平移旋轉。

**面板結構固定(v3.9 起):** 基本(名稱 → **元素預設 → 纜索(只有用 CABLE 時)** → 編號一行唯讀)
→ 段(或 連接/放置)→ 節點(**只有選到節點時**)→ 手動節點與元素 → 結果。
整組的平移旋轉改用標題列的**小視窗**。每個段是一張可摺疊的卡片,分割與屬性覆寫收在段的「進階」裡。

## v3 刪掉的東西

- **3D 剛體轉換模組** —— 端點直接給全域座標就定位完成,不需要先在局部座標算完再轉。
- **「主線」的特殊地位** —— 線形鏈只是其中一種組,可以有 0 個或 5 個。
- **全域縱斷面** —— 變成線形鏈自己的屬性,而且是**選用**的(沒有就整條維持起點高程)。
- **餘數對話框** —— 改成每組的一個下拉選單,預設「均分吸收」。
- 「主纜/吊索/橫梁/斜張索」四個橋梁名詞 —— 變成三種通用連接型別。

## v3 的關鍵實作細節

- **相依解析**:偏置/連接族引用其他組,採反覆解析,**被引用的組排在後面也能正確建立**;循環引用會報錯且不當機。
- **自動配號**:全域起始 + **本組在清單中的順序** × 號段增量(與相依解析順序無關,可預測);另有全域衝突檢查。
- **圓弧兩點加半徑有兩組解**:偏向決定圓心在哪一側,弧段(優/劣)決定走短的還是長的;半徑必須 ≥ 弦長一半。鉛直平面的 `dir = +1` 為**上凸**(圓心在弦線下方)。
- **弦線偏差量的是「元素到真實曲線的 3D 垂距」**。線形鏈有縱斷面時,偏差會比平面解析解 `R(1−cos(θ/2))` 大,因為含鉛直分量。
- **立面圖橫軸**:組與組沒有共同樁號,所以用**投影到某個水平方向**的距離(自動 / 沿 X / 沿 Y / 自訂方位角)。

---

以下為 v2 規格,幾何公式、檢核定義、MCT 格式與待驗證清單**仍然適用**(v3 沿用同一套引擎)。

# 曲線節點/元素產生器 — 開發規格 v2

> v1 → v2 的主要變更:改為**樁號 (station) 為主鍵**、**平面線形與縱斷面分離**、加入**偏置線/橫梁/纜索**、加入**群組輸出與檢核報表**、移除 Catmull-Rom 與 sensitivity study 賣點。

---

## 0. 定位

**給拿到道路線形表數字、但沒有(或不該用)CAD 的結構工程師,把線形表變成 midas Civil 分析模型的幾何。**

不是要取代 Civil 3D(線形設計是它的本業),而是補「線形表 → 分析模型節點」這一段今天由 Excel 手工填補的縫。

**現有做法對照:**

| 現有做法 | 它強在哪 | 它的斷點 |
|---|---|---|
| Excel 逐樁號算 | 天然以樁號為主鍵,可交叉檢核 | 沒有視覺回饋、多段方位角遞推易錯、只有節點沒有元素、不能直接產 .mct |
| midas Rotate Copy | 單一等半徑圓弧最快,一步到位 | 做不出緩和曲線、多段串接、縱坡、樁號控制 |
| Civil 3D / OpenRoads | 線形建立與規範檢核極強 | 抽點到分析模型無法控制節點位置與樁號;結構工程師多半沒授權也不會用 |

**本工具的四項差異化(規格第一段就要講):**

1. 即時圖形預覽(平面 + 側視),參數錯了立刻看得出來
2. 多段自動切線銜接,含反向/複合曲線與緩和曲線
3. 一次輸出節點 + 元素 + 群組的 .mct
4. 自動檢核(弦線偏差、總長縮短)與可追溯的節點清單報表

**這版明確不做:** Catmull-Rom 控制點模式、超高與 beta angle、施工階段/邊界/荷載群組、梁格與殼元素模型。

---

## 1. 核心資料模型

**單一主鍵:樁號 s(沿主線的累積里程,m)。**

平面線形與縱斷面是**兩份獨立的資料**,只在樁號上相遇:

```
平面線形段落表  ──┐
                  ├─→ 對每個樁號 s 求值 ─→ (X, Y, Z, 方位角, 當地曲率)
縱斷面 VPI 表  ──┘
```

這是整份規格最重要的架構決定。原因:平面的分段點(直線/緩和/圓弧交界)與縱斷面的分段點(變坡點)**位在不同樁號**,綁在一起就表達不出實務線形。

### 演算流程

1. 由平面段落表建立線形,計算每段的起訖樁號 → 得到總長
2. 依各段的分割規則 + 強制節點樁號,產生**樁號清單**(去重、排序)
3. 對每個樁號:平面求值得 (X, Y, 方位角);縱斷面求值得 Z
4. 生成主線節點 → 偏置線節點(法線投影)→ 纜索節點
5. 連接元素、指派屬性與群組
6. 輸出 .mct / CSV / JSON

### 座標與角度慣例

- **X = 東,Y = 北,Z = 高程**(對應 midas 整體座標)
- **方位角 (azimuth)**:自 +Y 軸(北)起,**順時針**量測,單位度,範圍 [0, 360)
- 因此:`X += L·sin(az)`,`Y += L·cos(az)`
- **偏向**:以行進方向論,**右偏 = +1,左偏 = −1**

### 樁號格式

- 內部一律用 **double(公尺)**
- 顯示與輸入用台灣慣例 **`0k+125.500`**(公里數 + `k+` + 三位小數的公尺)
- 輸入框需同時接受純數值(`125.5`)與格式字串(`0k+125.500`)
- 起始樁號可自訂,預設 `0k+000.000`

---

## 2. 平面線形

> **線形可以完全不存在。** 段落數允許為 **0**,而且**預設就是 0**(開啟頁面是空的,要範例請按「載入案例 A / S 彎 / 懸索橋」)。
> 主纜模組(§6.4)有自己的局部座標系,不需要線形也能單獨產生節點、元素、群組並匯出 .mct。
> 偏置線、橫梁、扇形纜索則依賴線形節點,沒有線形時自然為空(不報錯)。

一張表格,每列一個段落,由上而下串接。除第一段(或勾選「獨立起點」的段落)外,每段的起點座標與起始方位角**自動繼承前一段終點**,保證切線連續。

### 2.1 段落型別與參數

**直線 (LINE)**

- 長度 `L`

**圓弧 (ARC)**

- 半徑 `R`
- 偏向 `dir`(左 / 右 下拉選單,不要求輸入正負號)
- 二選一定義長度:**偏角 Δ**(度)或 **弧長 L**;`L = R·Δ(rad)`

**緩和曲線 (SPIRAL,clothoid)**

- 起始半徑 `R0`(可為 ∞,代表接直線)
- 終止半徑 `R1`(可為 ∞)
- 偏向 `dir`
- 二選一定義長度:**長度 Ls** 或 **A 值**(`Ls = A² / R`,R 取有限的那一端半徑)

> A 值是道路線形表的常用交付形式,必須支援,否則使用者仍要自己換算。

**獨立起點(每段可勾選)**

- 起點 X、Y、起始方位角、起始樁號
- 用於同一模型中互不相接的獨立構件

### 2.2 公式

令段落起點 `P0 = (x0, y0)`、起始方位角 `az0`、段內里程 `t ∈ [0, L]`。

**直線**

```
az(t) = az0
x(t)  = x0 + t·sin(az0)
y(t)  = y0 + t·cos(az0)
```

**圓弧**

```
az(t) = az0 + dir·(t/R)                        [弧度,顯示時轉度]
圓心 C = ( x0 + R·sin(az0 + dir·90°),
           y0 + R·cos(az0 + dir·90°) )
β(t)  = az0 − dir·90° + dir·(t/R)
x(t)  = Cx + R·sin(β(t))
y(t)  = Cy + R·cos(β(t))
```

(可驗證 `t = 0` 時回到 `P0`)

**緩和曲線**

```
k0 = 1/R0  (R0 = ∞ → 0)
k1 = 1/R1  (R1 = ∞ → 0)
k(t)  = k0 + (k1 − k0)·t/Ls                        曲率線性變化
az(t) = az0 + dir·[ k0·t + (k1 − k0)·t²/(2·Ls) ]   [弧度]
x(t)  = x0 + ∫₀ᵗ sin(az(u)) du
y(t)  = y0 + ∫₀ᵗ cos(az(u)) du
```

積分用 **Simpson 法**,步長 `min(0.1 m, Ls/1000)`。

### 2.3 驗證與警告

- 反向曲線 (S 彎):相鄰兩段 `dir` 相反時自動成立,PRC 因切線連續無斷點 — 不需特別處理,但預覽應標示反曲點
- 緩和曲線的 `R0`、`R1` 不可同時為 ∞(退化成直線)→ 錯誤
- 圓弧 `R ≤ 0` → 錯誤
- 段落長度 ≤ 0 → 錯誤

---

## 3. 縱斷面

另一張表格,**VPI(變坡點)清單**,以樁號排序。

### 3.1 輸入

| 欄位 | 說明 |
|---|---|
| 樁號 | VPI 位置 |
| 高程 Z | VPI 的設計高程 |
| 豎曲線長 L | 對稱拋物線的水平長度;`L = 0` 表示直接折角 |

**第一列與最後一列**視為起訖點(`L` 欄位停用),只給樁號與高程。

若只有兩列(起點、終點)→ 等坡度直線,即 v1 的行為。

### 3.2 公式

相鄰 VPI 之間的坡度:

```
g_i = (Z_{i+1} − Z_i) / (S_{i+1} − S_i)
```

VPI `i` 的對稱豎曲線(進入坡度 `g_in`、離開坡度 `g_out`、長度 `L`):

```
BVC = S_i − L/2
EVC = S_i + L/2
```

- `s < BVC` 或 `s > EVC`:沿相鄰坡度線性內插
- `BVC ≤ s ≤ EVC`,令 `x = s − BVC`:

```
Z(s) = Z_i − g_in·(L/2) + g_in·x + (g_out − g_in)·x² / (2L)
```

顯示用參數:

- 中央縱距 `E = (g_out − g_in)·L / 8`
- `K = L / |g_out − g_in|`(坡度以 % 計)

### 3.3 檢核

- **相鄰豎曲線重疊**(`EVC_i > BVC_{i+1}`)→ 錯誤,不可產生
- 豎曲線超出縱斷面起訖樁號 → 錯誤
- `L = 0` 且坡度變化 ≠ 0 → 警告(模型會有折角)
- **縱斷面範圍未涵蓋線形範圍** → 警告,超出部分以端點坡度**線性外插**(不阻擋產生)
  - 容差 1 mm:小於此的落差在工程上無意義,不發警告(否則 `257.080` 對上 `257.07963` 會一直跳)

### 3.4 已移除

v1 的「每段一個線性傾角 θ」**廢除**。縱斷面與平面段落完全解耦。

---

## 4. 節點分割

**每段獨立設定**(直線段可疏、小半徑彎道要密),另提供「套用到所有段落」按鈕。

### 4.1 五種模式

| 模式 | 輸入 | **量的是什麼** | 適用 |
|---|---|---|---|
| **指定元素數** | N | **曲線參數 u**(意義依段型別而定,見 4.1.1) | 快速試算 |
| **指定間距** | d | **沿曲線的 3D 弧長** | 最常用 |
| **軸向間距**(v3.12) | 軸 X/Y/Z + d + 格線基準 | **該軸的座標差**(既非弧長亦非弦長) | 要讓節點落在整數樁號 / 整數座標的平面上、跨組對齊 |
| **間距表**(v3.13) | midas 寫法字串 `3 36@4 3` + 量測方向 | 依量測方向:**弧長**或**某軸的座標增量** | 端部加密、跨中放疏、照設計圖的分段表直接打 |
| **弦線偏差容差** | m_max + **最大間距上限** | **曲線離弦線的最大垂距**(步長本身仍是弧長) | 唯一能同時吃圓弧與緩和曲線的規則;自動在曲率大處加密 |

> **最大間距上限**是實作時追加的必要欄位:容差模式在直線段(R = ∞)算出的容許弦長是無限大,沒有上限的話整條直線會變成一個元素。預設 20 m。

> v1 的「等圓心角」刪除:在圓弧上等同於等弧長(冗餘),在緩和曲線上無定義。其正確的廣義形式就是弦線偏差容差(= 等切線轉角)。

**弦線偏差模式的實作:** 由當地曲率求容許弦長 `L_chord = √(8·R·m_max)`,沿段落逐步推進;緩和曲線上 R 隨樁號變化,故每步重算。

### 4.1.1 弧長還是弦線?—— 四種模式各自量的東西

三種長度必須分清楚,**它們不是同一個數字**:

- **弧長**:沿曲線本身量的長度(`makeCurve()` 以 400 段折線累積,3D)。
- **弦長**:兩個節點之間的直線距離 —— 也就是 midas 裡**元素真正的長度**。曲線上弦長恆 < 弧長。
- **軸向座標差**:兩個節點在 X / Y / Z 某一軸上的座標差。

| 模式 | 設定的數字量的是 | 產出的元素(弦長) |
|---|---|---|
| 指定元素數 | 參數 u 的等分 | 依段型別而定,見下表 |
| 指定間距 d | **弧長** | **略短於 d**(直線段相等) |
| 軸向間距 d | **該軸的座標差** | **d ÷ 方向餘弦 ≥ d** |
| 間距表 | 逐段的**弧長**或**軸向座標增量**(由量測方向決定) | 同上兩列,逐段不同 |
| 弦線偏差容差 | 曲線離弦線的最大垂距 | 隨曲率變動,曲率大處短 |

**「指定元素數」的 u 在各段型別的意義**(這是最容易誤會的一項):

| 段型別 | 等分 u = 等分什麼 | 是否等弧長 |
|---|---|---|
| 直線 | 長度 | 是(弧長 = 弦長) |
| 圓弧 | **圓心角** | 是(且弦長也相等) |
| 懸垂 | **弦線的水平投影**(x、y 隨 u 線性) | **否** —— 坡度大處弧長較長 |
| 螺旋 | **圓心角** | 等半徑時是;半徑漸變時否 |
| 線形鏈 | **每個子段各自**切 N 根,子段內沿弧長等分 | 子段內是 |
| 節點 | 不分割 | — |

介面上這件事有三個地方寫明:分割方式下拉的標籤(「指定間距(沿弧長)」)、下方隨模式與段型別變動的說明句(`splitMeasure()`),以及段卡片裡唯讀的**實際結果**一行 —— 同時列出「曲線長(弧長)」、「元素弦長 min–max」與「弦長總和」,讓兩個數字直接對照。

### 4.1.2 軸向間距(v3.12)

**做法:** 用一組**垂直於 X / Y / Z 某一軸、間隔 d 的平面**去截這一段曲線,每個交點放一個節點(`splitAxis()`)。

- **格線基準**兩選一:
  - **本段起點**(預設)—— 格線通過段起點,起點必在格線上,零頭只會出現在終點側。
  - **指定座標** —— 格線通過「該軸 = 基準座標」的平面,並以 d 為間隔往兩側延伸。這是**跨段、跨組對齊**用的:不同段填同一個基準值,節點就會落在同一組平面上。頭尾各留一段零頭。
- **餘數處理只提供「獨立留一段 / 併入相鄰元素」。** 「均分吸收」「頭尾各分一半」會把整組格線挪走,軸向間距就失去對齊的意義,所以不給。
- **非單調的段(曲線在該軸上來回)每一次穿越都放節點**,並警告折返處數量 —— 折返點附近必然出現極短元素。
- **與該軸垂直的段**(該軸座標不變)切不出任何格線 → 整段一個元素 + 警告。
- 直線的**兩端延長**沿用同一套語意:延長段的標稱間距換算成 `d ÷ 方向餘弦`(`axisNominal()`),元素長度與基準段一致。

**實作要點:**
- 沿**弧長等距**取樣(u 不一定與長度成正比),每一格至少 6 個取樣點才抓得到折返;逐取樣區間以半開區間 `(ga, gb]` / `[gb, ga)` 判斷跨過哪幾條格線,再在區間內二分求交點。
- **去重必須用幾何距離,不能只看 u。** 端點剛好落在格線上、且曲線在該處與格線相切時(例:1/4 圓弧的終點沿 Y 分割),交點會收斂到 `u` 略小於 1 的位置、座標卻與終點相同 → 產生零長度元素。改用 `dist3` 與「前一個交點 / 終點」比對才擋得住。
- 保險上限 `AXIS_MAX_EL = 20000`:間距打太小時退回整段一個元素並警告,不讓頁面卡死。

### 4.1.3 間距表(v3.13)

照 midas Civil 的寫法直接打一串間距,例如 `3 36@4 3` = 3 + 36 段 × 4 + 3 = **150**,切成 **38 根元素**。

- **語法**(`parseSpacingList()`):`n@d` 是「**n 段、每段 d**」,單獨一個數字就是一段。
  分隔符接受空白 / 逗號 / 分號(含全形),`@` 前後可以有空白。任何看不懂的 token 都回報訊息並退回整段一根,不當機。
- **量測方向**四選一:**沿弧長**(預設)或 **X / Y / Z 軸**。
  選軸時,表裡每一個數字是**該軸的座標增量**,方向取本段起點 → 終點在該軸上的走向
  (終點在起點的負向時,表就往負向鋪)。斜的段元素長度 = `d ÷ 方向餘弦`。
- **表長與段長不符時**三選一(這一格取代了一般的「餘數處理」):
  - **尾端獨立留一段**(預設)—— 剩下的長度自成一根元素
  - **併入最後一根** —— 不放最後一個站點,零頭併進去
  - **整表按比例縮放到剛好** —— 把整張表乘以 `段長 ÷ 表長`,適合表寫的是相對比例
  三種都會在檢核面板明講差多少;表比段長時,放不下的段數會被略過並警告。
- **線形鏈是每個子段各自套用整張表**(與其他模式一致)。
- 直線的兩端延長:延長段各自沿用**最靠近那一端的間距**(`nomOf("S")` / `nomOf("E")`),
  選軸量測時一樣先換算成 `d ÷ 方向餘弦`。

**踩過的坑:**
- **「剛好鋪到終點」不能算成「放不下」。** 軸向量測的迴圈原本用 `dir*(tgt − a1) > −eps` 當中斷條件,
  最後一段剛好落在終點時也會中斷,`dropped` 變成 1 → 明明完美對上卻跳「最後 1 段放不下」的假警告
  (幾何是對的,只有訊息錯)。要先判 `> eps`(真的超出)再判 `|tgt − a1| ≤ eps`(剛好到底)。
- **即時結果那一行必須自己一個 `data-ro` 的 `<div>`。** 原本把它塞在說明句的 `.hint` 裡,
  `syncRO()` 只換 `[data-ro]` 的 innerHTML,打字時那行永遠是舊值。
  拆出來之後另一個問題是「沒有元素時內容為空」會讓 `[data-ro]` 的**數量**在兩次算繪之間改變 →
  `syncRO()` 判定結構變了就整塊 `renderPane()` → 輸入框被重建、游標跳掉(v3.3 那個老坑)。
  解法是**永遠輸出這個 div**,再用 CSS `.hint:empty{display:none}` 讓它沒內容時不佔位
  ——**不能**用 `style="display:none"` 切換,因為 `syncRO()` 不會更新屬性。
- 換單位時**整張表要重寫**(`scaleSpacingList()`:parse → 乘係數 → 寫回同樣的 `n@d` 形式),
  不能只換數值欄位。

### 4.2 強制節點

- 輸入:樁號清單(逗號或換行分隔,接受 `0k+180.500` 或 `180.5`)
- 用途:支承中心、伸縮縫、橫隔梁、施工分段、預力錨定、變斷面起訖
- **與均分節點的合併:容差內(預設 0.1 m,可調)自動吸收均分節點,只保留強制節點**
- 段落交界樁號永遠是節點,且**只產生一次**(共用節點不重複編號)

### 4.3 餘數處理

均分後若有餘數,**產生完成後跳出對話框**讓使用者選(實作:編輯停止 700 ms 後自動彈出,關閉後同一情境不再重複打擾;檢核面板常駐一條「處理餘數…」提示可隨時再開):

1. **均分吸收** — **維持原本的區間數**,間距略為放大(例:31 段 × 5.000 m + 餘數 2.080 → 31 段 × 5.067 m)
2. **頭尾各分一半**(v3.8 新增)— 餘數除以二,**頭尾各留一半**,中間維持標稱間距
   (例:全長 100、間距 30 → 5 + 30 + 30 + 30 + 5)。對稱結構、跨中對稱配置很方便。
3. **獨立留一段** — 保留短元素
4. **併入前一元素** — 刪除該節點,最後一個元素變長;**幾何不變**

> 「截短曲線」(真的把終點往回移)**僅在最末段提供**,且必須明示「終點後退 x.xxx m」。中間段落禁用,否則後續所有段落會跟著平移。

> **實作註(v3.8)**:對話框的設計已作廢,餘數處理是段的「分割與元素覆寫」裡的一個下拉。

無論選哪一種,餘數長度小於間距 30% 時**顯示警告**(極短元素會惡化勁度矩陣條件數)。

### 4.4 即時檢核顯示(預覽面板常駐)

- 每段的**最大弦線偏差** `m ≈ L²/(8R)`,單位 mm
- **總長縮短量**:折線總長 vs 真實弧長,絕對值(m)與百分比
  - 單元素縮短率 `≈ 1 − sin(θ/2)/(θ/2) ≈ θ²/24`,`θ = L_chord/R`
- 最短 / 最長元素長度
- 節點總數、元素總數
- **3D 元素總長,以及它比平面折線長多少(mm 與 %)**
  - 樁號是**平面里程**(業界慣例),但 midas 裡元素的長度是真實 3D 長度
  - 定坡 `g` 時比值為 `√(1+g²)`;`g = 4%` → +0.08%
  - 這與弦線縮短是**方向相反**的兩個效應,面板上必須各自列出,不可混為一談
- 最大坡度、節點高程範圍

> 這一區是最便宜的信任來源。使用者不會自己算,但錯了會直接影響自重、預力鋼腱長度與支承位置。

---

## 5. 偏置線與橫梁

### 5.1 偏置線

> **⚠️ v3.7 起本節描述的是「舊的偏置組」。** 新的「偏置複製」是**剛體平移的複本**,不是這裡的平行曲線
> —— 兩者幾何不同,詳見 §v3.7.1。以下的法線投影邏輯仍在程式裡(舊組別照常運作),但不再提供新增。

以主線為基準產生平行線。可新增任意多條。

| 欄位 | 說明 |
|---|---|
| 名稱 | 用於群組命名 |
| 橫向偏置 d | **右 = 正,左 = 負**(以行進方向論),m |
| 垂直偏置 h | 整體 Z,上為正,m |
| 元素類型 / 材料 / 斷面 | 獨立指定 |
| 節點/元素起始編號 | 見 §7 |

**產生規則(關鍵):以主線樁號為基準,在每個主線節點做曲線法線,法線與偏置線的交點即偏置節點。**

```
偏置點 = ( x(s) + d·sin(az(s) + 90°),
           y(s) + d·cos(az(s) + 90°),
           z(s) + h )
```

**後果(必須寫進限制):**

- 偏置線上的節點**間距不相等**(外側疏、內側密),但**所有線在同一樁號上橫向嚴格對齊**,橫梁必為直線
- **當地半徑 `R_local = R − dir·d`**,弦線偏差與主線不同,檢核面板要分線顯示

> ⚠️ **哪一側是內側,要用 `dir` 判斷,不能憑直覺。**
> `dir = +1`(右偏)時**圓心在右**,所以**右側偏置(`d > 0`)是內側**,`R_local = R − d`;
> 左側偏置(`d < 0`)是外側,`R_local = R + |d|`。左偏曲線則相反。
> 通式:`R_local = |1/k − d|`,`k = dir/R` 為有號曲率(正 = 圓心在右)。

- **當偏置點達到或越過曲率中心(`1/k − d` 變號或為 0)→ 幾何退化 → 錯誤,拒絕產生**
  - 對 `dir = +1`、`R = 100` 而言:`d = +100` 或 `d = +106` **報錯**;`d = −106`(外側)**合法**

### 5.2 橫梁(可選)

勾選後,**在每個樁號上把該樁號的所有線由左至右串成一排**:

- 線的排序依橫向偏置 `d` 由小到大(最左 → 最右),主線 `d = 0` 一併納入
- 每個樁號產生 `(線數 − 1)` 根元素
- 可指定元素類型 / 材料 / 斷面 / 群組
- 可指定「每 n 個樁號放一根」(不必每個節點都有橫梁)

---

## 6. 纜索

獨立的生成器,與沿曲線生成邏輯不同。

### 6.1 輸入

| 欄位 | 說明 |
|---|---|
| 起點 | 自訂 X/Y/Z,或指定一個已生成的節點編號 |
| 目標節點 | 節點編號清單(支援 `101-120` 範圍語法) |
| 分割數 N | `N = 1` → 單一元素(斜張索);`N > 1` → 切成 N 段 |
| 跨中垂度 f | **絕對值 (m) 或垂跨比**(下拉選);`N = 1` 時停用 |
| 元素類型 / 材料 / 斷面 / 群組 | 獨立指定 |

**一組纜索 = 一個起點 + 一組目標節點(扇形)。** 起點用「自訂座標」時,該組只產生**一個**起點節點(塔頂),由所有索共用;用「既有節點」時**不新增節點**。

**節點清單解析的兩個坑(實作時踩到的):**

- 空白必須當作**分隔符**,不能整串移除 —— `"1006  1008-1010"` 若先 `replace(/\s+/g,"")` 會黏成 `"10061008-1010"`,展開成上千萬個編號。正確做法是**只吃掉連字號兩側的空白**,其餘空白仍是分隔符。
- 範圍長度要設**上限(10000)**,超過就警告並略過。輸入錯誤不該讓瀏覽器當掉。

### 6.2 幾何

- `N = 1`:起點直連目標節點,一根元素,無中間節點
- `N > 1`:沿弦線等分,疊加**拋物線垂度**。令 `t ∈ [0,1]` 為沿弦的比例:

```
P(t)   = 起點 + t·(終點 − 起點)
P(t).Z = P(t).Z − 4·f·t·(1 − t)
```

### 6.3 使用注意(寫進限制與 UI 提示)

- **斜張橋斜索建議 `N = 1`**:midas 的 CABLE 元素內建垂度處理(等效彈性模數),切分反而失去該處理
- **`N > 1` 適用懸索橋主纜或需觀察纜索本身變位的情況**,通常須搭配幾何非線性分析
- **`N > 1` 且 `f = 0`(共線直線串)在幾何非線性分析中橫向勁度為零,極可能不收斂** → UI 需明確警告

### 6.4 主纜模組(懸索橋)

與 §6.1 的扇形纜索是**兩個不同的生成器**:這裡先在**局部座標**算主纜與吊索,再用兩個控制點做 **3D 剛體轉換**放進全域座標。

**局部座標系:** `x` 沿跨徑、`y` 橫向、`z` 鉛直。兩端鞍座在 `(0, ·, z1)` 與 `(Lx, ·, z2)`。

| 欄位 | 說明 |
|---|---|
| 跨徑 Lx、左右鞍座局部 z1 / z2 | 允許不等高 |
| 曲線 | **拋物線** 或 **懸鏈線** |
| 垂度 f | 垂跨比或絕對值,**自弦線量測** |
| 主纜分割 N | 產生 N+1 個節點、N 根元素 |
| 索面數 / 橫向間距 | 1(單索面)或 2(雙索面,`y = ±gap/2`) |
| 吊索 | 橋面局部 z、每 n 個主纜節點一根、下端**新增節點**或**貼附既有節點(給容差)** |
| 主纜 / 吊索的元素類型、材料、斷面 | 各自獨立 |

**幾何公式**(兩者都是「弦線 + 下垂量」的疊加,與 §6.2 的作法一致):

```
弦線    chord(x) = z1 + (z2 − z1)·x/Lx
拋物線  dip(x) = −4f·t(1−t),  t = x/Lx
懸鏈線  dip(x) = a·cosh((x − Lx/2)/a) − a·cosh(Lx/(2a))
        其中 a 由 f = a(cosh(Lx/2a) − 1) 以二分法解出
z(x) = chord(x) + dip(x)
```

> **懸鏈線 vs 拋物線:** 懸鏈線的自重按**弧長**分布(端部較陡處重量較多),所以同跨徑同垂度下,懸鏈線在 1/4 點**比拋物線低**、纜長**比拋物線長**。
> 差異很小:`f/L = 0.1`、`L = 400 m` 時,1/4 點只差 **0.097 m**(0.24%)。一般建模兩者可互換,精算才需要分。

> **v3.10**:垂度的**量測基準**可選(弦線 / 高側 / 低側 / 指定高程),見 §v3.10.1。
> 1/4 點解析解:`z = −f·(cosh β − cosh(β/2))/(cosh β − 1)`,`β = Lx/(2a)`;`β → 0` 時該比值 → `0.75`,退化成拋物線。

> **不等高鞍座的近似:** 懸鏈線採「懸鏈線疊加在弦線上」的作法,對不等高鞍座是近似解(真實懸鏈線不對稱)。與拋物線版本用同一套近似,足夠一般建模;精算須另解。

### 6.5 空間剛體轉換

**輸入:** 局部基準點 `P1`(左鞍座 / 右鞍座 / 局部原點 / 自訂)→ 目標 `Q1 (X1,Y1,Z1)`;局部方向點 `P2` → 目標 `Q2 (X2,Y2,Z2)` **或**平面方位角 `θ`;另可給繞主軸的**滾轉角 roll**。

**演算法:**

```
v = P2 − P1            局部主軸
u = Q2 − Q1            目標主軸
R = 使 v 轉到 u 的最小旋轉(Rodrigues,軸 = v×u,角 = atan2(|v×u|, v·u))
P_global = R·(P_local − P1) + Q1
```

> ⚠️ **`T = Q1 − P1`、`P_global = R·P_local + T` 是錯的。**
> 代入 `P1` 得 `R·P1 + Q1 − P1`,只有在 `R·P1 = P1`(基準點落在旋轉軸上,通常是原點)時才等於 `Q1`。
> 基準點不在原點時**整座橋會平移錯位**。正確的平移量是 **`T = Q1 − R·P1`**,等價於上式。

**兩種方向模式:**

| 模式 | 行為 | 適用 |
|---|---|---|
| **目標點 Q2** | 完整 3D 最小旋轉 | 兩端座標都已知 |
| **平面方位角 θ** | **只繞整體 Z 軸轉**,鉛直方向完全不動 | 只知道全橋方位,不想讓結構傾斜 |

**必須揭露的殘差:** 剛體轉換**不縮放**。若 `|Q2 − Q1| ≠ |P2 − P1|`,方向點會落在 `Q1 + |v|·û` 而**不是** `Q2`,差值 = `||u| − |v||`。UI 與檢核面板要顯示這個殘差並發警告——方向是對的,只是長度不同。

**退化情況:** `v` 與 `u` 反向(夾角 180°)時 `v×u = 0`,須改繞任一垂直軸轉 180°,不可讓矩陣出現 NaN。

**編號:** 每組主纜各佔一號段,預設 `20001+`(+ 號段增量)。群組為 `{前綴}MAIN_{名稱}` 與 `{前綴}HANGER_{名稱}`。

**輸出:** 節點與元素直接併入既有的 `*NODE` / `*ELEMENT` / `*GROUP` 與 CSV 報表,**不另建一套匯出程式**。

**獨立存在:** 主纜模組**不依賴平面線形**。線形段落為 0 時,主纜仍可正常產生、預覽、檢核與匯出;此時側視圖會自動改用主纜主軸當橫軸(見 §11)。

---

## 7. 編號管理

- 主線、每條偏置線、橫梁、每組纜索各佔一個**號段**
- 號段增量使用者自訂,**預設 1000**
- 建議配置:主線 `1001+`、右偏置 `2001+`、左偏置 `3001+`、橫梁 `8001+`、纜索 `9001+`
- 節點編號與元素編號各自獨立成號段
- 段落交界的共用節點只產生一次
- 產生前檢查號段是否重疊 → 錯誤

---

## 8. 群組 (`*GROUP`)

- **每個段落一組**:`{前綴}ARC_01`、`{前綴}SPIRAL_02`、`{前綴}LINE_03`
- **每條偏置線一組**、**橫梁一組**、**每組纜索一組**
- **另有一個涵蓋全部的總組**:`{前綴}ALL`
- 前綴可自訂,**預設 `GEN_`**(避免與既有模型群組撞名)
- 命名保留擴充空間,未來可長出邊界群組 / 荷載群組

> 群組是「模型須重建」的止血帶:使用者可一鍵選取整批舊元素刪除,不必在 3D 視圖裡框選。

---

## 9. 輸出

### 9.1 .mct 檔

包含 `*UNIT`、`*NODE`、`*ELEMENT`、`*GROUP` 四個區塊。

> **v3.7 起整份 .mct 是純 ASCII**(註解全英文、群組名清洗成英數底線、參數 JSON 逸出),
> 因為 midas 匯入時常以系統 ANSI 編碼讀取,中文會變亂碼 —— 而 `*GROUP` 的 `NAME`
> **是功能性欄位不是註解**,壞掉就是真的壞掉。詳見 §v3.7.5。
> 檔頭時間戳為**本機時區**(§v3.7.6)。

**`*UNIT` 一律輸出。** 長度單位由使用者選(M / CM / MM / IN / FT),力單位由使用者選(預設 KN)。

> **⚠ 本段已於 v3.5 作廢** —— 現在是「輸入什麼單位就算什麼單位,匯出不換算」,見前面的 v3.5 一節。以下保留當初的推理紀錄:
>
> **座標換算(實作時補的關鍵行為):** 介面上所有長度**一律以公尺輸入**,匯出 .mct 時才依選定的長度單位換算(選 MM 就 ×1000)。CSV 與檢核面板維持公尺。
> 不做這個換算的話,選了 MM 會得到一座**一千倍長的橋,而且 midas 不會報錯**——正是 §9.1 一開始就在警告的那個 1000 倍問題。
> 例外:纜索 EXVAL 由使用者直接填目標單位的值,不換算。
>
> v3.5 之後這整個「例外」已經沒有意義——**現在什麼都不換算**,EXVAL 跟座標一樣照使用者輸入的原樣輸出。唯一還有意義的殘留問題是:EXVAL 若選的種類是 Lu(長度),它到底是不是使用者心裡認定的那個長度單位?這**已經不是程式要不要換算的問題,而是使用者自己要對到目前標題列選的長度單位去填**,見下方 EXVAL 種類欄位的說明。

> 副作用必須在 UI 明示:匯入含 `*UNIT` 的 MCT **會改變模型當前的作用單位系統**(不會毀損資料,midas 會換算,但使用者的工作單位會被改掉)。選擇輸出 `*UNIT` 是因為「幾何被正確解讀」的優先度高於「不動使用者設定」——不輸出的話單位選錯就是 1000 倍且不報錯。

### 9.1.1 已實作的格式(纜索類 iSUB/EXVAL/EXVAL2 已依實機樣本逐一核對)

程式中所有格式定義集中在 `MCT_FMT` 與 `elemLine()` 兩處,要改只需要動這兩塊。

> **2026-08-24 使用者提供兩段實機 Export 樣本**,第二段涵蓋 Truss(允許有限壓力)/ Hook / Cable(Lu、Pretension、Horizontal 各一組),逐行拆解後把子類型代碼與 EXVAL2 的枚舉值都坐實了:
> ```
> *ELEMENT    ; Elements
> ; iEL, TYPE, iMAT, iPRO, iN1, iN2, ANGLE, iSUB,                     ; Frame  Element
> ; iEL, TYPE, iMAT, iPRO, iN1, iN2, ANGLE, iSUB, EXVAL, EXVAL2, bLMT ; Comp/Tens Truss
> ; iEL, TYPE, iMAT, iPRO, iN1, iN2, iN3, iN4, iSUB, iWID , LCAXIS    ; Planar Element
> ; iEL, TYPE, iMAT, iPRO, iN1, iN2, iN3, iN4, iN5, iN6, iN7, iN8     ; Solid  Element
>      1, TENSTR,    1,     1,     1,     2,     0,     1,     0,     0,    NO   ; Truss, Allow.Comp.: 0 kN
>      3, TENSTR,    1,     1,     4,     5,     0,     2,     0                ; Hook, 0 m
>      5, TENSTR,    1,     1,     7,     8,     0,     3,     5,     3         ; Cable, Lu: 5 m
>      7, TENSTR,    1,     1,    10,    11,     0,     3,   100,     1         ; Cable, Pretension: 100 kN
>      9, TENSTR,    1,     1,    13,    14,     0,     3,   100,     2         ; Cable, Horizontal: 100 kN
> ```
> **確認結果(纜索類 `iSUB`,子類型代碼):**
> | iSUB | 子類型 | 欄位長度 | EXVAL 的意義 | EXVAL2 | bLMT |
> |---|---|---|---|---|---|
> | 1 | Truss(允許有限壓力) | 11 欄 | 容許壓力(力單位) | 恆為 0 | 有,樣本見過 `NO` |
> | 2 | Hook | **9 欄**(到 EXVAL 為止,無 EXVAL2/bLMT) | 間隙距離(長度單位) | 無 | 無 |
> | 3 | Cable | 10 欄 | 依 EXVAL2 種類而定 | 三選一,見下表 | 無 |
> | 0 | 未知 | — | — | — | — |
>
> **確認結果(`iSUB=3` 時 `EXVAL2` 的種類碼)—— 跟使用者原先描述的敘述順序相反:**
> | EXVAL2 | 種類 | EXVAL 的單位 |
> |---|---|---|
> | 1 | Pretension(預拉力) | 力 |
> | 2 | Horizontal(水平分力) | 力 |
> | 3 | Lu(無應力長度) | 長度 |
>
> `iSUB=0` 未見樣本,`bLMT="YES"` 時的行為也未見樣本,兩者仍是待驗證項目。

```
;---------------------------------------------------------------------------
; Spatial Curve Generator  (spatial-curve-gen)
; Generated : 2026-08-25 08:23 +08:00
; Units     : KN, M   (coordinates exported as entered, no conversion)
; Groups    : 9
; Nodes     : 325    Elements : 346    Structure groups : 10
; Total elem length : 2994.726351
;
;   G_MAIN (CURVE)  nodes 1001-1053  elements 1001-1052
;   G_G02 (LINK2)  nodes -  elements 3001-3027
;
; NOTE: importing an MCT containing *UNIT changes the model's active unit system.
; NOTE: element beta angle is always 0.
; The parameter block below restores every input if this file is dropped back
; onto the generator page.
;---------------------------------------------------------------------------
; ==== CURVE-GEN-PARAMS-BEGIN ====
; { …完整參數 JSON,每行前綴 "; "… }
; ==== CURVE-GEN-PARAMS-END ====

*UNIT
; FORCE, LENGTH, HEAT, TEMPER
   KN, M, KJ, C

*NODE
; iNO, X, Y, Z
; --- G_MAIN (CURVE) ---
   1001, 0, 0, 100

*ELEMENT
; iEL, TYPE, iMAT, iPRO, iN1, iN2, ANGLE, iSUB,                     ; Frame  Element
; iEL, TYPE, iMAT, iPRO, iN1, iN2, ANGLE, iSUB, EXVAL, EXVAL2, bLMT ; Comp/Tens Truss
; iEL, TYPE, iMAT, iPRO, iN1, iN2, iN3, iN4, iSUB, iWID , LCAXIS    ; Planar Element
; iEL, TYPE, iMAT, iPRO, iN1, iN2, iN3, iN4, iN5, iN6, iN7, iN8     ; Solid  Element
; --- G_MAIN (CURVE) ---
   1001, BEAM, 1, 1, 1001, 1002, 0, 0
; --- G_XBEAM (LINK2) ---
   8001, BEAM, 1, 9, 2001, 1001, 0, 0
; --- G_STAY (FAN) ---
   9001, TENSTR, 2, 5, 9001, 1015, 0, 3, 0, 3

*GROUP
; NAME, NODE_LIST, ELEM_LIST, PLANE_TYPE
   G_MAIN, 1001to1011, 1001to1010, 0
   G_XBEAM, , 8001to8011, 0
   G_ALL, 1001to1053 2001to2053 9001, 1001to1052 2001to2052 8001to8011 9001to9024, 0
```

**實作決定:**

- 節點/元素**依組分段**,每段前加一行註解標示所屬(**用匯出的 ASCII 群組名 + 英文型別碼**),方便人工核對
- 群組清單用 **`AtoB` 連號壓縮**(連續 ≥3 個才壓,2 個直接列出比較短)
- 橫梁群組的 `NODE_LIST` 為**空**(節點已屬各線的群組)
- 數值用 `toFixed(6)` 後去除尾端多餘的零(`50.000000` → `50`)
- 纜索元素的**類型字串 / iSUB / EXVAL 種類 / EXVAL 在 UI 上可直接改**,預設 `TENSTR, iSUB=3(Cable), 種類=3(Lu), EXVAL=0`
- `*ELEMENT` 的欄位群依 `TYPE` 分岔:`BEAM`/`TRUSS` 走 Frame Element(到 `iSUB` 為止,8 欄);`TENSTR`/`COMPTR` 走 Comp/Tens Truss——本工具目前只會產生這兩類,不會產生 Planar / Solid 元素,但四種欄位群的註解仍照 midas 慣例整段輸出,方便人工比對
- **Comp/Tens Truss 的欄位長度依 `iSUB` 而變**,`elemLine()` 依此分岔:`iSUB=2`(Hook)只到 EXVAL(9 欄);`iSUB=3`(Cable)到 EXVAL2(10 欄);`iSUB=1`(Truss 允許有限壓力)多帶 `bLMT`(11 欄)。UI 上 EXVAL2 欄位只在 `iSUB=3` 時可編輯、`bLMT` 欄位只在 `iSUB=1` 時可編輯,其餘情形自動 disable(見 §9.1.1 的對照表)

### 9.1.2 待驗證清單(拿到實機 Export 後逐項比對)

| 項目 | 目前寫法 | 風險 |
|---|---|---|
| 註解字元 | `;` | 低 |
| `*ELEMENT` 欄位群與 Frame/Comp-Tens Truss 的分岔 | 依實機樣本對齊(見 §9.1.1) | **低 —— 已用實機樣本確認** |
| BEAM 尾欄位數 | 8 欄(到 `iSUB`),樣本的註解行有列出,但沒有實際 BEAM 資料列可比對 | 中 |
| **纜索類 `iSUB`(1/2/3)與各自欄位長度** | 1=Truss(11 欄)、2=Hook(9 欄)、3=Cable(10 欄) | **低 —— 已用實機樣本逐一核對(見 §9.1.1)** |
| `iSUB = 0` | 未使用,行為未知 | 中 —— 未見樣本 |
| **`iSUB=3` 時 `EXVAL2` 的種類碼** | 1=Pretension、2=Horizontal、3=Lu | **低 —— 已用實機樣本逐一核對**(注意與最初描述的敘述順序相反) |
| **`bLMT="YES"` 時的行為** | 樣本只出現過 `NO` | 中 —— `YES` 語意未見樣本 |
| `*GROUP` 欄位與空清單 | `NAME, NODE_LIST, ELEM_LIST, PLANE_TYPE`,空清單留白 | 中 |
| `AtoB` 清單語法 | `1001to1011` | 低 |
| `*UNIT` 的 HEAT/TEMPER | `KJ, C` | 低 |

> v3.5 把「輸入即輸出、全程不換算」定成唯一規則後,§9.1 原本「EXVAL 不換算」的例外已經沒有特殊地位——**現在所有數值都不換算**,EXVAL 只是恰好也遵守這條規則。使用者自己要留意:EXVAL2 選 Lu 時,EXVAL 這個數字要用目前標題列選定的長度單位去填,選 Pretension/Horizontal 時則要用目前選定的力單位去填,程式不會替你換算或提醒。

**驗證方法(尚未做的部分):** 在 midas Civil 手動建 BEAM 元素與 `iSUB=0` 的纜索元素各一根,連同 `*GROUP`/`*UNIT` 一起 Export,補上表剩下的空白。

**檔頭註解**:寫入產生時間、工具版本、單位、總長、節點/元素數,**並嵌入完整的參數 JSON**(見 §10)。

### 9.1.3 官方 MCT Quick Reference 對照(2026-08-30)

來源:midas Civil 870 線上手冊 *MCT File Quick Reference*
(`https://manual.midasuser.com/EN_Common/Civil/870/Start/14_Appendix/MCT_File_Quick_Reference.htm`)。
這是**手冊的欄位簡表**,不是實機 Export 樣本 —— 兩者的關係是「手冊給欄位名,樣本給實際寫法」,
底下的不一致就是這樣來的。

**與本工具直接相關的區塊(手冊原文欄位):**

| 區塊 | 手冊列出的欄位 |
|---|---|
| `*UNIT` | `FORCE, LENGTH` |
| `*NODE` | `iNO, X, Y, Z` |
| `*ELEMENT`(Frame) | `iEL, TYPE, iMAT, iPRO, iN1, iN2, ANGLE, iSUB, EXVAL` |
| `*ELEMENT`(Planar) | `iEL, TYPE, iMAT, iPRO, iN1, iN2, iN3, iN4, iSUB, iWID` |
| `*ELEMENT`(Solid) | `iEL, TYPE, iMAT, iPRO, iN1 … iN8` |
| `*MATERIAL` | `iMAT, TYPE, MNAME, SPHEAT, HEATCO, [DATA1]` |
| `*SECTION` | `iSEC, TYPE, SNAME, OFFSET, SHAPE, [DATA1], [DATA2]` |
| **`*GROUP`** | `NAME, NODE_LIST, ELEM_LIST` |
| **`*BNDR-GROUP`** | `NAME` |
| **`*LOAD-GROUP`** | `NAME` |
| `*TS-GROUP`(變斷面群組) | `NAME, ELEM_LIST, ZVAR, ZEXP, ZFROM, ZDIST, YVAR, YEXP, YFROM, YDIST` |
| `*CONSTRAINT` | `NODE_LIST, CONST(Dx, Dy, Dz, Rx, Ry, Rz), GROUP` |
| `*ELASTICLINK`(GEN) | `iNODE1, iNODE2, Link, ANGLE, SDx … SRz, DRy, DRz, GROUP` |
| `*LOCALAXIS`(角度式) | `NODE_LIST, iMETHOD, ANGLE-x, ANGLE-y, ANGLE-z` |
| `*ENDDATA` | 資料輸入結束 |

**這份手冊確認的三件事:**

1. **`*MATERIAL` / `*SECTION` 第 3 欄是名稱** —— 現行 `parseMctLib()` 的假設正確,不用改。
2. **`*BNDR-GROUP` 與 `*LOAD-GROUP` 每列就只有一個名稱。** 這表示「輸出空的具名邊界群組 / 荷載群組」
   是**零格式風險**的,不必等實機樣本(見 §16.7)。
3. ~~**`*STAGE` 不在這份手冊裡。**~~ —— **2026-08-30 更正:使用者在同一個網站上找到了**,
   是我第一次抓這頁時漏掉的。欄位定義如下(手冊原文),已據此實作,見 §16.10:

   ```
   ; NAME=NAME, DURATION, bSAVESTAGE, bSAVESTEP          ; line 1
   ; STEP=DAY1, DAY2, ...                                ; line 2
   ; AELEM=GROUP1, AGE1, GROUP2, AGE2, ...               ; line 3
   ; DELEM=GROUP1, REDIST1, GROUP2, REDIST2, ...         ; line 4
   ; ABNDR=BGROUP1, POS1, BGROUP2, POS2, ...             ; line 5
   ; DBNDR=BGROUP1, BGROUP2, ...                         ; line 6
   ; ALOAD=LGROUP1, DAY1, LGROUP2, DAY2, ...             ; line 7
   ; DLOAD=LGROUP1, DAY1, LGROUP2, DAY2, ...             ; line 8
   ```

   | 欄位 | 意義 |
   |---|---|
   | `NAME` / `DURATION` | 階段名稱 / 階段延時 |
   | `bSAVESTAGE` / `bSAVESTEP` | 結果要不要按階段 / 按 Step 存 |
   | `STEP` | 階段延時內要另外切出來當 Step 的經過天數 |
   | `AELEM` = `GROUP, AGE` | 啟用結構群組 + 該群組的**材齡** |
   | `DELEM` = `GROUP, REDIST` | 停用結構群組 + **內力重分配百分比**(停用元素承擔的內力分給相鄰元素的比例) |
   | `ABNDR` = `BGROUP, POS` | 啟用邊界群組;`POS` = `DEFORMED`(變形後位置)或 `ORIGINAL`(原始位置) |
   | `DBNDR` = `BGROUP` | 停用邊界群組 |
   | `ALOAD` / `DLOAD` = `LGROUP, DAY` | 啟用 / 停用荷載群組 + 天數 |

   手冊另外列了兩個階段相關的荷載指令(`*TIMELOAD`、`*CREEPCOEF`),本工具不輸出。

**三個與現行輸出不一致的地方(先不改,列入待驗證):**

| 項目 | 現行輸出 | 手冊 | 判斷 |
|---|---|---|---|
| `*UNIT` | `FORCE, LENGTH, HEAT, TEMPER`(4 欄) | `FORCE, LENGTH`(2 欄) | 保留 4 欄。手冊是簡表,而 MCT 的尾欄位一般可省;**多給欄位的風險低於缺欄位**。仍待實機樣本 |
| `*GROUP` | `NAME, NODE_LIST, ELEM_LIST, PLANE_TYPE`(4 欄) | `NAME, NODE_LIST, ELEM_LIST`(3 欄) | 同上,先保留 4 欄。**這一項最該優先拿樣本確認** —— 群組名是功能性欄位,壞掉就是真的壞掉 |
| `*ELEMENT` Frame | 到 `iSUB` 為止(8 欄) | 到 `EXVAL`(9 欄) | 保留 8 欄。實機樣本的註解行也是寫到 `iSUB,` 為止,與手冊不同;尾欄位可省 |

`*ENDDATA` 現行**沒有輸出**。手冊列為「資料輸入結束」,補上的成本是零,已納入 §16.7。

### 9.2 節點清單報表(CSV,可下載 / 可複製到 Excel)

每列一個節點:

| 節點編號 | 樁號 | X | Y | Z | 所屬線 | 所屬段落 | 當地半徑 | 切線方位角 | 該元素長度 | 弦線偏差 |
|---|---|---|---|---|---|---|---|---|---|---|

用途:抽查回核道路線形表、存入計算書、支承位置檢核。**這是實務上「可追溯性」的來源,也是 Excel 派願意換過來的主要理由之一。**

另提供元素清單 CSV(元素編號、類型、材料、斷面、i 節點、j 節點、長度、所屬群組)。

---

## 10. 存檔與載入

- 下載 **JSON**,含 `formatVersion` 欄位(日後格式演進要能升級或提示不相容)
- JSON 內容 = 所有輸入參數(平面段落表、VPI 表、分割設定、偏置線、纜索、編號、群組前綴、單位)
- **同一份 JSON 也嵌在 .mct 檔頭的註解中** → 該 .mct 檔本身就是參數存檔,拖回網頁即可還原重編
- 網頁需支援拖放 / 選擇檔案載入 `.json` 與 `.mct`(從註解解析)

---

## 11. UI

> **⚠ 本節是 v2 的分頁式介面,v3 起已作廢** —— 現在是**模型樹 + 屬性面板**(見 v3 各節),
> 兩張預覽圖也在 v3.6 合併成單一可旋轉的 3D 視圖。以下保留當初的分區規劃當紀錄。
>
> v3.7 補充的互動:組的 **⋯ / 右鍵選單**(原位複製 / 偏置複製 / 停用 / 刪除)、
> 每組的**手動節點與元素**區(元素靠在 3D 視圖上點兩個節點決定兩端)、
> **所有組的節點都可以點選**(能不能改座標見 §v3.7.4)。

單頁,分區:

**輸入區**

- 分頁 1:平面線形段落表(可新增 / 刪除 / 排序 / 複製列)
- 分頁 2:縱斷面 VPI 表
- 分頁 3:偏置線與橫梁
- 分頁 4:纜索
- 分頁 5:編號、群組前綴、單位、輸出設定

**預覽區(隨輸入即時重算重繪)**

- **平面預覽(俯視)**:工程繪圖風格,分段以顏色區分,標示起點、節點、反曲點;可切換顯示偏置線與橫梁
- **側視圖(剖面)**:縱軸高程;**橫軸有兩種來源**
  - **有線形** → 樁號,並顯示縱斷面線、VPI、BVC/EVC、偏置線
  - **沒有線形但有主纜** → 以第一組主纜的**主軸投影距離**為橫軸(標準立面圖),單位標示為 m 而非樁號
  - 兩者皆無 → 側視圖留空
- 兩圖共用游標樁號連動(滑過平面圖時側視圖標出對應位置)

**檢核面板(常駐)**

- §4.4 的所有數值
- 錯誤(紅,禁止輸出)與警告(黃,可輸出)清單

**輸出區**

- 下載 .mct / 複製 MCT 文字 / 下載節點 CSV / 下載元素 CSV / 下載 JSON

**平台:** 網頁(單檔 HTML 亦可),純前端,不需後端。

---

## 12. 限制(誠實版,寫進說明頁)

1. **節點之間為直線元素**,模型幾何是內接於真實曲線的折線。弦線偏差 `m ≈ L²/(8R)`,折線總長短於設計弧長(誤差 `≈ θ²/24`)。工具會即時顯示這兩個數值,但**分割密度的最終判斷仍在使用者**。
2. **緩和曲線採數值積分近似 clothoid**(Simpson,步長 ≤ 0.1 m),非解析解;精度足以滿足一般結構建模需求。
3. **輸出元素之 beta angle 一律為 0(斷面正置)。** 斷面需傾轉(I 型梁強弱軸朝向、墩柱斷面朝向、超高)須於 midas 中框選群組指定 —— **這是刻意的決定,不是缺口**,見 §16.1。
4. **不適用於幾何變更後的模型更新。** 節點編號重複時 midas 會覆蓋座標,但元素數量改變後,既有的支承、邊界、荷載、斷面指定、施工階段會錯位。**本工具定位為建模階段的幾何生成;幾何變更後模型須重建**(群組功能可加速重建)。
5. **僅產生單線(spine)幾何。** 梁格 (grillage) 或殼元素模型須自行擴充。另提醒:**曲線箱梁的扭轉效應顯著**,單梁模型須正確設定扭轉常數。
6. 產生的元素僅含基本連接資訊(類型、材料、斷面、端點、群組);邊界條件、荷載、施工階段仍須於 midas 內設定。
7. 匯入格式以標準 `.mct` 文字命令為準;若需 `.cst` 副檔名,存檔後更名即可。
8. **`*UNIT` 會改變模型當前作用單位系統**(見 §9.1)。
9. **控制點 / 樣條曲線模式本版不提供。** Catmull-Rom 僅 C¹ 連續,曲率不連續會使彎矩圖在段落交界跳動;且其起點切線由控制點自身決定,與「切線自動繼承」規則衝突,不適合作為橋梁設計線形。
10. **「偏置複製」是剛體平移,不是平行曲線**(v3.7)。對直線沒有差別,但對圓弧 / 緩和曲線,複本的半徑與原曲線**相同** —— 需要真正的平行線(當地半徑 `R − d`)請用舊的偏置組。
11. **匯出的 `*GROUP` 群組名只能是英數底線**(v3.7)。組名若全是中文,匯出時會**退回 `G01` 這種序號**;想在 midas 裡看到有意義的群組名,請在組名裡帶上英數。
12. **手動元素只記錄兩端節點編號。** 若之後改動幾何導致節點編號重排,手動元素可能連到非預期的節點或找不到端點(會報錯不會當掉);大幅改動幾何後請檢查手動元素。
13. **重合的節點預設會被合併**(v3.8,見 §v3.8.1.1)。同一位置只留編號最小的,其餘刪掉、元素端點改指到它,因此**節點編號會跳號**。可在「編號總表」關掉;關掉時重合節點會保留並列為警告。
14. **材料 / 斷面名稱只是顯示用**(v3.8)。匯入的名稱不會寫進輸出的 `.mct`(輸出的仍然是編號),也不會驗證該編號在目標模型裡真的存在 —— 換一個模型匯入時要自己確認編號對得上。
15. **編號全自動,不能逐組指定**(v3.9)。只能調全域起始號與號段增量;舊檔用過手動編號的話,載入後編號會改變。

---

## 13. 驗收測試案例

實作完成後必須通過以下手算可驗證的案例。

### 案例 A:平面線形

起點 `(0, 0)`,起始樁號 `0k+000.000`,起始方位角 `90°`(正東)

| # | 型別 | 參數 |
|---|---|---|
| 1 | 直線 | L = 50 |
| 2 | 圓弧 | R = 100,右偏,Δ = 90°(弧長 157.0796) |
| 3 | 直線 | L = 50 |

**預期結果:**

- 段 1 終點 `(50.000, 0.000)`,樁號 `0k+050.000`,方位角 `90°`
- 段 2 圓心 `(50.000, −100.000)`
- 段 2 終點 `(150.000, −100.000)`,樁號 `0k+207.080`,方位角 `180°`
- 段 3 終點 `(150.000, −150.000)`,樁號 `0k+257.080`,方位角 `180°`
- 總長 `257.0796`

### 案例 B:縱斷面(對稱豎曲線)

| VPI | 樁號 | Z | L |
|---|---|---|---|
| 1 | 0k+000.000 | 100.0000 | — |
| 2 | 0k+128.540 | 105.1416 | 100 |
| 3 | 0k+257.080 | 100.0000 | — |

**預期結果:**

- `g_in = +4.000%`,`g_out = −4.000%`
- `BVC = 0k+078.540`,`EVC = 0k+178.540`
- 中央縱距 `E = (−0.08)·100/8 = −1.000 m`
- **VPI 樁號處的實際高程 `Z = 104.1416`**(比 VPI 低 1.000 m)
- `K = 100 / 8 = 12.5`

**連續性(必須一併驗證,否則公式寫錯不會被發現):**

- `Z(BVC) = 103.1416`、`Z(EVC) = 103.1416`(皆等於切線值,本例因對稱而相等)
- `坡度(BVC) = +4.000%`、`坡度(EVC) = −4.000%`、**`坡度(VPI 樁號) = 0`**(對稱曲線的頂點)
- BVC 之前與 EVC 之後皆為直線坡度

**縱斷面錯誤檢核:**

- VPI 100(L=100)+ VPI 180(L=100) → `EVC₁ = 150 > BVC₂ = 130` → **錯誤**
- L = 0 且坡度變化 ≠ 0 → **折角警告**

### 案例 B-2:3D 元素長度 vs 平面樁號

定坡 4%(`0k+000 z=100` → `0k+257.080 z=110.2832`),直線段 50 m、間距 5 m:

- 每根元素的 3D 長度 = `5 × √(1 + 0.04²) = 5.003998 m`
- 3D 總長 / 平面折線長 = `√(1.0016) = 1.00079968`
- 50 m 平面 → 3D 多出 **39.98 mm**
- 平坡時 3D 總長必須**恰等於**平面折線長

### 案例 C:分割與檢核

案例 A 的段 2(R = 100,弧長 157.0796),間距模式 `d = 5 m`,餘數政策「獨立留一段」:

- 完整段數 `31`,餘數 `2.07963 m` → 觸發餘數對話框;`2.0796 / 5 = 41.6% > 30%`,**不觸發短元素警告**
- 段 2 元素數 `32`(31 + 餘數)
- **最大弦線偏差(解析解)** `m = R(1 − cos(θ/2)) = 100(1 − cos 0.025) = 31.2484 mm`
  - 對照 `L²/(8R)` 近似式 = `31.25 mm`,兩者差 1.6 μm,近似式可用於面板顯示
- **本段總長縮短 = 16.183 mm**
  - 精確值 `31·(5 − 200 sin 0.025) + (2.07963 − 200 sin 0.0103982)`
  - 用 `θ²/24 × 全弧長` 的近似式會得到 16.36 mm(偏大約 1%),因為餘數元素的 θ 較小、縮短率也較小
- 直線段不縮短,故全線縮短量 = 段 2 縮短量

**其他餘數政策的預期結果(同一段落):**

| 政策 | 段 2 元素數 | 說明 |
|---|---|---|
| 均分吸收 | 31 | 每個元素弧長 = 157.0796 / 31 = 5.06709 m(間距略為放大) |
| 獨立留一段 | 32 | 末元素弧長 2.07963 m |
| 併入前一元素 | 31 | 末元素弧長 = 5 + 2.07963 = 7.07963 m,幾何不變 |
| 截短曲線 | 31 | 節點停在 0k+205.000,**終點後退 2.07963 m**;僅線形鏈末段可用 |

**強制節點(容差 0.1 m):**

- 強制 `180.02` → 均分節點 `180.000` 被吸收,節點總數不變
- 強制 `180.5` → 距離 0.5 m > 容差,兩個節點都保留
- 強制 `50.05` → 段落交界 `0k+050.000` **受保護不被吸收**,改由交界取代該強制節點並發出警告
- 強制樁號落在線形範圍外 → 警告並忽略

**弦線偏差容差模式:** R = 100、容差 20 mm → 容許弦長 `√(8×100×0.02) = 4.000 m`,產生的所有元素偏差必須 ≤ 20 mm

### 案例 D:偏置線

案例 A 加一條 `d = +6.0`(右)、`h = 0` 的偏置線:

- 段 1 上,偏置線與主線平行,間距 6.0(起點方位角 90°,右 = −Y,故偏置點在 `y − 6`)
- 段 2 偏置線終點 `= (150 + 6·sin(270°), −100 + 6·cos(270°)) = (144.000, −100.000)`
- **段 2 偏置線當地半徑 = `100 − 6 = 94`**
  - 驗算:圓心 `(50, −100)`,到偏置終點 `(144, −100)` 的距離 = `94` ✅
  - 🔧 **v2 初稿在此寫成 106 是錯的。** 右偏曲線的圓心在右,右側偏置在**內側**,半徑要**減**。
- 內側偏置線的元素長度 = 主線 × `94/100`
- 橫向對齊驗證:同索引節點的樁號相同,且兩點連線與主線切線**正交**、距離恆為 `|d|`

**幾何退化:**

- `d = +106`(右 = 內側,越過圓心)→ **必須報錯**
- `d = +100`(恰好落在圓心)→ **必須報錯**
- `d = −106`(左 = 外側,`R_local = 206`)→ **合法,不可報錯**

**垂直偏置:** `h` 直接疊加在該樁號的縱斷面高程上。

### 案例 D-2:橫梁與編號

主線 + `d = −4` + `d = +4` 三條線,橫梁每個樁號都放:

- 橫梁元素數 = `樁號數 × (線數 − 1)`
- 串接順序由左至右:`左(−4) → 主線(0) → 右(+4)`
- 同樁號兩根橫梁的跨距和 = `8.000 m`
- 「每 3 個樁號放一根」→ 元素數 = `⌈樁號數/3⌉ × (線數 − 1)`

**編號(§7,增量 1000):** 主線節點 `1001+`、左偏置 `2001+`、右偏置 `3001+`、橫梁元素 `8001+`;所有節點編號與元素編號皆不得重複。節點數超過號段增量 → **錯誤**。

**群組(§8):** `GEN_LINE_01`、`GEN_ARC_02`、`GEN_LINE_03`、`GEN_OFF_L`、`GEN_OFF_R`、`GEN_XBEAM`、`GEN_ALL`(共 7 組)。

### 案例 F:纜索(§6)

測試線形:直線 `0k+000 → 0k+100`,每 10 m 一節點 → 節點 `1001..1011`,`z = 100`。塔頂 `(50, 0, 140)`。

**N = 1(斜張索):**

- 目標 `1001-1011` → **11 條索、11 根元素**
- **只新增 1 個節點**(塔頂,編號 `9001`),元素起始 `9001`
- 端點索長度 = `√(50² + 40²) = 64.031 m`;塔正下方那條 = `40.000 m`
- 元素類型 = `CABLE`,群組 = `GEN_CABLE_<名稱>`

**N = 4、f = 2 m(懸索橋主纜):**

- 4 根元素、新增 `1 + 3` 個節點
- **跨中節點 `Z` = 線性中點 − f**(`u = 0.5` 時 `4f·u(1−u) = f`)
- `u = 0.25` 處垂度 = `4f·u(1−u) = 1.5 m`
- 跨中節點的 X/Y 仍在弦線上(垂度只降 Z)
- 有垂度時**纜索總長 > 弦長**

**垂跨比模式:** `f = 垂跨比 × 弦長`。

**警告與錯誤:**

| 情形 | 反應 |
|---|---|
| `N > 1` 且 `f = 0` | **警告**:節點共線,幾何非線性分析橫向勁度為零,極可能不收斂 |
| 目標節點不存在 | 警告,略過該條索,其餘照常產生 |
| 起點節點不存在 | **錯誤**,整組不產生 |
| 目標與起點重合 | 警告,略過 |

**編號:** 每組纜索各佔一號段 —— 組 1 `9001+`、組 2 `10001+`(+ 增量);與主線/偏置線/橫梁的編號皆不得衝突。

### 案例 M:主纜與空間轉換(§6.4 / §6.5)

**剛體轉換**(`P1 = (10,20,30) → Q1 = (1000,−500,80)`,`v = +X`,`u = +Y`):

- 基準點 `P1` **精確**落在 `Q1`(三軸皆 < 1e-9)
- 對照組:用 `T = Q1 − P1` 會讓基準點偏離 **> 1 m** ← 故不採用
- 任兩點距離不變(剛體)
- `v` 轉後方向 = `u`
- `v` 與 `u` 反向(180°)時不得出現 NaN
- 方位角模式:局部 `+Y`、`θ = 90°` → 全域 `+X`,且 **Z 分量完全不變**

**局部幾何**(`Lx = 400`、`f = 40`、`N = 40`):

| 檢查 | 拋物線 | 懸鏈線 |
|---|---|---|
| 兩端鞍座 z | 0 | 0 |
| 跨中 z | −40 | −40 |
| 1/4 點 z | −30.0000 | **−30.0968**(解析解) |
| 節點 / 元素數 | 41 / 40 | 41 / 40 |

不等高鞍座(`z1 = 0`、`z2 = 20`):右端 `z = 20`,跨中 `= 弦線中點 10 − f 40 = −30`。

**索面、吊索、編號**(雙索面 `gap = 20`、`deckZ = −45`、每 4 個節點一根):

- 主纜節點 = `2 × (N+1) = 82`
- 索面橫向間距 = `20.000`
- 吊索數 = `2 × 9 = 18`(不含兩端鞍座)
- 跨中吊索長 = `45 − 40 = 5.000`
- 節點起始 `20001`、元素起始 `20001`,皆不重複
- 群組 `GEN_MAIN_*` 與 `GEN_HANGER_*` 各一

**殘差:** `|v| = 400`、`|u| = 300` → 殘差 `100.000 m` 且發出警告;`|u| = |v|` 時殘差 `0`。

**主纜低於橋面時** → 該吊索略過並警告(避免產生倒吊的元素)。

### 案例 E:參數存檔往返(§10)

- 參數 JSON 下載 → 載入 → **模型完全一致**(節點數、元素數、偏置線、橫梁、群組前綴、VPI 數)
- 同一份 JSON 嵌入 `.mct` 檔頭註解 → 把該 `.mct` 拖回頁面 → **同樣完全還原**
- 沒有參數區塊的檔案 → `extractParams` 回傳 `null`,並提示使用者
- CSV 需正確跳脫逗號與雙引號,並含 UTF-8 BOM(Windows Excel 直接開啟不亂碼)

---

## 14. 建議開發順序

| 階段 | 內容 | 狀態 |
|---|---|---|
| **0** | **MCT 格式驗證 spike**(§9.1.2)— 與實機 Export 逐欄比對 | 待辦(已先依標準格式實作,待比對後修正)|
| 1 | 平面線形核心(三種段落 + 切線繼承 + 樁號)+ 平面預覽 + 案例 A | ✅ 完成 |
| 2 | 分割規則(四模式 + 強制節點 + 餘數對話框)+ 檢核面板 + 案例 C | ✅ 完成 |
| 3 | 縱斷面 VPI + 豎曲線 + 側視圖 + 案例 B | ✅ 完成 |
| 4 | **.mct 文字輸出**(節點/元素/群組的序列化)| 完成(格式待驗證)|
| 5 | 節點/元素 CSV 報表 | ✅ 完成 |
| 6 | JSON 存檔 / 載入(含從 .mct 註解還原) | ✅ 完成 |
| 7 | 偏置線 + 橫梁 + 案例 D | ✅ 完成 |
| 8 | 纜索 | 完成 |
| 9 | **主纜模組(懸索橋)+ 3D 剛體轉換** — §6.4 / §6.5、案例 M | 完成(格式待驗證)|

> **階段順序的調整:** §7 編號管理、§8 群組、以及元素屬性(類型/材料/斷面)原本排在階段 4,實作時提前到階段 5/7 —— CSV 報表需要節點編號,偏置線需要號段配置,兩者都與 MCT 文字格式無關。**階段 4 現在只剩「把已經算好的模型序列化成 MCT 文字」這一件事。**

> **階段順序的調整:** §7 編號管理、§8 群組、以及元素屬性(類型/材料/斷面)原本排在階段 4,實作時提前到 5/7 —— 因為 CSV 報表需要節點編號,偏置線需要號段配置,兩者都與 MCT 文字格式無關。**階段 4 現在只剩下「把已經算好的模型序列化成 MCT 文字」這一件事。**

---

## 15. 仍待決定 / 未解事項

- **§10 存檔格式**未經確認,目前採建議值(JSON + 嵌入 .mct 註解)。
- ~~**超高與 beta angle** 是已知最大缺口~~ —— **2026-08-30 決定不做**,見 §16.1。`ANGLE` 是斷面繞元素局部 x 軸的旋轉角,不是超高專用欄位;既然群組已經輸出,到 midas 裡框選群組改的速度差不多,而且在 midas 裡看得到斷面實際角度。
- **與上游線形資料的介接**(線形要素表 IP 交點法貼上 / LandXML)未納入本版,但 §1 的樁號主鍵架構已為它預留空間 —— 已排入 §16.6 與 §16.11。
- **MCT 格式仍有未見樣本的欄位**:`iSUB = 0`、`bLMT = "YES"` 的行為、BEAM 的實際資料列、`*GROUP` 與 `*UNIT` 的實機格式,以及 `*STAGE` 的 `bSAVESTAGE` / `bSAVESTEP` 枚舉值與「省略整行」的行為,見 §9.1.2、§9.1.3 與 §16.10。~~整個 `*STAGE`~~ 的欄位定義已由官方手冊坐實(2026-08-30)。
- ~~**纜索初始值目前是全模型一組**~~ —— **v3.8 已解決**,下放到組層級 `g.cable`(見 §v3.8.6)。仍未解的是「同一組裡各索不同」(例如斜張索每根張力都不一樣),要再下放到元素層級。
- ~~**段之間的節點合併**~~ —— **v3.8 已用「合併重合節點」解決**(§v3.8.1.1)。仍然是「先產生再合併」而不是原生的圖結構,所以段卡片上的節點數是合併**前**的數字,實際輸出以「編號總表」為準。
- **材料 / 斷面名稱庫不寫進輸出**,也不檢查編號在目標模型是否存在(§12.14)。
- **舊的偏置組要不要真的移除**(v3.7 只是不給新增)。移除前要先確認使用者手上沒有還在用它的檔案,或提供轉換。
- **手動元素的端點用節點編號記錄**,幾何大改後可能失效(§12.12),之後或許該改成記「組 + 序號」之類更穩的參照。

---

# 16. 規劃(2026-08-30 定案)

這一章是「midas Civil 建模流程」方向的功能規劃,經一輪來回討論收斂而成。
**宗旨:讓 midas Civil 的建模流程快速順暢,讓 midas Civil 回歸結構分析。**

## 16.0 界線(先定,免得工具膨脹成第二個前處理器)

> **凡是由線形／幾何唯一決定、且在 midas 裡只能逐個手動點的,工具做;
> 凡是需要工程判斷或屬於分析設定的,留在 midas。**

用這條檢驗:階段群組的劃分、樁號對應的斷面編號、DXF 疊圖 —— 通過;
支承釋放方向、掛籃荷載、收縮潛變參數、載重組合 —— 不通過。
施工階段的**群組骨架**通過,階段的**參數**不通過(只給欄位,不猜預設值)。

## 16.1 已決定**不做**的項目(連同理由,不要再重新發明)

| 項目 | 不做的理由 |
|---|---|
| **beta angle(斷面旋轉角)輸入模式** | `*ELEMENT` 的 `ANGLE` 是斷面繞元素局部 x 軸的旋轉角(I 型梁強弱軸朝向、墩柱斷面朝向),不是超高專用欄位。**既然群組已經輸出了,到 midas 裡框選群組改的速度差不多,而且在 midas 裡看得到斷面實際角度。** 工具端多一套「強軸朝向」的輸入方式反而讓操作變複雜。維持 `ANGLE` 恆為 0 |
| **支承 / 邊界條件輸出(`*CONSTRAINT` 等)** | midas 的操作已經夠簡單,而且量級是「每個支承填一次」不是「每根元素填一次」,不值得 |
| **支承局部軸角度(`*LOCALAXIS` / `*ELASTICLINK` 的 `ANGLE`)** | 同上。註:曲線橋支承常沿橋軸切線擺放,局部軸要轉角度,工具手上有那個方位角 —— 但這是少數幾個點的事,先不做 |
| **工具端輸入完整斷面(輸出 `*SECTION` / `*MATERIAL`)** | midas 有內建斷面資料庫,在那邊建比較方便。**工具端只需要編號**,使用者之後依需求在 midas 依序建材料與斷面即可。連帶要求:斷面 / 材料欄位在沒有名稱庫時仍必須能直接打數字,不可強迫先匯入 mct |
| **建模精靈 / 橋型模板** | midas Civil 已有內建 Wizard,往人家強項上撞。本工具的差異化正好在**自定義程度高**。範例維持現在的定位(展示能做出什麼、方便到什麼程度) |
| **推進工法(ILM)** | 每階段是整體位移,不是群組啟用,不在階段群組的架構裡 |
| **直接介接 API** | 公司用的是 midas Civil,不是 Civil NX。**MCT 檔就是唯一介面** |
| **自動反推匯入模型的參數** | 把一串節點擬合成圓弧、認出緩和曲線、判斷分段點,全是猜測。猜錯比不猜更糟 —— 使用者會以為那是原始線形,其實是擬合值 |

## 16.2 優先序

前六項**都不需要新的 MCT 格式樣本**,可以直接動工;只有 §16.8 卡在樣本上。

| 順位 | 項目 | 章節 | 格式風險 | 狀態 |
|---|---|---|---|---|
| 1 | 編號衝突掃描 | §16.3 | 無 | **v3.34 完成** |
| 2 | 反向匯入 + 匯入群組族 | §16.4 | 無 | **v3.34 完成** |
| 3 | 階段群組切分器 | §16.5 層一 | 無 | **v3.35 完成** |
| 3b | 階段表 + 時間軸滑桿 | §16.5 層二 | 無 | **v3.38 完成** |
| 4 | 線形要素表 IP 交點法 + 整表貼上 | §16.6 | 無 | **v3.37 完成** |
| 5 | 樁號 → 斷面編號表 | §16.7 | 無 | **v3.36 完成** |
| 6 | DXF 輸出 | §16.8 | 無(自成格式)| **v3.36 完成** |
| 7 | `*ENDDATA` | §16.9 | 無 | **v3.35 完成**(`*BNDR-GROUP` / `*LOAD-GROUP` v3.41 移除,見 §16.13)|
| 8 | `*STAGE` 施工階段輸出 | §16.10 | 中(手冊有欄位,枚舉值未驗證)| **v3.39 完成** |
| 9 | LandXML 匯入 | §16.11 | 低(優先度低)| 未做 |
| 10 | 三個範例的施工階段 | §16.12 | 無 | **v3.40 完成** |
| 11 | 施工階段的使用者回饋 | §16.13 | 無 | **v3.41 完成** |
| 12 | 介面回饋(線寬、面板開關、左欄收合、圖上 hover 連動、工具列圖示)| §16.14 | 無 | **v3.42 完成** |
| 13 | 未選取 / 未啟用統一成同一種淡灰 | §16.14.1 | 無 | **v3.43 完成** |

## 16.3 編號衝突掃描

**問題。** 匯出的節點從 1001 之類的號碼開始編。使用者手上已有一個 midas 模型,節點用到 1–2400、
元素用到 1–2650。要加一組新構件時匯入這份 .mct —— **midas 遇到已存在的編號是直接覆蓋,不會問**。
原模型節點 1001 的座標被改掉,掛在它上面的支承、荷載、施工階段全部無聲錯位。
這就是 §12.4「不適用於更新既有模型」的實際成因:**它不是原理性的限制,是編號管理沒做。**

**做法。** 匯入 .mct 時,除了現在讀的 `*MATERIAL` / `*SECTION`,再掃三件事,**只記號碼不記內容**:

- `*NODE` 第 1 欄 → 已用節點號集合
- `*ELEMENT` 第 1 欄 → 已用元素號集合
- `*GROUP` 第 1 欄 → 已用群組名集合

存進 `state.target = { nodes:[…], elems:[…], names:[…], src:"檔名", at:"時間" }`,
顯示在編號設定面板:`目標模型:節點 1–2400、元素 1–2650、群組 12 個`。

**三件自動化:**

1. **一顆「跳到安全號段」按鈕** —— 把節點 / 元素起始號推到已用最大號之上並向上取整到號段增量的倍數
   (例如 2650 → 3000),留一段緩衝,不緊貼 2401
2. **匯出前檢核多一條**:「節點 1001–1053 與目標模型衝突,匯入後會覆蓋」,
   與弦線偏差、編號衝突並列在既有的檢核面板
3. **群組名撞名一起檢**(同名在 midas 裡是合併還是覆蓋未經確認,先警告總是對的)

**成本。** `parseMctLib()` 已經是逐行掃區塊的狀態機,多兩個 case 而已。與 §16.4 共用同一段解析程式碼。

## 16.4 反向匯入 + 「匯入」群組族

讓已經在 midas 建過的專案能還原到本工具上。`*GROUP` 的節點 / 元素清單是明文的,原樣讀回沒有困難。

**新增一個群組族「匯入」(`IMPORT`)** —— 有節點與元素清單、**沒有參數**:

| | |
|---|---|
| **能做** | 顯示、選取、設顏色、啟用 / 停用、刪除 |
| | **被別的群組參照**(「某群組的第 k 個節點」)← 最大價值:新構件可以直接接到舊模型的節點上 |
| | 整組平移 / 旋轉 / 鏡射 / 位移複製 / 沿線陣列(只動座標,不需要參數) |
| **不能做** | 改半徑、重新分割、改分割方式 —— 沒有母體參數 |

**實作要點:**

- 匯入時把 `*NODE` 全部收進來,依 `*GROUP` 的 `NODE_LIST` / `ELEM_LIST` 分派到各群組
- `*GROUP` 沒收到的元素要有歸屬,丟進一個 `_UNGROUPED` 群組,不然會有一堆看不見的東西
- 節點編號**原樣保留**(這是它的價值所在:編號要對得回原模型),
  所以 `IMPORT` 群組不吃全域編號規則,`buildOneRaw()` 要為它開一條旁路
- 元素的材料 / 斷面 / 類型照讀,顯示用
- 清單語法要支援 midas 的 `1001to1011` 與 `1001to1051by2`
- 輸出時原樣輸出(保留原編號),或按新號段重編(和 §16.3 的安全號段連動)

## 16.5 施工階段:分三層,前兩層零格式風險

**最有價值的部分不是輸出 `*STAGE`,是「階段群組的自動切分」,而那個輸出的還是 `*GROUP`。**

### 層一:階段群組切分器

懸臂工法的建模痛點:主梁要切成幾十個節塊群組,每個 2–4 根元素,以墩心為中心左右對稱、由內往外編號。
在 midas 裡這是「框選 → 新增群組 → 命名」重複六十次,而且框錯不會有人告訴你。
工具知道元素沿線形的順序,可以一次生出來。

在線群組上加一個動作 **「切分成施工群組」**,規則四選一:

| 規則 | 輸入 | 適用 |
|---|---|---|
| 每 n 根元素一組 | n | 等節塊 |
| 依節塊長度表 | `2@3 8@4 4@4.5`(沿用既有間距表語法) | 變節塊長 |
| **對稱懸臂** | 指定墩心節點(可在 3D 視圖點選),往兩側成對展開 | T 構懸臂 |
| 依樁號分界 | 一串樁號 | 合龍段、支撐架分界 |

- 命名樣板 `{前綴}{序號:02}{L/R}`,產生前先列清單預覽
- **原本的整組群組保留** —— midas 裡「全橋」與「逐節塊」兩種選取都在,`*GROUP` 只是多幾十行

### 層二:階段表 + 時間軸滑桿(獨立有價值)

一張表,一列一個階段:`階段名稱 | 天數 | 啟用結構群組 | 啟用邊界群組 | 啟用荷載群組 | 停用項`

- 從層一的群組清單**自動排出預設順序**(墩 → 節塊由內而外 → 合龍 → 二次載重),再拖拉調整
- 3D 視圖底下加一條**時間軸滑桿**:拖到第 k 階段,只把該階段(含之前)啟用的元素畫成彩色,
  之後的畫灰或隱藏

> 這條滑桿**即使完全不輸出 `*STAGE` 也成立** —— 它是階段群組劃分的驗證工具。
> 切錯一格、左右不對稱、漏一個節塊,拖一次滑桿就看見,不必等 midas 跑完階段分析才發現。

**v3.38 實作細節(已完成):**

- 標題列多一顆 **▤ 施工階段** 全域按鈕。
- 「自動排一份順序」照群組清單的順序排:有切階段群組的逐節塊排開,沒有的整組一個階段。
- 階段的「啟用的結構群組」在 UI 上是一行逗號分隔的群組名,`state` 裡是陣列
  (`applyField` 在 `^stages\.\d+\.on$` 這個 key 上做轉換)。填了不存在的名字會標出來。
- **啟用是累積的**:第 k 階段顯示第 1 到 k 階段的總和;已啟用元素的兩端節點一定跟著亮,
  否則線會浮在空中。
- 繪圖端只加三個切點:`elemPolysBy()` 遇到未啟用的元素就斷開折線、連接族的元素迴圈跳過、
  節點改用 `visNodes(r)`。未啟用的部分先畫成一層很淡的灰,所以
  **「整座橋長什麼樣」和「現在蓋到哪」同時看得到**。
- `VIEWSTAGE` 只是視圖狀態,不進 `state`、不進復原堆疊。
- **已知的坑:對稱懸臂的「墩心是第幾個節點」會被「合併重合節點」影響** ——
  墩頂與主梁交會處的重合節點被併掉之後,該群組的節點序號會跳,
  照圖數出來的第 k 個不等於這裡的第 k 個。面板上因此**顯示墩心實際落在哪個節點與座標**讓人對照。

### 層三:輸出 `*STAGE`(v3.39 完成,見 §16.10)

## 16.6 線形要素表:IP 交點法 + 整表貼上

現在線形鏈只能一列一列按 `+ 直線 / + 圓弧 / + 緩和曲線`,參數是**元素法**(L / R·Δ / R0·R1·Ls)。
但實務上拿到的是**平面線形要素表**,用的是**交點法**:IP 樁號或座標、R、A1/A2(或 Ls1/Ls2)、偏向。

- 線形鏈多一個輸入模式:**交點法**。給起點座標 + 一串 IP(座標)+ 每個 IP 的 R、A1、A2 + 終點座標,
  工具算出切線長、緩和曲線、圓弧,展開成現有的子段結構
- **整表貼上**:接受從 Excel 整塊貼過來,自動跳過標題列,分隔符比照 `parseXYZTable()`
  (tab / 空白 / 逗號 / 分號,含全形)
- 兩種模式可互轉(交點法算完就是元素法的子段串),但**保留原始輸入**以便回頭核對

**v3.37 實作細節(已完成):**

- **偏向與起始方位角由座標自動判斷**,不必自己填。偏角 `Δ = norm180(azOut − azIn)`,正 = 右偏。
- **切線長用數值積分算,不用級數近似。** `clothoidPK(R, Ls)` 把緩和曲線積出來,取
  `p`(內移量)、`k`(切線增距)、`th`(緩和曲線偏角)—— 和 `buildSegment` 實際畫出來的
  那條緩和曲線**完全一致**,不然切線長會和圖面對不起來。
- **非對稱(兩側緩和曲線不等長)也成立。** 圓心到切線 1 的距離是 `R+p1`、到切線 2 是 `R+p2`,
  由這兩個條件解出圓心再投影回兩條切線:

  ```
  Cx = ((R+p1)·cosΔ − (R+p2)) / sinΔ
  T1 = k1 − Cx
  T2 = k2 + Cx·cosΔ + (R+p1)·sinΔ
  ```

  對稱時(`p1=p2=p`、`k1=k2=k`)化簡成課本的 `T = k + (R+p)·tan(Δ/2)`。
- **A 值換算** `A² = R·Ls`,勾「用 A 值」時第 4、5 欄填 A,填 0 = 該側沒有緩和曲線。
- **算不出來就報錯,不靜靜畫出怪東西**:偏角容不下兩段緩和曲線(`Δ < θs1 + θs2`)、
  兩個交點太近放不下切線長、偏角接近 0 或 180°,三種情形各有各的訊息。
- 驗收測試以「線形是否**收在指定的終點座標上**」為主要判準(純圓、對稱緩和、非對稱緩和、
  兩個交點的 S 形各一組),這比核對單一公式強得多。

## 16.7 樁號 → 斷面編號表

元素屬性變成三層,**最細的贏**:整組預設 → 段覆寫 → **樁號區間表**。

- 表格:`起樁號 | 迄樁號 | 斷面編號 |(選)材料編號`
- 分界樁號可沿用既有「間距表」語法
- 輸出時逐元素寫 `iPRO`;元素歸屬看它的**中點樁號**落在哪個區間
- 變高箱梁若要用 midas 的變斷面群組,`*TS-GROUP` 的欄位已在 §9.1.3 記錄,但**本版不輸出**
  —— 先做「逐元素給不同斷面編號」這條,它不需要新區塊

## 16.8 DXF 輸出

**.dwg 做不到,.dxf 做得到。** DWG 是 Autodesk 控制的封閉二進位格式,純前端沒有可行路徑
(需要 ODA / RealDWG 那類原生函式庫)。DXF 是純文字,自己寫幾百行就有。
AutoCAD / BricsCAD / Civil 3D / Rhino 全都直接讀,使用者要 .dwg 在 CAD 裡另存即可。

- **格式選 R12 ASCII**:只需要 `TABLES`(圖層)+ `ENTITIES` + `EOF`,相容性最廣
- **每根元素一條 3D `LINE`,每個節點一個 `POINT`**;元素本來就是一根一根的,不必湊 POLYLINE
- **圖層 = 匯出用的 ASCII 群組名**,顏色映射到 ACI 索引色(群組顏色已經有了)。
  中文圖層名要 AC1015 以上並處理 `$DWGCODEPAGE`,不划算 —— 沿用 ASCII 群組名
- 選配:節點編號 / 元素編號各自放一個獨立圖層的 `TEXT`,預設關閉
- **定位:可追溯性的另一半。** CSV 是數字,DXF 是圖 —— 拿回 CAD 疊上道路平面圖核對、
  或給道路組確認,這件事現在是空白的

## 16.9 `*ENDDATA` 與空的邊界 / 荷載群組

手冊已確認格式(§9.1.3),零風險:

- 檔尾補 `*ENDDATA`
- `*BNDR-GROUP` / `*LOAD-GROUP` 每列一個名稱。工具**不產生內容,只產生空的具名群組**,
  讓 midas 端直接有格子可填。符合 §16.0 的界線:群組劃分是幾何,支承釋放與掛籃荷載是判斷

## 16.10 `*STAGE`(v3.39 完成)

欄位定義見 §9.1.3(手冊原文)。**輸出的內容:**

```
*STAGE
; NAME=NAME, DURATION, bSAVESTAGE, bSAVESTEP
; …(八行欄位註解照手冊整段輸出,方便人工比對)
   NAME=CS01, 30, YES, NO
     AELEM=G_PIER, 7
     ABNDR=BND_PIER, DEFORMED
   NAME=CS02, 14, YES, NO
     AELEM=G_MAIN_01L, 5
     ALOAD=LD_FT, 0
```

**實作決定:**

- **逐階段填只是噪音的四個值集中在一處設定**(施工階段面板的「每個階段共用的設定」):
  `bSAVESTAGE` / `bSAVESTEP` / `POS`(`DEFORMED` / `ORIGINAL`)/ `REDIST`(%)。
  寫進每一個階段。
- **沒有內容的行整行不輸出。** 只有 `AELEM` / `DELEM` / `ABNDR` / `ALOAD` 四行會產生;
  **`STEP=` / `DBNDR=` / `DLOAD=` 不輸出**(前者要逐階段給經過天數,後兩者在本工具的
  使用情境裡沒有來源)。
- `ALOAD` 的 `DAY` 一律寫 **0**(階段一開始就施加)。要別的天數請在 midas 裡改。
- 階段名稱與群組名一樣**清洗成英數底線**(整份 .mct 維持純 ASCII)。
- **指到不存在的群組照樣輸出,但一定出警告。** 目標模型裡本來就有的群組是合法的參照,
  工具沒資格判它死刑;但打錯字和「指到既有模型的群組」長得一模一樣,所以一定要講出來。
- **收縮潛變、材齡函數、載重組合留在 midas** —— 那是工程判斷,不是幾何(§16.0)。
- 整個 `*STAGE` 可以一鍵關掉(面板上的開關),階段表全空時也不輸出。

**仍未驗證(第一次匯進 midas 要先確認階段有沒有正確建起來):**

| 項目 | 目前寫法 | 風險 |
|---|---|---|
| `bSAVESTAGE` / `bSAVESTEP` 的字面值 | `YES` / `NO`(和元素的 `bLMT` 同一套) | 中 —— 手冊只給欄位名,沒給枚舉值 |
| 省略整行是否合法 | 沒內容就不輸出該行 | 中 |
| `NAME=` 行與其後幾行的縮排 | 3 空格 / 5 空格 | 低 —— MCT 對空白寬容 |

**仍然值得要一份實機樣本**,不過現在只剩 §9.1.2 那幾格(BEAM 實際資料列、`*GROUP` 到底
3 欄還是 4 欄、`*UNIT`、`iSUB=0`、`bLMT="YES"`)以及上表這三項。

## 16.11 LandXML 匯入(優先度低)

LandXML 的 `<Alignment><CoordGeom>` 是 Line / Curve / Spiral 的串接,
`<Profile><ProfAlign>` 是 VPI + 豎曲線 —— **和線形鏈的資料模型幾乎一對一對應**。

但目標使用者的定義是「沒有 Civil 3D 的結構工程師」,這種人拿到的通常是 PDF 或 Excel 的線形要素表;
拿得到 LandXML 的人多半也拿得到 Civil 3D。**§16.6 覆蓋九成的實際情境**,
LandXML 是「道路顧問願意給檔」時的加分項,不是必需。

---

## 16.12 三個範例的施工階段(v3.40)

### 前置:範例的群組改名

原本的範例群組全是中文名,`mctName()` 清洗後沒有英數可用,匯出群組名一律退回
`G_G01`…`G_G24` —— 階段表也就沒有東西可以指。所以**每個範例群組的名稱都補上一個英數代號**
(「邊樑左 EDGE_L」→ 匯出群組名 `G_EDGE_L`)。這同時也是 README 一直在講的那條建議的示範。

> 測試裡有十幾處用中文名找群組(`byN(SUS, "邊樑左")`、`byN(SPR, sd + "扶手")` …),
> 改成 `gByName()`:**名稱相等或以「中文名 + 空格」開頭**都算命中,所以那些查找不必逐一改。

### 一、人行吊橋(8 階段)

| # | 階段 | 天數 | 啟用 |
|---|---|---|---|
| CS01 | 塔架 | 60 | `G_TOWER_W` `G_TOWER_E`(材齡 28)|
| CS02 | 主纜 | 30 | `G_CABLE_L` `G_CABLE_R` |
| CS03 | **抗風索** | 10 | `G_WIND_L` `G_WIND_R` |
| CS04 | 吊索 | 15 | `G_HANG_L` `G_HANG_R` |
| CS05 | 縱橫梁 | 30 | `G_DECK` `G_EDGE_L` `G_EDGE_R` `G_XBEAM` |
| CS06 | 底部桁架 | 25 | 下弦 / 豎桿 / 斜撐 / 下弦橫梁 / 下弦斜撐 / 橫隔斜撐,共 10 組 |
| CS07 | 抗風支索 | 10 | `G_WSTAY_L` `G_WSTAY_R` |

**與使用者原本提的順序的唯一差別:抗風索從最後移到第 3 階段。** 理由:
抗風索兩端錨在岩壁、**完全不依附橋體**,不必等任何橋上的東西;而人行吊橋施工中最怕的就是
橋面的橫向擺動與顫振,抗風系統要在橋面上去之前就到位。等全部做完才裝,分析上會低估施工中的
橫向位移。真正必須等橋面的是**抗風支索**(它把桁架下弦拉到抗風索上),所以那一項留在最後。

**其餘順序照使用者說的,而且是對的**:塔 → 主纜 → 吊索 → 縱橫梁 → 底部桁架。
兩座塔可以同時施工,所以放同一階段(`AELEM` 本來就吃多個群組)。

**這個範例做不到、但實務上該做的兩件事**(受限於模型粒度,不是工具的限制):

1. **加勁梁應該逐節塊吊裝。** 一次全部啟用會低估施工中的主纜線形變化與吊索力。
   要做就把「邊樑左 / 右」打開**施工階段群組**切成節塊,再把 CS05 拆成好幾階段。
2. **空纜線形。** 本工具產生的是**成橋幾何**;懸索橋的施工階段分析要從無應力長度倒推空纜線形,
   那是 midas 的纜索初始平衡 / Suspension Bridge Wizard 的工作,分階段啟用不會自動幫你做。

另外主索是「引索 → 懸鏈線 → 引索」一個群組三段,**沒辦法只啟用其中的引索段**,
所以錨碇段與主纜在同一階段。要分開就得把主索拆成三個群組。

### 二、公路橋梁(4 階段)

| # | 階段 | 天數 | 啟用 |
|---|---|---|---|
| CS01 | 下部結構 | 90 | `G_COL_L` `G_COL_R` `G_TIE`(材齡 28)|
| CS02 | 墩頂橫梁 | 20 | `G_PIER_CAP`(材齡 14) |
| CS03 | 上部結構 | 120 | `G_MAIN` `G_EDGE_L` `G_EDGE_R` `G_DIAPH` |
| CS04 | 附屬 | 30 | 護欄四組 |

**承台繫梁與墩柱同一階段**:繫梁接的是兩根柱的**下端節點**,柱子不在就沒有東西可接。
要**逐跨架設**,把「主線」打開施工階段群組、切成每 8 根元素(= 40 m = 一跨)一組,
階段表就照 `G_MAIN_01`…`G_MAIN_12` 一跨一階段排。

### 三、螺旋人行步道(4 階段)

| # | 階段 | 天數 | 啟用 |
|---|---|---|---|
| CS01 | 中央柱 | 30 | `G_MAST`(材齡 28)|
| CS02 | 坡道與外柱 | 60 | `G_RAMP` `G_EDGE_I` `G_EDGE_O` `G_TREAD` `G_COL_GND` `G_COL_UP` |
| CS03 | 吊索 | 15 | `G_STAY` |
| CS04 | 護欄 | 20 | 護欄四組 |

**坡道與外柱放同一階段**是因為這個模型沒有臨時支撐:外柱的上端接在外緣上,坡道不存在就接不到。
這一階段代表「靠假設工程整體架設,再裝上永久外柱」。要模擬**逐圈懸臂上升**,
得把坡道切成每 36 根元素(= 一圈)一組,而且外柱與吊索也要跟著逐圈拆開 ——
本範例不做到那麼細。

> **v3.41 拿掉了邊界 / 荷載群組**(見 §16.13),所以上面三張表只剩結構群組。

### 驗收測試

三個範例各加五項(共 15 項):匯出群組名都是有意義的英數、有階段表(≥ 4 階段)、
階段指到的群組都存在、**每一個有元素的群組都被某個階段收走**(不能漏)、
階段名清洗後仍可辨識(`CS01_TOWER` 這種)。最後一項特別重要 ——
漏掉一個群組在 midas 裡就是「那些元素永遠不啟用」,而且不會有人告訴你。

---

## 16.13 施工階段的使用者回饋(v3.41)

實際用過三個範例之後的四點回饋,全部照做。

### 1. 已啟用的線要粗一點

時間軸開著的時候,已啟用的元素和背後那層淡灰分不出來。改成:
連接族 `1 → 2.1`、幾何族 `1.4 → 2.4`(**選取中的群組維持原本的 2.6 / 2.8**),
未啟用那層的灰再淡一階(`#ccd0d6 → #d5d9de`、alpha `.75 → .7`)。
另外時間軸開著時不再套「有選取就把其他組壓到 .55 alpha」那條規則 ——
兩種淡化疊在一起會讓已啟用的部分也看不清楚。

### 2. 滑桿改成「已經蓋到第幾階段」的計數

原本 `-1 = 全部顯示`、`k = 第 1…k+1 階段`,最左邊反而是全亮,直覺相反。改成:

| 值 | 意思 |
|---|---|
| **0** | 開工前,一根都還沒蓋(全部灰) |
| k | 第 1 … k 階段已啟用 |
| **拉滿(= 階段數)** | 不過濾,全部照常畫 |

`VIEWSTAGE = -1` 保留成「還沒動過滑桿」的初值,與拉滿同義;`stageVal()` 統一夾範圍。
**另外加四顆按鈕** `⏮ ◀ ▶ ⏭` —— 階段少的時候滑桿一格很大,用按鈕一階一階走比較準。
這四個操作只換視圖,**不進復原堆疊**。

### 3. 「啟用結構群組」改成挑選視窗

原本要自己打群組名(得先記得 `G_MAIN_03L` 這種東西)。改成群組**標籤 + 「+ 群組」按鈕**:

- 視窗列出所有有元素的群組,勾選即可;標籤上的 ✕ 直接移除
- **每一組會標出已經被哪個階段收走**(「已在 CS05 縱橫梁 DECK」)——
  一眼看得出還有誰沒排到,那正是階段表最容易出錯的地方
- 一顆「只勾還沒排入的」快速鍵
- 檢核面板同時多一條警告:**有元素卻沒被任何階段收走的群組**
  (在 midas 的階段分析裡會永遠不啟用,而且沒人會告訴你)

### 4. 拿掉邊界 / 荷載群組

**使用者的理由是對的,照做。** midas 的荷載多半跟結構綁在一起
(`beam element load` 要作用在特定元素上),支承也一樣 —— 這些都得**先把模型匯進 midas 才設得了**,
在這邊先開一堆空群組省不到任何事,反而讓階段表變寬、多四欄要維護。

移除的東西:

- `*BNDR-GROUP` / `*LOAD-GROUP` 兩個輸出區塊,以及匯出面板那兩個欄位
- 階段表的「停用結構群組 / 啟用邊界群組 / 啟用荷載群組」三欄
  (停用結構群組同時也被判定為「不太需要」)
- `*STAGE` 的 `DELEM` / `ABNDR` / `ALOAD` 三行,以及共用設定裡的 `POS` / `REDIST`
- 三個範例的 `BND_*` / `LD_SDL`

**`*STAGE` 現在只輸出 `NAME=` 與 `AELEM=` 兩種行。** 匯進 midas 之後,
在施工階段對話框裡補上邊界 / 荷載群組即可 —— 那本來就是要在 midas 裡做的事。

> 欄位註解仍然照手冊整段輸出(八行),和 `*ELEMENT` 一樣 —— 方便人工比對,
> 也留著日後要補回來時的參考。`mctText()` 的 `pair()` 是通用的,補回 `ABNDR` 只是兩行。

---

## 16.14 介面回饋(v3.42)

### 1. 時間軸只在施工階段面板作用

`STAGE_ACT = (SEL === "_stg") ? stageActive(VIEWSTAGE) : null` —— **一行,而且不可能漏掉路徑**。
原本想在「SEL 改變時把 VIEWSTAGE 歸位」,但 SEL 有十幾個寫入點(清單、圖上點選、四顆全域按鈕、
`selOnly()`、載入…),漏一個就會出現「離開了但畫面還是半灰」。
`VIEWSTAGE` 本身留著,回到面板還停在原來那一階段。

### 2. 線寬統一

非編輯中的群組:連接族 `1 → 2.1`、幾何族 `1.4 → 2.4`(編輯中維持 2.6 / 2.8),
淡化也放寬一級(`.55/.6 → .62/.7`、幾何族 `.8 → .85`)。
原本 1 px 的細線在群組一多就糊成一團,這不是時間軸專屬的問題,所以**不再分「有沒有開時間軸」**,
一律用同一組粗度。

### 3. 四個全域頁面改成開關

`togglePane(k)`:再按一次同一顆就關掉(`SEL = null`)。
舊版只能「點圖面」才退得出來 —— 按鈕點下去有反應、再點一次卻沒反應,很難不當成壞掉。

### 4. 左側群組欄可收合

標題列加一顆 **群組欄**,切 `#app.tcol`。

> **踩到的坑:`grid-template-columns` 有三份。** 基本版五欄、`@media(max-width:1180px)` 三欄、
> `@media(max-width:740px)` 一欄,而且 `.nosel` 各自還有一份。
> 只在最外層寫 `#app.tcol` / `#app.nosel.tcol`(五欄)的話,在 1180px 以下會用五欄的模板
> 配上媒體查詢裡「`#right` 放第 3 欄」的規則 —— **`#right` 被丟進寬度 0 的格子**,
> 畫面剩一條 25 px 的直條。三個斷點都要各自補一份 `.tcol`。

### 5. 圖上掃到的群組 → 左側清單打亮

`syncTreeHot(gid)` 在 `draw()` 裡呼叫,只在 id 真的變了才動 DOM;
`renderTree()` 重畫清單後把 `treeHot` 清掉,下一次 `draw()` 會補回來。
和既有的 `hoverTree`(清單 → 圖)是同一件事的兩個方向,顏色也用同一個藍 `#0f62fe`。

### 6. 畫布工具列補圖示

重新框選(四個角括號)、方位角(羅盤)、仰角(角度符號 + 弧)、Z ×(上下雙箭頭)。
按鈕沿用既有慣例「文字在前、圖示在後」;`.tag` 那三個因為後面接輸入框,改成**圖示在前**,
並把圖示調成 `--ink-3` 免得跟數值搶注意力。

### 16.14.1 「現在不看它」只有一種畫法(v3.43)

選了某個群組時,其他群組原本是**黑色**(`IDLE_COL = #161a20`);時間軸未啟用的元素是**淡灰**。
兩者其實是同一件事 ——「在那裡,但現在不是重點」—— 卻長得不一樣。統一成一組常數:

```js
const DIM_COL = "#d5d9de", DIM_W = 1, DIM_A = .7;
```

未選取時大家維持自己的顏色與 v3.42 那組粗度(連接族 2.1 / 幾何族 2.4);
**一旦選了誰,其餘全部退成 `DIM_*`**,和時間軸未啟用的那層完全一樣。

> **例外:點選模式(`PICK`)進行中不淡化。** 那時候要做的事正好是
> 「在**別的**群組上點一個節點 / 元素」(跨群組參照、補一根元素),
> 把它們畫成淡灰等於把靶子藏起來。`dim = anySel && !on && !PICK`。

## 16.15 位移複製的座標基準,與「延伸」開放給所有群組(v3.44)

三件事都是實際建模時撞到的:複本的座標欄位不是全域座標、複製方式的名字會誤導、
延伸只有「線」群組的直線段有。

### 1. 位移複製:純平移直接**併進座標參數**,不再疊在 `xf` 上

**症狀。** 用位移複製(dY = +0.52)做出來的群組,之後在面板裡輸入全域座標時，
結果整整差 0.52 —— 因為複本的內容是**原封不動抄過來的**,位移只記在群組的
`xf.dy` 上,建模時才在最後套上去。也就是說複本的每一個座標欄位講的都是
「原本那一份的位置」,不是全域座標。

**修法。** `dupGroups()` 判斷這次的位移是不是**純平移**(旋轉角 0、沒有鏡射);
是的話呼叫 `bakeShift(g, d)` 把位移加進這個群組**每一個絕對座標欄位**,`xf` 保持乾淨。
複本從此就是一份「座標本來就在那裡」的獨立群組。

只有**上游也一起複製了**的複本才併(`depIds(c).every(id => newIds.has(id))`)。
選「只複製這一個」時複本的形狀是從原群組算出來的,自己沒有座標可以改,
只能靠 `xf` 偏開 —— 那是那個選項的定義,不是 bug。

**`bakeShift()` 要穿過群組自己的變換。** 一個群組的變換順序是
**鏡射 → 繞中心旋轉 → 平移**。要讓「座標先加 d′」等於「原本的結果再平移 d」:

| 群組原本的 `xf` | 座標要加的 d′ | 說明 |
|---|---|---|
| 沒有鏡射(有沒有旋轉都一樣) | `d′ = d` | 旋轉中心跟著座標一起搬,所以自動相容 |
| 有鏡射、沒有旋轉 | `d′ = d`,鏡射軸那一項變號 | 鏡射先做,平移要先被鏡射「翻」回去 |
| 鏡射又帶旋轉 | 無解 | 沒有單一等效平移,`bakeShift` 回 `false`,整組退回 `xf` |

自訂旋轉中心 `cx/cy/cz` 要跟著加 d′;**鏡射平面 `mirAt` 反而不動**
(d′ 的定義就是讓鏡射平面不必動)。段自己的 `xf` 同一套規則各算各的,
任何一段辦不到就整組不併 —— 半數併進座標、半數留在 `xf` 只會更難懂。

**哪些欄位算「絕對座標」:** 段的 `x1/y1/z1`、`x2/y2/z2`;圓弧的圓心 `cx/cy`
與通過點 `mx/my/mz`;懸垂 `fBase = "z"` 時的基準高程 `fz`;螺旋的圓心;
線形鏈的起點、IP 表的 `x/y`、`ipEndX/ipEndY`、縱斷面 VPI 的 `z`;
垂落的 `targetZ` 與斜面過點 `px/py/pz`;扇出的自訂起點 `sx/sy/sz`;
匯入組的節點座標;手動節點 `addN`;節點座標覆寫 `edits`。
樁號、半徑、緩和曲線長、偏置量 `d/h`、垂跨比這些**相對量一律不動**。

> **貼上的座標表(POLY 段)是唯一的例外。** 不去改寫使用者貼進來的文字,
> 位移改記在**那一段自己的平移**上。表是使用者的原始資料,重寫會把格式與註解洗掉。

**舊檔怎麼修。** 群組的「平移旋轉…」視窗新增一顆
**「把平移併入座標」**(`bakeXfShift()`):把 dX/dY/dZ 加進所有座標欄位再把平移歸零,
**幾何完全不變**,但之後看到、輸入的座標就是全域座標。v3.43 以前做出來的複本用這顆鈕修。

### 2. 「獨立 / 連動」改名

原本的講法自相矛盾 —— **「獨立」卻要連別的群組一起複製**,看起來像寫反了。
名字直接講**做了什麼**,理由留給說明文字:

| 舊 | 新 |
|---|---|
| 獨立 —— 連 X 一起複製,複本自成一套 | **整套複製** —— 連 X 也各複製一份,複本自成一套 |
| 連動 —— 只複製這一個群組,仍然掛在原來的 X 上 | **只複製這一個** —— 複本的形狀仍然由原來的 X 決定 |

沿線陣列與多選複製的文案一起改。

### 3. 「延伸」開放給所有群組

延伸的定義三種群組完全一樣,而且**只有一種**:

> 沿**元素自己的方向**往外**多接一根元素**。
> **被延伸的那根元素、以及原本的節點完全不動**,延伸出來的節點與元素都算這個群組的。
> 只吃 0 或正值(要縮短請改幾何)。

| 群組 | 兩端的欄位 | 作用對象 |
|---|---|---|
| 線(直線段) | `extS` / `extE` | 那一段(延伸段照該段的分割規則再切,舊行為) |
| 偏置 | `extS` / `extE` | 整條節點串的頭尾 |
| 對接 | `extA` / `extB` | **每一根連桿**的起始群組端 / 目標群組端 |
| 垂落 | `extT` / `extB` | 每一根的上端 / 下端(下端延伸 = 墩柱再往下插進基礎) |
| 扇出 | `extS` / `extE` | 每一根的起點端 / 目標端 |

**方向取哪一段。** 一律取**最靠近那一端的那一根元素**的方向:偏置取節點串最外側那一根;
扇出有垂度時取最靠近那一端的那一小段(取整根弦線會歪掉)。

**連接族的延伸排在整組平移旋轉之後算**(`buildOne()` 裡 `addExtElems()` 在 `xfApplyNodes()` 之後)。
這些元素有一端(對接是兩端)在**別的群組**,整組平移只動得到自己的節點;
延伸段必須貼著「搬完之後」的那根元素,否則群組一被搬動延伸段就會歪掉或脫開。
偏置則相反 —— 節點全是自己的,延伸直接排進節點串裡(`pts.unshift` / `push`),
順序、編號、下游的「第 k 個節點」參照才不會亂;斷點旗標 `brk` 要跟著移到新的第一個節點。

### 4. 直線段:兩端被別的段釘住時**照樣可以延伸**

v3.43 以前,起點接續前段、或終點被別的段釘住時,`extS` / `extE` 會被**無條件忽略**
(理由寫的是「再往外推會把接縫扯開」)。但延伸的定義本來就是「原本的東西不動、往外多接」,
接點還是接點,沒有什麼會被扯開。現在兩端一律可以延伸。

要付的代價只有一個:**共用接縫節點的最佳化要關掉**。共用的前提是
「兩段的那一個節點真的是同一點」,做了延伸就不成立了:

* 起點端延伸 → 本段的第一個節點跑到延伸段最外端,不再是接縫那一點;
* 前一段做了終點端延伸、**而且它的終點又被別的段釘住** → 它的最後一個節點也在延伸段最外端。
  (終點沒被釘住時,接縫本來就跟著延伸出去,共用仍然成立 —— 這是舊行為,不要動它。)

這兩種情況 `cont = false`,接縫變成**兩個重合節點**,由「合併重合節點」併回去
—— 和「起點指到不相鄰的段」用的是同一套機制,不是新風險。關掉合併就會看到重複的點。

**錨點(別的段講的「本段起點 / 終點」)。** 一般仍然是這一段幾何上的端點(含延伸);
但**被釘住的那一端例外**,錨點就是那個釘住的點,不是延伸段最外端 ——
否則一延伸就把對方拖著跑。

## 16.16 檢核訊息改成可收合(v3.44)

錯誤與警告本來全部攤在 3D 視圖上方。一個像樣的模型很容易同時有七八則
(合併重合節點、弦線偏差、節點沒排進階段…),而且**每一則都是讀過一次就知道的事**,
卻一直佔著預覽區將近四分之一的高度。

**做法:一條計數列 + 可收合的清單。**

* 收起來時只剩 `▸ ✕ n 個錯誤 ⚠ n 個警告` 一條約 28 px 的列(展開時 189 px,
  預覽區從 465 px 長回 622 px)。點一下切換。
* **只有出現「沒讀過的訊息」時才自己攤開**,其餘時候維持使用者上次的選擇 ——
  重算不會把收起來的清單彈開。新出現的那幾則旁邊掛一個**「新」**標籤,
  十則裡面一眼就找得到是哪一則變了。

**「讀過」怎麼判斷。** 把訊息裡的數字抽掉再比(`msgKey`:`/[0-9]+(\.[0-9]+)?/g → "#"`)。
同一則警告只是數量從 3 變成 4 **不算新訊息** —— 否則拖一個數值就會一直彈開。
真的換了一種警告才算。`MSG_SEEN` 是整個 session 累積的,不是「上一次算完的那批」:
訊息在中間狀態短暫消失又出現時才不會被誤判成新的。

> **啟動時要把 `MSG_SEEN` 清掉。** 驗收測試在開檔時就跑完了,途中會借用真正的
> `state` / `MODEL` 建一堆模型,冒出來的警告會被記成「讀過」。清在
> `runTests()` 之後、最後那次 `recompute()` 之前。

**版面細節:** `.msghead` 是 flex,子項一律 `white-space:nowrap` 並允許整條 `flex-wrap`
—— 右欄拉窄時中文會被壓成一個字一行,一條計數列變成 170 px 高。
清單本身不再自己捲(外層 `#right > #msgs` 已經有 `max-height:24%` + `overflow:auto`),
巢狀捲軸只會更難用。

## 16.17 「第 k 個節點」在合併重合節點之後跳號(v3.44.2 修正)

**症狀。** 在 3D 視圖上點一個節點當某一段的起點 / 終點,**實際連到的是別的節點** ——
通常是往前差一到數個位置。上游被合併掉愈多節點,差得愈多。

**成因:兩邊數的是不同時間點的陣列。**

```
buildModel()
  ├─ buildOne() × N ── 這裡解析跨群組參照(gRefPt / nodeSetOf)  ← 合併「之前」
  ├─ addManualElems()
  └─ weldNodes()  ── r.nodes = r.nodes.filter(沒被併掉的)        ← 陣列在這裡變短
MODEL = 上面的結果                                                ← 合併「之後」
  └─ locateNode() / chainCount() 從 MODEL 數第 k 個               ← 面板與圖上點選
```

`weldNodes()` 只留下每個位置編號最小的那一顆,被併掉的從**它原本那一組**的 `r.nodes` 移除。
所以只要上游有任何一個節點被併走,「面板數出來的第 k 個」就比「建模時取的第 k 個」少算幾格。
人行吊橋範例的塔架就差 4 格(17 → 13)。

> 存序號而不是節點編號是刻意的(§16.4:上游重新分割時編號會跑掉,序號不會)。
> 問題不在序號本身,而在**兩邊數的不是同一串**。

**修法:序號一律以「合併之前」為準,而且在建模當下就釘死。**

`buildOne()` 最後呼叫 `stampChainIdx(R)`:把 `n.ci = 這一組節點串裡的第幾個` 寫在節點上,
並把各側(`own` / `i` / `j` / `all`)的數量記進 `R.preN`。之後:

* `locateNode()` 回傳 `n.ci`,不再從 `MODEL` 重數;
* `chainCount()` 讀 `R.preN`,不再數合併後剩下的節點。

被併掉的節點**在圖上其實還看得到**(留下來的那一顆就畫在同一個位置),
所以「合併前的序號」正好就是使用者照著圖數出來的那個序號 —— 兩邊因此一致。
留下來的那一顆本身帶著它自己那一組的 `ci`,所以點到重合位置時
拿到的是「留下來那一組的第 k 個」,落點仍然完全正確。

**順帶修掉的:** 連接族的「起算 / 到第幾個節點」欄位上限本來也是數合併後的陣列,
比建模時實際能取到的少 —— 現在同樣走 `R.preN`。

**回歸測試(W1)。** P 先建佔住 x=50,A 是 0→100 切 4 段,A 在 x=50 那顆會被併掉:
合併後陣列剩 4 個、`preN.own` 仍是 5;最右邊那顆的 `ci + 1 = 5`;
用 `@A:5` 接過去落在 x=100(點到的那一顆),而 `@A:4` 落在 x=75
—— 後者就是修好之前會發生的事。

## 16.18 時間軸過濾時「參考曲線」會露出群組自己的顏色(v3.44.3 修正)

**症狀。** 匯入舊模型、再加幾個群組之後進施工階段拉時間軸,
那些**還沒蓋到的新群組**會在一片淡灰裡露出自己的顏色。

**成因:淡化與過濾都只作用在「元素」上,參考曲線是另一層。**
線群組會畫兩層:

| 層 | 畫的是什麼 | 有沒有吃過濾 |
|---|---|---|
| 元素折線(`elemPolysBy`) | 節點串出來的實際元素 | ✔ 逐根 `stageOffE(e)` 過濾 |
| **參考曲線(`curvePolys`)** | **段的數學曲線本身**(淡、1.2 px、α .28) | ✘ 完全沒有 |

參考曲線是用**群組自己的顏色**畫的,而且畫的是段的參數曲線、不是元素,
所以既不吃時間軸過濾,也不吃 §16.14.1 的「其他群組退成淡灰」。
新加的線群組通常還沒排進任何階段,那條彩色的線就特別顯眼
—— 舊模型是匯入組(沒有段參數),對比之下更像是「只有新的那些不對」。

**修法:參考曲線只有「現在是重點」的群組才畫。** 規則直接沿用既有的 `dim`:

```js
if (!dim && !STAGE_ACT){ …畫參考曲線… }
```

* 淡化中的群組不畫 —— 它的元素已經只剩一條淡灰線,再疊一條自己顏色的曲線就是雜訊。
* 時間軸在過濾時一律不畫 —— 那個畫面問的是「蓋到哪」,不是「弦線差多少」。
  (階段群組會把同一個群組的元素拆到不同階段,所以「只畫已啟用的那一段參考曲線」
  得把參數曲線裁開;那條曲線在這個畫面本來就沒有價值,不值得為它做裁切。)
* 沒有選取、也沒有過濾時完全照舊 —— 參考曲線是弦線偏差的視覺提示,不能矯枉過正。

**順帶修掉的兩個同類問題:**

* 座標被改過的節點記號(琥珀色圓點)本來用 `r.nodes`,不吃時間軸過濾,
  還沒蓋到的群組會留下一排點 —— 改用已經過濾好的 `vNodes`。
* **匯入組不再畫參考曲線。** 它沒有段參數,`curvePolys()` 會退回
  「把節點依序連起來」——但匯入組的節點是一份死清單、不是沿著曲線的一串點,
  連起來只會在舊模型上疊一團淡淡的鋸齒。

**回歸測試(W2)。** 直接攔 canvas 的 `stroke`,只記「真的畫出線」的那些
(空路徑的 `stroke()` 什麼都不會畫,不能算)。四個斷言:過濾時已啟用的群組照樣有自己的顏色、
還沒蓋到的群組**完全沒有**自己的顏色、還沒蓋到的部分用統一的 `DIM_COL`、
沒有過濾時兩組都畫得出來(沒被矯枉過正),再加一個「選到某一組時其他組也不露色」。

> 測試會先用行內樣式把畫布撐成 640×320 再量,量完拿掉 ——
> `draw()` 在畫布沒有版面尺寸時直接 `return`,否則這個測試會**看視窗寬度而定**
> (窄視窗下 `cv.clientWidth` 是 0,四個斷言會全部假性失敗)。
